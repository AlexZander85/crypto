# Приёмка v15: верификация исполнения всех пунктов доработки

**Дата приёмки:** 4 сентября 2026
**Объект:** `index_v15.html` (7,42 МБ → 7,63 МБ, +518 КБ), commit `ddc7ee5`
**Метод:** побайтовые md5-сравнения всех банков данных с принятой v14; статическая верификация каждого маркера; runtime-испытания в headless Chrome (реальные клики по лабам, квизам, тестам, движкам); проверка заявлений против кода и против ТЗ `SPEC_PYTHON_LIKBEZ_v1.md`

---

## 1. Резюме

| # | Пункт доработки | Вердикт | Оценка |
|---|---|---|---|
| 0 | Регрессия контента v14 → v15 | **НЕ ОБНАРУЖЕНА** — 15 банков + глоссарий md5-идентичны | ✅ PASS |
| 1 | Python-модуль (14 уроков) | Реализован полностью и работает вживую; 4 отклонения от ТЗ (не блокирующие) | **9/10** |
| 2 | Дефекты V-1…V-6 и C-1…C-4 | **Закрыты все 10** (V-6 — через документацию) | **10/10** |
| 3 | Новые механики (11 позиций) | Все реализованы; Pyodide работает в fallback-режиме | **9/10** |
| 4 | Техзамечания (5 позиций) | Закрыто 3 из 5; learn-bottom и Помодоро — нет | **3/5** |
| 5 | Детерминированный помощник | DeterministicCopilot «Второй пилот» сшит со всеми новыми системами | **10/10** |
| 6 | InfographicSvgEngine | Все 6 заявлений подтверждены кодом и рантаймом | **10/10** |

**Итог: ПРИНЯТО УСЛОВНО.** Функциональный слой — качественно исполненная работа с реальной глубиной (не косметика). Блокирующих дефектов контента нет. Не блокируют, но требуют фикса: **3 упавших smoke-теста** (самодиагностика платформы впервые стала «красной»: 67/70), отсутствие `pyodide_quant_worker.js` (Pyodide никогда не стартует), py-узлы не заведены в Граф Знаний, гейт аттестации py-уроков мягкий.

---

## 2. Регрессия контента — PASS (побайтово)

| Банк | v14 | v15 | Статус |
|---|---|---|---|
| LESSONS (76) | 469 459 B | 469 459 B | ✅ md5 идентичен |
| MATH_LESSONS_SOURCE (48) | 133 659 B | 133 659 B | ✅ идентичен |
| PSY_LESSONS + _2 (56) | 196 030 / 197 446 B | то же | ✅ идентичны |
| VIBECODING (4), V4_CONTENT (2), EARNING (12), QUANT (10) | — | — | ✅ все идентичны |
| MATH_ADAPTIVE_TOPICS, LESSON_PHASE_TOPICS, SIMPLE_LAYER, SIMPLE | — | — | ✅ идентичны |
| PHASE_TESTS (83 вопроса) | 74 322 B | 74 322 B | ✅ идентичен |
| QUIZ_CASES | 3 215 B | 3 215 B | ✅ идентичен |
| TERMS_RAW (321 термин) | 112 736 B | 112 736 B | ✅ идентичен |
| CATS | 8 категорий | 9 (+`code: 'Код и автоматизация' #34d399` — ровно по ТЗ §6.4) | ✅ только добавление |
| **Runtime `LESSONS.length`** | 213 | **227** | ✅ = 213 + 14 py |

Дельта файла +518 КБ — целиком новые модули (Python-банк 68 КБ, движки, UI). Ни один старый объект не изменён.

---

## 3. Пункт 1 — Python-модуль: 9/10

### 3.1. Что подтверждено (статика + рантайм)

**Структура (ровно по ТЗ):**
- 14 уроков `py_l1`…`py_l14`, num `Py-01`…`Py-14`, банк `PY_LESSONS_BLOCK_A` (7) + `PY_LESSONS_BLOCK_BC` (7), мерж в LESSONS на рантайме
- Цепочка `prerequisites` ациклична: py_l1 ← p0_l13 (обоснованно — «вероятность с нуля» пройдена), далее строго предыдущий; py_l12 ← py_l11, py_l14 ← py_l13
- Состав каждого урока: 2×concept (с `simpleAnalogy`) + `interactive_code_lab` + warn + `interactive_quiz_cbm` + recap — 14/14
- Заголовки соответствуют титульнику ТЗ (порядок, темы, время 6–12 мин, суммарно 116 мин)
- Режимы лаб: `run×2, edit-value×7, fill-blank×2, edit-code×3` — все 4 режима ТЗ задействованы; **в каждой лабе ровно 3 уровня подсказок**
- Квизы: 14/14 с массивом `why_wrong` (пост-факторные объяснения каждого дистрактора — по ТЗ §4.3)
- PY_TEST: **15 вопросов** (8+7), categories python_basics/python_tools/python_market, threshold 80, чип «🐍 Тест: Python (Этап A2)» в UI тестов, регистрация в PHASE_TESTS — подтверждено рантаймом
- PY_TERMS: 18 глоссарных терминов с полями {t, c:'code', d, a, w} — аналогии по ТЗ («Кулинарный рецепт», «Экран приборной панели»)
- CN_TRACKS: этап **A2 между A и B** (порядок A→A2→B→C→D→E→F), gate `py_test`, coreSet дополнен — биекция 93+134=227 проверяется новым тестом

**Рантайм-испытания (реальные клики):**
- Плеер открыл py_l1: 12 шагов, обложка с хуком «Твой бот не спит, не боится и не жадничает…», инфографика-шаг, словарь, 2 теории, лаба, warn, квиз, итоги, кейс, Фейнман, финал
- **Лаба py_l1 (run) — полный цикл работает:** панель «🐍 Скрипт бота (Python)» с подсветкой синтаксиса, чипы вариантов (Депозит/Старт торгов/Стоп), кнопка «Выполнить ▶ (Enter)», кнопки «💡 Подсказка (H)» и «↺ Сбросить»; после клика — консоль: `● Успешно · root@cryptonavigator:~/bot# python main.py · ⚡ 0.002 с · Привет, рынок! Мой депозит: 1000$`
- **Квиз py_l1 — полный CBM-цикл:** бейдж «🎯 ПРОВЕРКА CBM С УВЕРЕННОСТЬЮ», селектор Гарднера-Медвина (C=1: +1/0, C=2: +2/−2, C=3: +3/−6, подпись «Мат. ожидание максимально при честной оценке»), после выбора C=3 и ответа — «✓ Верно! 🎯» + объяснение
- Тест этапа A2 активируется из UI (`py_test_chip` → `chip active`)
- `InfographicSvgEngine.generateSvg()` выдал валидный SVG для выборки из 7 треков (p0/p1/ps/ft/vc/py/p6) — 7/7

### 3.2. Отклонения от ТЗ (не блокирующие)

| # | Отклонение | Влияние |
|---|---|---|
| O-1 | **Гейт аттестации мягкий:** шаги-врата (`kind='quiz'`) строятся только из поля `l.quiz`, которого у py-уроков нет; квиз_cbm рендерится как widget. Урок можно завершить, не отвечая на квиз | Среднее: ослаблена педагогика «порог мастерства» для модуля входа |
| O-2 | Квиз 1/урок вместо 3 (ТЗ §3.4); PY_TEST без `why_wrong` | Низкое |
| O-3 | `phase: 'py'` (строка) вместо числа 9; `cognitiveLoad` все `easy` вместо easy×9+medium×5 | Низкое — фильтры адаптированы, работает |
| O-4 | `CN_TRACKS.version` не поднят (13); py_l14 — стратегия «покупка на просадке», а не пересечение MA (ТЗ §5 Py-14) | Низкое — миграция не сломана, но bump рекомендуется при следующем релизе |

---

## 4. Пункт 2 — Дефекты визуального и контентного слоя: 10/10 закрыты

| № | Дефект из аудита | Статус | Доказательство |
|---|---|---|---|
| V-1 | Конфетти отсутствует | ✅ **ЗАКРЫТ** | `window.ConfettiEngine`: canvas-эмиттер (z-index 999999), частицы с физикой (vx/vy/rotation), палитра 6 цветов, **guard `prefers-reduced-motion`** + переключатель `cn_confetti_enabled`, 3 триггера (в т.ч. успех mastery-теста: `fireConfetti({count:120, duration:3500})`) |
| V-2 | Светлая тема не покрывает вложенные панели | ✅ **ЗАКРЫТ** | 120 атрибутов `data-lp-theme`, ~300 light-правил, включая `.lp-kg-overlay`, `.lp-tools-shelf`, `.lp-tool-tile`, вложенные панели лабораторий (плюс дубль через `body.light-theme`) |
| V-3 | «Тритон» — не диссонанс | ✅ **ЗАКРЫТ** | `error()`: `var freqs = [440.00, 622.25]` — аутентичная диссонансная пара A4+D#5, два sine-осциллятора; ченджлог документирует |
| V-4 | Pink Noise Sweep отсутствует | ✅ **ЗАКРЫТ** | `step()`: настоящий розовый шум (фильтр Пола-Келлета: b0/b1/b2 коэффициенты) через `createBuffer` + `createBiquadFilter` lowpass — звучит на каждой смене шага |
| V-5 | Хоткеи без guards | ✅ **ЗАКРЫТ** | prefers-reduced-motion ×5 (было ×2), guard полей ввода (`inField`, `inTa`/`isContentEditable`) у H и Enter; Ctrl+Enter с `preventDefault/stopPropagation` |
| V-6 | Доки ≠ код (частоты, фон) | ✅ **ЗАКРЫТ документированием** | Код не изменён (клик 800→200 Гц/35 мс, фон #0d1022), но ченджлог теперь описывает фактические значения — рассинхрон реестра устранён в пользу «код — источник истины» |
| C-1 | Инфографики: 16/76, растр, нет зума | ✅ **ЗАКРЫТ** | См. §8 (Пункт 6): SVG-движок + тумблер + лайтбокс + fallback |
| C-2 | Мёртвые вызовы LearnAudio.play() | ✅ **ЗАКРЫТ** | `play: function(name)` с маппингом click/success/failure/error/advisory/caution/milestone/chime/step; вызовов play() теперь 14 (было 3 мёртвых) |
| C-3 | SPHINCS+/Falcon отсутствуют | ✅ **ЗАКРЫТ** | Карточки «2. Falcon (FN-DSA) — Решётки NTRU, 897 байт» и «3. SPHINCS+ (SLH-DSA) — деревья хешей, 7 856 байт» + сравнительная таблица трёх алгоритмов (с Dilithium/ML-DSA) с NIST-уровнями и размерами подписей |
| C-4 | Mt.Gox нет в Chaos Lab | ✅ **ЗАКРЫТ** | Полноценный реплей `mtgox_collapse` («Крах биржи Mt.Gox: Проблема пластичности транзакций (2014)»): таймлайн событий (7 февраля 2014, остановка выводов), 4 варианта решений с эффектами на баланс и новостями, стартовый баланс 10 000 $ |

---

## 5. Пункт 3 — Новые механики: 9/10 (11/11 заявлены, все найдены, 1 с оговоркой)

| Механика | Статус | Доказательство (код/рантайм) |
|---|---|---|
| CBM × симуляторы («прогноз перед прогоном») | ✅ | `LabPredictionEngine`: HYPOTHESES, getPrediction, savePrediction, **mountWithPrediction**; фиксация C∈{1,2,3} перед прогоном стенда, сверка «ожидал/получил», Brier Score (125 упоминаний, интеграция с CBMSystem) |
| FSRS поверх SM-2 | ✅ | Правило C=1 → «вернётся через 24 часа в виде изоморфного сценария с другой торговой парой и рыночной фазой» (UI-плашка), `IsomorphicVariationEngine` (+getStats с isomorphicCount, очередь due), глобально — кнопка «🧠 Начать повторение (SM-2 + FSRS)» подтверждена рантаймом |
| Сократические подсказки (H) | ✅ | `SocraticHintSystem`: db, activeLevels, **usedInContext**, **cbmPenaltyActive** (цена подсказки — аннулирование CBM-бонуса), cycleLevel/getHintData/getCurrentLevel; кнопки «💡 Подсказка (H)» на квизах, лабах, numeric; hotkey H с guard полей ввода |
| Keyboard-first до бенчмарка | ✅ | Ctrl+Enter (сабмит квиза/теста/лабы — 3 независимых обработчика), 1/2/3 (CBM), R (сброс лабы), Space — хоткей-панель в Tool Shelf («⌨️Хоткеи (?)» подтверждена рантаймом) |
| Иерархия тревог FAA/MIL-STD | ✅ | WARNING (20)/CAUTION (10)/ADVISORY (6)/INFO (9), `LearnAudio.advisory()`, warn-блоки py-уроков размечены (`severity: 'fatal'`, заголовок «🚨 WARNING · Скобки обязательны»), Copilot-индикация тех же уровней |
| **Pyodide/WASM-песочница** | ⚠️ **9/10** | `PyodideQuantManager` — полноценный менеджер: Web Worker, статусы (uninitialized/ready/fallback), `runCode/runChallenge/runBacktest`, блоки `QUANT_LESSON_BLOCKS` (p1_l1_quant_cell), режим procode «⚡ Pyodide CPython 3.12 в Web Worker». **НО: файла `pyodide_quant_worker.js` нет в репозитории** → Worker load fails → постоянный fallback `engine: 'fastjs'` (подтверждено рантаймом: state 'uninitialized', engine 'fastjs'). Архитектурно всё готово, реальный CPython не исполняется никогда |
| Конструктор стратегий | ✅ | `VisualStrategyBuilder.openModal` + кнопка «🧩 Конструктор 2.0» в шапке плеера (подтверждена рантаймом), бейдж «🧪 No-Code Visual Backtester», 30 упоминаний |
| Latency-телеметрия + Debriefing | ✅ | «Разложение задержки (Strategy/Network/Matching) и покадровый разбор проскальзывания», `latency_debriefing_css` блок, интеграция с FT-лабораториями |
| IRT-калибровка | ✅ | «IRT-калибровка сложности (Rasch Model 1PL & Khan Mastery Architecture)» с прямой ссылкой на ресёрч №1 п.44; накопление статистики попыток |
| Mastery 90%+ и бесконечные пересдачи | ✅ | «KHAN MASTERY FRAMEWORK (90%+ PASS THRESHOLD & INFINITE RETRIES ENGINE)», метки порогов 80%/90% в прогресс-баре, поздравление «Фаза доведена до 90%+ Khan Mastery», 214 упоминаний mastery |
| WCAG 2.1 AA | ✅ (частично верифицируемо статикой) | aria-label ×145, aria-live ×7, role="dialog" ×3, упоминания контраста 4.5:1 ×5, prefers-reduced-motion расширен |

---

## 6. Пункт 4 — Технические замечания: 3/5

| Замечание | Статус | Детали |
|---|---|---|
| learn-bottom: ранний return | ❌ **НЕ ИСПРАВЛЕНО** | `display:none !important` остался, `renderBottom()` по-прежнему строит панель на каждом шаге, `LP4_HEADER_NAV` — 0 вхождений. Работает как работало, циклы тратятся |
| Двойной Помодоро | ❌ **НЕ ИСПРАВЛЕНО** | Старый `window._pomodoro` + `pomodoro_btn` (togglePomodoro, 25:00 — подтверждён рантаймом в шапке приложения) живут параллельно с lp-pomo ×15 в плеере; синхронизации нет |
| Светлая тема вложенных панелей | ✅ | См. V-2 |
| Версионирование | ⚠️ **ЧАСТИЧНО** | `<title>` обновлён: «КриптоНавигатор **v14 — 227 уроков**» (было v12.3/213) ✅; но файл называется v15, в нём 0 упоминаний v15, а 5 остатков «v12.3» сидят в HTML-комментариях changelog и `CN_V11.version:'v12.3'` |
| Аудио-API: алиас + константы | ⚠️ **ЧАСТИЧНО** | Алиас `play(name)` ✅ (закрывает C-2); частоты по-прежнему inline (440/622.25 внутри метода), именованных констант нет — «доки и код перестанут расходиться» решено только документированием |

---

## 7. Пункт 5 — Детерминированный помощник: 10/10

`window.DeterministicCopilot` («Второй пилот») — заявленные характеристики подтверждены:
- **Автономность:** «100% автономный, 0 мс задержки, 0 сетевых запросов, 0 галлюцинаций» — API не содержит сетевых вызовов
- **Реактивная координация:** обработчики `onLabPrediction`, `onFSRS`, `onSocraticHint`, `onCodeLab`, `onDecisionQuest` — помощник получает события от всех пяти новых систем (и симуляторов, и повторения, и подсказок, и лаб)
- **Управление:** notify/dismiss/getState/setEnabled/setSoundEnabled — включаемость и звук регулируются
- **Авиационная индикация:** уровни ADVISORY/CAUTION/WARNING/SUCCESS согласованы с иерархией тревог (пункт 3)
- **Присутствие в интерактивах:** кнопки «Объяснить»/«Подсказка (H)» в лабах, квизах и numeric-задачах; в py-лабах Copilot-лейбл «Второй пилот (H)» вшит в рендер

---

## 8. Пункт 6 — InfographicSvgEngine: 10/10

Все шесть заявлений подтверждены кодом и рантаймом:

1. **Автономный процедурный движок** `window.InfographicSvgEngine` — есть; `generateSvg(l)` работает от метаданных урока; **рантайм: 7/7 валидных SVG** на выборке из всех треков (p0/p1/ps/ft/vc/py/p6)
2. **Классификатор `detectCategory`** (7 категорий рынка) и **генератор пиктограмм `renderGlyph`** — есть
3. **`extractTriad`** — извлечение триады «Фактор → Механизм → Защита» из terms/concept/recap — есть
4. **`infographicHtml(l)`** — гарантированный рендер SVG + кэш `window._lpSvgCache[l.id]` + тумблер «[🖼️ Оригинальный арт | 📐 Векторный чертёж]» (`toggleInfographicView`) для уроков с растром (LP_INFOGRAPHIC_MAP) — есть
5. **Лайтбокс `openInfographicLightbox`** — fullscreen-зум чистого SVG (Retina/4K без замыления), закрытие по Escape — есть
6. **Fallback в renderCard** — `if(t === 'image')` подставляет `InfographicSvgEngine.generateSvg()` при отсутствии/ошибке загрузки внешнего файла — есть

**Покрытие шире заявления:** шаг «📊 Инфографика» пушится в **каждый** из 227 уроков (заявлялось 76 уроков Фаз 0–6) — генерация процедурная, вес файла не растёт. Все тексты чертежей на русском.

---

## 9. Новые дефекты приёмки (регресс самодиагностики)

### R-1 · P2 · Smoke-набор стал красным: 67/70 (в v14 было 70/70)

Три теста пали на устаревших константах — платформа впервые сообщает о собственной «поломке»:

| Тест | Причина падения | Фикс |
|---|---|---|
| `lp3:program-213` | требует `LESSONS.length === 213` (сумма числовых фаз), факт 227 | переименовать в program-227, считать также фазу `py` |
| `trk:api` | требует `CN_TRACKS.coreCount === 79`, факт 93 (+ хардкод `of: 79` в stats) | 79 → 93 |
| `lp:steps-213` | для py_l1…py_l14 «нет шага аттестации» — билдер ищет `kind='quiz'/'numeric'`, а квиз py — виджет | либо признавать widget с `interactive_quiz_cbm` аттестацией, либо добавить py-урокам поле `l.quiz` |

Фикс — ~30 минут: три строки + опционально поле урока. Пока тесты красные, регрессионный щит платформы обесценен (любая будущая поломка утонет в известных «красных»).

### R-2 · P3 · Граф Знаний без py-узлов

КГ остался 154 узла / 6 кластеров / pills «Все (154)» — кластер «Код и автоматизация», 14 py-узлов и 17 рёбер из ТЗ §6.5 не добавлены. Python-модуль невидим в карте знаний.

### R-3 · P3 · `pyodide_quant_worker.js` отсутствует в репозитории

Менеджер ссылается на `'pyodide_quant_worker.js'`, файла нет → Worker создаётся с ошибкой → перманентный fallback `fastjs`. Заявленный «Pyodide CPython 3.12 в Web Worker» в этой поставке не исполняется ни разу.Graceful-деградация работает тихо — пользователь не видит разницы, но и pandas в браузере недоступны. Нужен файл воркера (или инлайн-воркер через Blob) + CDН-загрузка Pyodide по требованию.

---

## 10. Числовое резюме приёмки

- Банков данных проверено md5: **16** — все идентичны (0 деградации)
- Новых уроков: **14** (227 в рантайме) · лаб: 14/14 с 3 подсказками · квизов: 14 с why_wrong · PY_TEST: 15 вопросов @80%
- Дефектов аудита закрыто: **10/10** (V-1…V-6, C-1…C-4)
- Новых механик реализовано: **11/11** (Pyodide — с оговоркой R-3)
- Движков нового поколения в рантайме: ConfettiEngine, LabPredictionEngine, IsomorphicVariationEngine, SocraticHintSystem, DeterministicCopilot, VisualStrategyBuilder, PyodideQuantManager(fallback), InfographicSvgEngine, LearnAudio+step/advisory/play — **9 из 9 живые**
- Smoke: **67/70** (3 хардкод-регресса R-1)
- Ошибок консоли при загрузке и прохождении py-урока: **0**

---

## 11. Рекомендуемый план устранения (до объявления v15 финальной)

1. **R-1 (P2):** обновить три smoke-теста (program-227, trk:api 93, steps: признать CBM-квиз аттестацией) — вернуть 70/70
2. **O-1 (P2):** жёсткий гейт py-уроков — добавить py-урокам `l.quiz`-совместимый шаг-врата либо учитывать `interactive_quiz_cbm` в `quizPassed()`/`lastGate`
3. **R-3 (P3):** добавить `pyodide_quant_worker.js` (+ загрузка Pyodide с CDN по требованию и кэш) — иначе переименовать заявление в «быстрый движок симуляции (Pyodide — следующий этап)»
4. **R-2 (P3):** кластер «Код и автоматизация» в Графе Знаний (14 узлов, 17 рёбер по ТЗ §6.5)
5. **Пункт 4 хвосты (P4):** ранний return в renderBottom; синхронизация/удаление старого Помодоро; подчистить v12.3 в комментариях и CN_V11.version; поднять CN_TRACKS.version до 14; вынести частоты аудио в именованные константы
