# КриптоНавигатор — Полный лог изменений: Ванильная v2026-08-23 → Финальная V5

> **Назначение документа.** Это исчерпывающий реестр всех изменений, внесённых в `index.html` между первоначальной («ванильной») версией, отданной на аудит, и финальной версией V5. Документ предназначен для проверяющего агента: имея на руках ванильную версию и V5, агент должен иметь возможность подтвердить или опровергнуть реализацию каждого пункта.
>
> **Как читать.** Изменения сгруппированы по итерациям (V1 → V2 → V3 → V4 → V5). Каждая запись содержит: идентификатор, суть, технические якоря для поиска в коде, и затронутые ключи localStorage. Все ссылки на функции/массивы/строки — точные для V5 (`/tmp/crypto-repo/index.html` @ `main`, merge-коммит `81a6737`).

---

## 0. Базовая линия (Vanilla v2026-08-23)

Исходная версия: коммит `2ca976e`, файл `index.html` **25 784 строки / ~1.43 МБ**, однофайловая SPA на чистом HTML/CSS/JS (без фреймворков, тёмная тема, русский язык).

| Метрика | Vanilla | V5 |
|---|---|---|
| Строк в index.html | 25 784 | 33 546 |
| Размер файла | ~1.43 МБ | ~1.94 МБ |
| Уникальных id уроков (`pN_lM`, `ps_lM`) | 73 | 83 |
| Уроков в рантайме (LESSONS) | ~115 | 131 |
| Уникальных симуляторов (`sim_*`) | 37 | 43 |
| Фаз в PHASES | 6 (0–5) | 7 (0–5 + П/8) |
| Вкладок в навигации | 9 плоских | 10 (9 в 3 группах + ...) |
| Ключей localStorage `cn_*` | базовый набор | + десятки новых (см. §7) |

**Архитектурный инвариант, сохранённый во всех итерациях:** однофайловая SPA, ванильный JS, без внешних рантайм-зависимостей (CDN-шрифты/three.js — опциональны и деградируют), весь прогресс в `localStorage` под префиксом `cn_`.

---

## 1. Итерация V1 — первый аудит (коммит `e629b24`)

**Контекст:** первый внешний аудит контента, методики и критических багов. Самый крупный дифф проекта: `+27760/−25173` (полная реорганизация файла).

### 1.1. Методика обучения (B1–B5)
| ID | Суть | Якоря в V5 |
|---|---|---|
| B1 | Трёхслойная структура блока-концепта: `level1` (простой язык) / `level2` (деталь) / `level3` (механика) / `level4` (практика/протокол) | массив `blocks[]`, поля `level1..level4` в каждом уроке |
| B2 | Блок типа `simpleAnalogy` — бытовая аналогия к каждому понятию | `block.simpleAnalogy` |
| B3 | Блок типа `numbers` — числовой разбор («посчитаем честно») | `block.type === 'numbers'` |
| B4 | Блок типа `warn` — красное предупреждение о ловушке | `block.type === 'warn'` |
| B5 | Блок типа `recap` — выжимка 3 пункта в конце урока | `block.type === 'recap'`, поле `points[]` |

### 1.2. Визуализации (C1–C9)
- C1–C3: градиентные карточки фаз, иконки эмодзи, прогресс-бар в шапке (`.hprog`, `#hbar`, `#hpct`).
- C4–C6: бейджи `.badge`, чипы `.chip`, карточки `.card`.
- C7–C9: тёмная тема CSS-переменных (`:root { --bg, --card, --acc1, --acc2, --ok, --warn, --bad, --mut, --line, --grad, --mono }`).

### 1.3. Критические багфиксы (первого аудита)
- Исправлены битые ссылки `openFullscreenLesson` на несуществующие id.
- Устранены дубли `id` в разметке симуляторов.
- Починен рендер квизов при переключении фаз.

---

## 2. Итерация V2 — внешний аудит D0/D1/D2 (коммиты `b8d183f`, `15fd766`, `32898d5`)

### 2.1. Поток D0 — «P0 внешнего аудита» (коммит `b8d183f`, +142/−70)
| Изменение | Технические якоря |
|---|---|
| **Перемешивание вариантов ответов** во всех квизах (ранее правильный ответ всегда в одной позиции) | функции `shuffledOptions()`, ГСЧ `mulberry32(seed)` @~31325, хэш `fnv1a()` — детерминированный, воспроизводимый шаффл |
| **К1–К9** — девять контрольных точек (проверочные квизы) по ходу Фазы 0 | `PHASE_TESTS`, `checkLessonQuiz()` |
| **Аттестации** по фазам с порогом | `PHASE_TESTS` @~11579, поле `threshold: 80`, `switchPhaseTest()`, `renderPhaseTestView()` |
| **Фаза 0** выделена и структурирована (базовая финансовая грамотность перед трейдингом) | `PHASES` @~12793, `p: 0` |

### 2.2. Поток D1 — правки контента (коммит `15fd766`, +37/−30)
- Правки формулировок Фаз 1–5 и Фазы 6 (вайб-кодинг).
- Апдейты «рынок-2026»: актуализация бенчмарков, упоминания ETF и институционализации.

### 2.3. Поток D2 — методика запоминания (коммит `32898d5`)
| Фича | Якоря |
|---|---|
| **Консолидация NSDR** (Non-Sleep Deep Rest): пост-уроковый экран «тишины» | `showConsolidationScreen()` @~27522, `renderConsolidationPhase1()`, оверлей `#consolidation_overlay`, таймер `#consolidation_timer`, дыхание 4-4-6 `#breath_circle` |
| **Интерливинг** (смешанная сессия вопросов из разных фаз) | `startInterleavingSession()` @~2211, чип `#quiz_mode_interleave`, `interleavingStats` @~27052, ключ `cn_interleaving_stats` |
| **Калибровка (Brier-прогнозы)** | блоки `brier`, функции оценки прогнозов |
| **Hover-хинты** на терминах | обработчики `oninput`/tooltip |
| **Метод Фейнмана** (объясни своими словами) | блок `feynman`, `completeLessonWithMastery()` @~13866, проверка `promptConfirmed` |

### 2.4. D4 — инфраструктура инфографики (коммит `e66e75a`)
- Каркас для SVG-иллюстраций + документы аудита (+127 строк).

---

## 3. Итерация V3 — аудит контента/методики (коммиты `480b23f` … `33f4b8d`)

### 3.1. Шаг 0 (коммит `480b23f`)
- Инвентарь v2: файл `V2_INVENTORY.md`.
- **Фикс коллизии id боссов** (два босса имели одинаковый id).

### 3.2. Поток A — когнитивная методика (`b851e23`…`08f744f`)
| ID | Суть | Якоря |
|---|---|---|
| A1 | **Числовые вопросы с открытым вводом** (Active Recall 2.0) | тип блока `numeric`, проверка по допуску |
| A2 | **Журнал когнитивных ошибок** (Mistake Tracker) | `cn_mistakes`, `renderMistakeTracker()` |
| A3 | **Микро-recall** в длинных уроках Фаз 1–3 («3 минуты и 1 клик») | блоки `recall` внутри `blocks[]` |
| A4 | **Фильтр слов-паразитов** в объяснении Фейнмана + апгрейд Костра (NSDR) | `feynman`-блок, список стоп-слов; `showConsolidationScreen` |
| A5 | **Навигация**: перемотка к блоку фазы из дорожной карты | `drawInteractiveSvgRoadmap()`, якорные ссылки |
| A6 | **Тренажёр**: 3 уровня мастерства + пре-квиз «до/после» (замер Δ-прироста) | блок `mastery_check`, `hasMasteryBlocks` @~13848 |

### 3.3. Поток B — симуляторы (`1323804`…`2f5898f`)
| ID | Суть | Якоря |
|---|---|---|
| B1 | **2 новых симулятора**: Funding Drag (Ф1) и Transaction Cost (Ф4) | `sim_funding_drag`, `sim_tcost` |
| B2 | **Интерактивные дашборды риск-метрик** в 4 уроках (p1_l10, p3_l1, p3_l4, p3_l6) | виджеты в этих уроках |
| B3 | **Контур капитала** — Account Size Filter (≤50К / 50К–500К / 500К+) | фильтр в симуляторах |
| B4 | **Симулятор DeFi Yield Lab** (APY vs IL vs HODL) в Фазе 2 | `sim_defi` |
| B5 | **Тюнинг дефолтов** симуляторов С4–С12 под реалистичные значения | начальные `value` в соотв. sim_* |

### 3.4. Поток C — прикладные навыки (`904cbef`, `fa556b3`)
| ID | Суть | Якоря |
|---|---|---|
| C1 | **Чек-лист перед сделкой** (5 пунктов) с гейтом на первую сделку | `sim_first_trade`, чек-лист из 5 чекбоксов |
| C2 | **Еженедельный квиз «Гипотезы из данных»** (8 вопросов, чтение мини-таблиц) | отдельный квиз-модуль |

### 3.5. Поток D — полировка (`33f4b8d`)
| ID | Суть | Якоря |
|---|---|---|
| D1/D2 | **3 SVG-иллюстрации** (hero / roadmap / progress) + **Open Graph / Twitter meta** | `<meta property="og:*">`, inline-SVG |

### 3.6. Поток E — геймификация (`4f59fb0`, `a5908a0`, `769f716`)
| ID | Суть | Якоря |
|---|---|---|
| E1/E2/E4 | **Хуки, боссы-дуэли, костёр NSDR** | `sim_boss`, босс-дуэли; костёр-консолидация |
| E3/E6 | **Brier-прогнозы, детектив, рулетка**; фикс персиста E4 | игровые симуляторы |
| E5/E7/E8 | **Лестница рангов, 10 новых бейджей, спидраны** | `cn_badges`, `#headerRankTitle`, ранги |

---

## 4. Итерация V4 — аудит контента/игр/тренажёров/UX (коммиты `186b4a0` … `16c241d`, ветка `v4-upgrade`)

### 4.1. Поток A — новые уроки (`58c4fa1`)
| ID | Суть | Якоря |
|---|---|---|
| A1 | **Урок «MEV-защита»** с симулятором сэндвич-атаки | урок + симулятор сэндвич-атаки |
| A2 | **Урок «LLM-агенты»** (цикл ReAct, галлюцинации) | урок в программе |

### 4.2. Поток B — мини-игры (`476c430`)
| ID | Суть | Якоря |
|---|---|---|
| B1 | **«Защити seed-фразу»** | игровой модуль (вкладка «Заработок»/earn) |
| B2 | **«Угадай режим рынка»** | игровой модуль |
| B3 | **«Спаси депозит»** | игровой модуль |

### 4.3. Поток C — продвинутые визуализации (`a889cb8`, `263589c`)
| ID | Суть | Якоря |
|---|---|---|
| C1/C2/C3 | **Генератор эквити-кривых**, **карта корреляций**, **конструктор дельта-нейтрала** | `sim_equity`, `sim_corr`, `sim_deltaneutral` |
| C4 | **Gauge «расстояние до ликвидации»** в sim_liq | доработка `sim_liq` |
| C5 | **Depth-chart** в стакане | доработка `sim_ob` |

### 4.4. Поток D — процесс обучения (`a5908a0`, `df3196a`, `16c241d`)
| ID | Суть | Якоря |
|---|---|---|
| D1 | **Пост-уроковая модалка консолидации — не чаще 1 раза за сессию** (анти-спам) | обёртка `completeLessonWithMastery` @~27686, флаг `window._consolShownThisSession`, `setTimeout(..., 400)` |
| D2 | **Помодоро-таймер 25 мин** (opt-in) | кнопка `#pomodoro_btn`, `window.togglePomodoro()` |
| D4 | **Автосохранение** каждые 30 сек | `setInterval(save, 30000)` |
| D3 | **«Сборочный пункт» SM-2** после каждых 5 уроков + **A3**: уточнение бенчмарка Fear&Greed (ETF/институционализация) | SRS `srsState`, `cn_consol_streak` |

### 4.5. Поток E — UX (`1d99bc7`, `e7c8551`, `df3196a`)
| ID | Суть | Якоря |
|---|---|---|
| E1 | **Группировка навигации**: 9 вкладок в 3 группы (Учёба / Практика / Справка) | `.nav-group`, `.nav-group-label` @~37 |
| E2 | **Строгий режим Фазы 0** (lock урока N+1 до завершения N, toggle, default OFF) | `isStrictPhase0()` @~27701, `window.toggleStrictPhase0`, ключ `cn_strict_phase0` |
| E3 | **SVG-инфографики** «жизненный цикл seed-фразы» и «защищённый стек» | inline-SVG в соотв. уроках |

---

## 5. Итерация V5 — блок «Психология оператора бота» (коммит `c6e0ad2`, ветка `v5-psych`, мерж `81a6737`)

**Методическое размещение:** фаза **П** (`p: 8`) вставлена **между Фазой 4 и Фазой 5** в массиве `PHASES` — после продакшна, до живых денег. Самодостаточный `<script>`-модуль перед `</body>` (по образцу электива по математике, phase 7).

### 5.1. Восемь уроков (П1–П8) — `PSY_LESSONS` / `PSY_LESSONS_2`
| ID | Урок | id | Якорь-тренажёр |
|---|---|---|---|
| П1 | Сломался или просто страшно? Когда можно трогать бота | `ps_l1` | `widget_ps_l1_night_alerts` «Тревога в 03:00» |
| П2 | Доверяй машине, но стой рядом: две крайние ошибки | `ps_l2` | `widget_ps_l2_trust_scale` «Качели доверия» |
| П3 | Головокружение от успехов | `ps_l3` | `widget_ps_l3_green_streak` «Зелёная полоса» |
| П4 | Ловушка «хоть бы вернуть своё»: якорь на нуле | `ps_l4` | `widget_ps_l4_anchor_trap` «Магия нуля» |
| П5 | Ночная смена: сон, телефон и рынок 24/7 | `ps_l5` | `widget_ps_l5_babymonitor` «Радионяня трейдера» |
| П6 | Качели как сахар: тяга к движению и чужие голы | `ps_l6` | `widget_ps_l6_dopamine_detector` «Детектор тяги» |
| П7 | Письмо из будущего: разбор катастрофы до её начала | `ps_l7` | `widget_ps_l7_future_letter` «Письмо из будущего» |
| П8 | Судьи считают технику, а не один гол | `ps_l8` | `widget_ps_l8_match_judge` «Судья недель» |

Каждый урок — полная схема: `lead`, 2 термина, `blocks[]` (concept L1–L4 + simpleAnalogy + warn/numbers), `interactive_psy`, `recap`, `checks[]`, `quiz{}`. Тексты — дословно из методического файла блока «Психология».

### 5.2. Восемь интерактивных тренажёров (ванильный JS)
- Реестр: `window.PSY_render_<widgetId>`, патч диспетчера `renderPhase0Widget` (массив `PSY_WIDGET_IDS`).
- ГСЧ с фиксированным зерном (`mulberry32`) — прохождение воспроизводимо.
- Прогресс: ключ **`cn_ps_progress`** → `{best, total, passed, ts}`, бейдж «✓ Пройдено».
- Письмо из будущего: ключ **`cn_future_letter`** → `{version, savedAt, targetDate, text}`.

### 5.3. Аттестация и навигация
- **Аттестация фазы 8**: `PHASE_TESTS.push({phase: 8, threshold: 80, questions: 8})` — те же 8 вопросов, что квизы уроков (единый источник истины).
- **Чипы навигации**: «🧠 Психология» в разделе уроков (после «Фаза 4: Исполнение») и в разделе тестов (после «Тест Фазы 5»).
- **Паспорт фазы П** в дорожной карте (вставка `PHASES.splice` между `p:4` и `p:5`).

### 5.4. Попутные фиксы интеграции (не механики, а пропуски ядра)
| Фикс | Якорь |
|---|---|
| **Мёртвые кнопки «Сдать аттестационный тест»** в дорожной карте: функции `startPhaseTest`/`startPhase0Test` не существовали → добавлены алиасы к `switchPhaseTest()` | `if(typeof window.startPhaseTest !== 'function'){...}` |
| **Горизонтальный скролл на 360px**: header и `.nav-group` не переносились | `header{...; flex-wrap:wrap}` @~28, `.nav-group{...; flex-wrap:wrap; max-width:100%}` @~37 |

---

## 6. Реестр новых симуляторов (Vanilla → V5)

Добавлены 6 симуляторов (37 → 43 уникальных `sim_*`):
1. `sim_funding_drag` — Funding Drag (V3-B1)
2. `sim_tcost` — Transaction Cost (V3-B1)
3. `sim_defi` — DeFi Yield Lab (V3-B4)
4. `sim_equity` — генератор эквити-кривых (V4-C1)
5. `sim_corr` — карта корреляций (V4-C2)
6. `sim_deltaneutral` — конструктор дельта-нейтрала (V4-C3)

Существующие доработаны: `sim_liq` (+gauge ликвидации), `sim_ob` (+depth-chart), дефолты С4–С12 (V3-B5).

---

## 7. Реестр ключей localStorage (добавленные)

> Инвариант: ни один существующий в Vanilla ключ не удалён и не переименован. Все ключи — префикс `cn_`.

| Ключ | Итерация | Назначение |
|---|---|---|
| `cn_interleaving_stats` | V2-D2 | статистика смешанных сессий |
| `cn_consol_streak` | V4-D3 | стрик дней консолидации |
| `cn_mistakes` | V3-A2 | журнал когнитивных ошибок |
| `cn_badges` | V3-E5 | бейджи/ранги |
| `cn_strict_phase0` | V4-E2 | строгий режим Фазы 0 |
| `cn_ps_progress` | V5 | прогресс 8 психотренажёров |
| `cn_future_letter` | V5 | «Письмо из будущего» |

---

## 8. Контрольные точки для проверяющего агента

Чтобы подтвердить реализацию, агент может выполнить в браузере (консоль) на V5:

```js
// Итоговые счётчики
LESSONS.length                          // 131
LESSONS.filter(l => l.phase === 8).length  // 8 (П1–П8)
TERMS.length                            // 205
PHASES.map(p => p.p)                    // [0,1,2,3,4,8,5]
PHASE_TESTS.find(t => t.phase === 8).questions.length  // 8

// Наличие модулей
typeof showConsolidationScreen          // 'function' (V2-D2)
typeof startInterleavingSession         // 'function' (V2-D2)
typeof completeLessonWithMastery        // 'function' (V2/V3)
typeof window.togglePomodoro            // 'function' (V4-D2)
typeof isStrictPhase0                   // 'function' (V4-E2)
typeof window.toggleStrictPhase0        // 'function'
typeof window.startPhaseTest            // 'function' (V5-фикс)
typeof window.PSY_render_widget_ps_l1_night_alerts  // 'function' (V5)

// Виджеты V5
document.querySelectorAll('[id^="widget_ps_l"]').length  // 8 (после открытия уроков)

// Ключи
Object.keys(localStorage).filter(k => k.startsWith('cn_'))
```

---

## 9. Хронология коммитов (git)

```
616e278  Initial commit
2ca976e  Crypto Navigator v2026-08-23  ← ВАНИЛЬ
a126982  Убраны локальные документы аудита
e629b24  V1: методика B1–B5, визуализации C1–C9, крит. багфиксы
b8d183f  V2-D0: перемешивание ответов, К1–К9, аттестации, Фаза 0
15fd766  V2-D1: правки Фаз 1–5, Ф6, рынок-2026
32898d5  V2-D2: консолидация NSDR, интерливинг, калибровка, Фейнман
e66e75a  V2-D4: инфраструктура инфографики
451b2dc  D3.6: апгрейды существующих симуляторов
44f44d2  D3.1: волна 1 игр (G-01…G-15)
af670a2  D3.2: волна 2 игр (G-02…G-13)
1cfb87e  D3.3: волна 3 игр бонус-фазы (G-17…G-23)
4f59fb0  V3-E1/E2/E4: хуки, боссы-дуэли, костёр NSDR
a5908a0  V3-E3/E6: Brier, детектив, рулетка; фикс персиста E4
769f716  V3-E5/E7/E8: лестница рангов, 10 бейджей, спидраны
480b23f  V3-Шаг 0: инвентарь v2 + фикс коллизии id боссов
b851e23  V3-A1: числовые вопросы (Active Recall 2.0)
2aab947  V3-A2: журнал когнитивных ошибок
2ae0537  V3-A3: микро-recall в длинных уроках
7830761  V3-A4: фильтр слов-паразитов Фейнмана + Костёр NSDR
bbc9fb2  V3-A5: навигация — перемотка к фазе из дорожной карты
08f744f  V3-A6: Тренажёр — 3 уровня мастерства + пре-квиз Δ
1323804  V3-B1: симуляторы Funding Drag + Transaction Cost
5dce70e  V3-B2: дашборды риск-метрик в 4 уроках
791a41b  V3-B3: контур капитала (Account Size Filter)
6767fbb  V3-B4: DeFi Yield Lab в Фазе 2
2f5898f  V3-B5: тюнинг дефолтов симуляторов С4–С12
904cbef  V3-C1: чек-лист перед сделкой (5 пунктов) + гейт
fa556b3  V3-C2: еженедельный квиз «Гипотезы из данных»
33f4b8d  V3-D1/D2: 3 SVG-иллюстрации + OG/Twitter meta
186b4a0  V4-D2/D4: помодоро 25 мин + автосохранение 30 сек
58c4fa1  V4-A1/A2: уроки MEV-защита и LLM-агенты
476c430  V4-B1/B2/B3: игры seed-фраза / режим рынка / спаси депозит
a889cb8  V4-C1/C2/C3: эквити-кривые / корреляции / дельта-нейтрал
263589c  V4-C4/C5: gauge ликвидации + depth-chart
df3196a  V4-D3: сборочный пункт SM-2 + V4-A3 бенчмарк F&G
e7c8551  V4-E2: строгий режим Фазы 0 (lock N+1)
1d99bc7  V4-E1: группировка навигации в 3 группы
df3196a  V4-E3: SVG «жизненный цикл seed-фразы» + «защищённый стек»
16c241d  V4-D1: консолидация — не чаще 1 раза за сессию
5a8fa63  MERGE v4-upgrade → main
c6e0ad2  V5: блок «Психология» (П1–П8 + 8 тренажёров + аттестация)
81a6737  MERGE v5-psych → main  ← ФИНАЛ V5
```
