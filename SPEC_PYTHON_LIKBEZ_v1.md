# Спецификация модуля «Python с нуля для трейдера» (Py-01…Py-14)

**Версия документа:** 1.0 · 3 сентября 2026
**Статус:** ТЗ на внедрение (готово к разработке)
**Связанные документы:** `AUDIT_v13.1_vs_v14.md` (§7 — гэп-анализ), ресёрч №1 (EdTech Blueprint), ресёрч №2 (deep-research UX, рекомендации 1–10, 11–18), ресёрч №3 (Gemini Benchmark, Pyodide/WASM, клавиатурная навигация)
**Целевая версия платформы:** v14.1 (этап 1, статика) → v15 (этап 2, Pyodide/WASM)

---

## 1. Проблема: почему модуль нужен (факты аудита)

Аудит v13.1 → v14 (§2, §7) зафиксировал структурный разрыв входа в алго-часть курса:

1. **Нулевой трек `p0` (20 уроков) не содержит программирования вообще** — там крипто-базис, свечи, стакан и вероятность. Фаза 0 завершается тестом, и ученик сразу попадает в Фазу 1, где в первом же уроке (`p1_l1`) встречает `import pandas` и лог-доходности.
2. **Ни один из 477 заголовков уроков курса не обучает основам Python.** Объяснений «что такое переменная», «типы данных», `if/else` как учебной темы — 0 вхождений по полным текстам всех банков уроков.
3. Python появляется только в «взрослом» контексте: `p1` (квант-трек, pandas/numpy), `p4_l1` (Jupyter → продакшн), FT-академия (`ft02` — установка через venv/pip/Docker с делегированием поломок ИИ-агенту), вайбкодинг (`vc_l1–l4` — «код пишет агент, ты ревьюишь»).
4. Единственный мост — виджет `interactive_ob_bridge` («Мостик: визуальный стакан → код») в `p1_l6` — рассчитан на ученика, который *уже* читает pandas-код.

**Вывод:** для ученика без базы программирования (целевой сегмент — «полный ноль», как сам заказчик) вход в Фазы 1–4 и FT-академию закрыт стеной синтаксиса. Одновременно в v14 **не реализована Python-песочница Pyodide/WASM** (рекомендация ресёрча №3) — то есть нет ни теории, ни практики кода.

**Решение:** компактный обязательный модуль из 14 уроков «Python с нуля для трейдера», вставленный между Фазой 0 и Фазой 1, спроектированный с первого дня под песочницу: этап 1 — статические интерактивные виджеты (0 КБ зависимостей, как весь курс), этап 2 — живой интерпретатор Pyodide в тех же виджетах.

---

## 2. Принципы модуля (нормативные)

### 2.1. Кому и зачем

- **Аудитория:** ученик, который никогда не писал код. Единственные предполагаемые навыки — те, что уже закрыты Фазой 0: чтение свечей, стакана, PnL, вероятность.
- **Цель модуля:** к уроку `Py-14` ученик своими руками собирает и прогоняет простейшую стратегию «пересечение MA» и понимает каждую строку из 8–10 строк кода. После этого Фаза 1 (`p1_l1` лог-доходности, pandas) читается как продолжение, а не как стена.
- **Чего модуль НЕ делает:** не готовит «python-разработчика». Не покрывает ООП, декораторы, async, виртуальные окружения (это осознанно вынесено в FT-академию и вайбкодинг).

### 2.2. Редакционный стиль для абсолютного нуля (обязательные правила)

Эти правила — жёсткие, они важнее любых сроков. Проверяются при приёмке каждого урока.

1. **Одно предложение — одна мысль.** Средняя длина предложения ≤ 14 слов. Максимум 20.
2. **Каждое новое понятие кода = бытовая аналогия из мира денег и трейдинга**, знакомая после Фазы 0:
   - переменная → «ячейка в таблице сделок с этикеткой»;
   - присваивание `=` → «положить в ячейку» (не «равно»!);
   - строка → «тикер в кавычках», текстовый ярлык;
   - список → «лента свечей, склеенных в ряд»;
   - индекс → «номер свечи, счёт с нуля — первая свеча нулевая»;
   - словарь → «паспорт свечи: ключ-поле и значение»;
   - цикл → «конвейер: для каждой свечи в истории — одно и то же действие»;
   - функция → «станок: загрузил цены — получил сигнал»;
   - библиотека → «готовый набор деталей, как открытый фреймворк Freqtrade из p4_l8»;
   - API → «официант между тобой и биржей»;
   - баг → «опечатка в ордере: биржа её не простит, а песочница — простит».
3. **Правило «зачем → как»:** каждый блок начинается с ответа «зачем это боту?», и только потом «как это пишется». Никакой синтаксис ради синтаксиса.
4. **Правило одного нового термина:** в одном блоке урока вводится максимум один новый термин кода. Если нужно два — это два блока.
5. **Код всегда с русскими комментариями над строкой.** Строк кода в одном блоке ≤ 3 (уроки Py-01…Py-09), ≤ 10 (Py-10…Py-14).
6. **Арифметика 5-го класса.** Проценты объясняются на пальцах («0.1% от 1000$ — это один доллар»).
7. **Сквозной пример на весь модуль:** один персонаж (Алексей), один депозит **1000 $**, один актив **BTC по 65 000 $**, комиссия **0.1%**, история из **одной недели свечей**. Все примеры всех уроков живут в этом мире; числа из урока в урок не меняются без причины.
8. **Снятие страха — обязательный слой:** в Py-01 и при первой ошибке в каждом виджете явно проговаривается: «В песочнице ошибаться бесплатно. Настоящий код не сломается, деньги не потеряются, всё сбрасывается кнопкой».
9. **Запрещённые слова:** «тривиально», «очевидно», «просто» (в смысле «это простая задача»), «как известно», не объяснённые аббревиатуры. Запрещено вводить термин и использовать его дальше без аналога хотя бы один раз.
10. **Тон** — как в существующем «человеческом слое» курса (`SIMPLE_LAYER`): на «ты», коротко, с лёгкой иронией, без сюсюканья. Пример калибровки: «Бэктест берёт твои правила ("покупай, когда X") и прокручивает их по истории» — вот такой плотности и тона.
11. **Каждый урок заканчивается recap-блоком** из 3–5 строк «что ты теперь умеешь» — конкретными глаголами («умеешь положить число в ячейку и вывести его на экран»).
12. **Квиз-дисциплина:** каждый урок содержит 3 проверочных вопроса (см. §4). Вопрос не проверяет запоминание формулировок — он проверяет умение *предсказать результат кода* («что выведет эта строка?»).

### 2.3. Анти-паттерны (что убивает новичка — фиксируем как запреты)

- ❌ Начинать с «типов данных» как отдельной теории — типы вводятся через деньги (число = цена), текст (тикер), флаг (купил/нет).
- ❌ Показывать код без кнопки «выполнить» рядом.
- ❌ Вводить `for` раньше списков, списки раньше переменных, `def` раньше цикла (строгая лесенка §3).
- ❌ Использовать слово «объект» до Py-08.
- ❌ Упоминать ООП, `__init__`, классы — никогда в этом модуле.
- ❌ Квизы-угадайки с 4 бессмысленными вариантами — каждый дистрактор это реальная ошибка новичка (см. §4.3).

---

## 3. Архитектура модуля

### 3.1. Место в программе

Модуль — **обязательный core-этап** между этапом A («Рынок и деньги», Фаза 0) и этапом B («Статистика и бэктест») в `CN_TRACKS.coreStages`:

```
[Этап A: Фаза 0, p0_l1…p0_l20]  →  [Этап A2: Py-01…Py-14 (НОВЫЙ)]  →  [Этап B: Фаза 1, p1_l1…]
```

Технически уроки хранятся отдельным банком `PY_LESSONS` с `phase: 9` (следующее свободное после 8 — психология) и нумерацией `Py1…Py14` — по образцу матфакультатива (`phase: 7`, num `М1…`) и психологии (`phase: 8`, num `П1…`). Включение в core-последовательность — через `CN_TRACKS` (см. §6.2), как уже сделано для FT-этапа E.

### 3.2. Три блока-лесенки

| Блок | Уроки | Что умеет ученик на выходе |
|---|---|---|
| **А. Слова языка** | Py-01…Py-07 | Читает и пишет простейший код: переменные, числа, строки, `if/else`, списки, цикл `for`, свою функцию |
| **Б. Данные рынка** | Py-08…Py-11 | Превращает свечи в словари и таблицы, понимает, откуда берутся цены (API/JSON), читает DataFrame |
| **В. Первый сигнал** | Py-12…Py-14 | Считает скользящую среднюю, строит правило входа, прогоняет бэктест в 8 строк, делает мини-проект |

### 3.3. Титульник (14 уроков)

| # | id | Заголовок | Суть | timeEst | Новые термины кода (ровно столько — и не больше) |
|---|---|---|---|---|---|
| 1 | `py_l1` | Что такое код и зачем он твоему боту | Программа = рецепт для исполнителя; первая команда `print` | 6 мин | код, `print()` |
| 2 | `py_l2` | Переменные: ячейки твоего портфеля | `balance = 1000`; число, строка, флаг | 7 мин | переменная, `=` |
| 3 | `py_l3` | Цена и тикер: числа и строки | Арифметика комиссий; f-строка «Купил 0.1 BTC по 65000$» | 7 мин | f-строка |
| 4 | `py_l4` | Когда покупать? Сравнения и если-иначе | `True/False`, `==`, `>`, `<`; `if цена < 60000` | 8 мин | `if/else`, `True/False` |
| 5 | `py_l5` | Лента свечей: списки | Список закрытий; индекс с нуля; «последние 3 свечи» | 8 мин | список, индекс, срез |
| 6 | `py_l6` | Конвейер по истории: цикл for | Для каждой свечи — одно действие; посчитать зелёные | 9 мин | `for`, `in` |
| 7 | `py_l7` | Свой первый станок: функции | `def ma(prices, n)`; вход → выход через `return` | 9 мин | `def`, `return` |
| 8 | `py_l8` | Паспорт свечи: словари | `{"open":…, "close":…}`; список словарей = история | 8 мин | словарь, ключ |
| 9 | `py_l9` | Откуда бот берёт цены: API и JSON | Официант-API; ответ биржи в JSON; что нельзя светить | 8 мин | API, JSON |
| 10 | `py_l10` | Чужие готовые детали: библиотеки | `import pandas`; таблица DataFrame = Excel с суперсилой | 9 мин | библиотека, `import`, DataFrame |
| 11 | `py_l11` | Мостик: стакан → таблица → код | Собственная версия `interactive_ob_bridge` из p1_l6 | 8 мин | — (закрепление) |
| 12 | `py_l12` | Первый индикатор: скользящая средняя | Сумма ÷ длина; почему MA глушит шум | 9 мин | индикатор, MA |
| 13 | `py_l13` | Правило входа: пересечение MA | Быстрая/медленная MA; сигнал; бэктест в 8 строк | 10 мин | сигнал, бэктест (как код) |
| 14 | `py_l14` | Мини-проект: собери и проверь стратегию | Ползунки параметров → прогон → отчёт PnL/просадка | 12 мин | — (синтез) |

Итого: 106 мин чистого времени, 14 уроков, 12 новых терминов кода (≤ 1 за урок в среднем).

### 3.4. Скелет каждого урока (7–9 шагов плеера)

Каждый урок собирается из существующих типов блоков плеера — новых типов контента не требуется, кроме одного нового интерактива `interactive_code_lab` (§4.1) и квиз-блока `interactive_quiz_cbm` (§4.2). Детальные спеки всех 14 уроков — §5:

```
1. case          — реальный кейс (кнопка «Реальный кейс» в шапке урока)
2. lead          — хук: проблема трейдера, которую урок закрывает (1-2 предложения)
3. concept       — «зачем»: проблема + аналогия (level1 — простым языком)
4. analogy       — бытовая аналогия (отдельный блок)
5. concept       — «как»: код в 1–3 строки с русскими комментариями (level1/level2)
6. interactive_code_lab — виджет: нажми → поменяй → посмотри (§4.1)
7. warn          — типичная ошибка новичка с этим синтаксисом
8. interactive_quiz_cbm — 3 вопроса с уверенностью (§4.2)
9. recap         — «что ты теперь умеешь» (3–5 глаголов)
```

`cognitiveLoad: "easy"` у Py-01…Py-09, `"medium"` у Py-10…Py-14. Поле `prerequisites` строится цепочкой: `py_l1: []`, далее каждый урок требует предыдущий; `py_l12` требует `py_l7`; `py_l14` требует `py_l11` и `py_l13`.

---

## 4. Интерактив и проверка: механика виджетов

### 4.1. Универсальный виджет `interactive_code_lab` — сердце модуля

Один новый тип блока покрывает все 14 уроков (параметризованный), по канону Brilliant из ресёрчей: **теория в один экран → сразу «нажми и поменяй»**.

**Этап 1 (v14.1, статика, 0 КБ внешних зависимостей):**
- Левая панель: код с подсветкой (read-only текст или частично редактируемый — см. режимы).
- Правая панель: «консоль бота» — область вывода.
- Кнопка **«Выполнить ▶»**: эмулирует вывод заранее описанной JS-функцией `runtime(code_state)` — детерминированной от состояния полей.
- Режимы взаимодействия (задаётся в конфиге виджета):
  1. `run` — только запуск (Py-01: первая команда).
  2. `edit-value` — редактируемые значения-литералы в инпутах, код фиксирован (Py-02…Py-05): поменяй баланс/цену/порог — посмотри вывод.
  3. `fill-blank` — пропуски-пустышки в коде, выбор из чипов (Py-04, Py-06, Py-07): собери условие из деталей.
  4. `edit-code` — настоящий редактируемый код в `<textarea>` со шпаргалкой (Py-12…Py-14).
- **Три уровня подсказки** (лесенка из ресёрча №2, п.19–27): подсказка 1 — наводящий вопрос; подсказка 2 — на какую строку смотреть; подсказка 3 — почти ответ. Кнопка «Показать решение» — после 3 подсказок, с пометкой в телеметрии.
- **Защита от страха:** кнопка «Сбросить» всегда видна; при любой «ошибке» (выход за диапазон, пустой ввод) — дружелюбное сообщение в консоли («Бот не понял число "abc". Песочница цела, попробуй ещё раз») — никогда красный «ERROR».
- Хоткей `Enter` = «Выполнить», `R` = «Сброс» (клавиатурная доступность — ресёрч №3).

**Этап 2 (v15, Pyodide/WASM):** тот же блок получает флаг `engine: 'pyodide'` и выполняет код настоящим CPython. Пользователь не замечает переключения (см. §7).

### 4.2. Квиз с уверенностью `interactive_quiz_cbm`

Переиспользуем математику CBM Гарднера-Медвина, уже реализованную в v14 (LEVELS +1/0, +2/−2, +3/−6):

- Формат данных — существующий `{q, opts, a, explain}` + обязательное поле `why_wrong` для каждого дистрактора (текст, показываемый после ответа: «Ты выбрал X — это ошибка Y, вот почему»).
- Ученик сначала выбирает ответ, затем отмечает **насколько уверен** (3 уровня). Высокая уверенность + правильный ответ = больше очков; высокая уверенность + ошибка = минус. Это анти-угадывание из ресёрчей №1 и №3, и уже работающий механизм платформы.
- В модуле: **3 квиза в каждом уроке** (после ключевых блоков) + аттестационный тест модуля `PY_TEST` (§4.4).

### 4.3. Конструктор дистракторов (обязательно)

Каждый неверный вариант = реальная ошибка новичка, а не мусор:

| Тип дистрактора | Пример (вопрос про `balance = balance - 100`) | why_wrong |
|---|---|---|
| Смысловая инверсия | «Баланс станет 900 и 100 исчезнут навсегда из кода» | Путаница «переменной» и «денег на бирже»: в ячейке изменилось число, никаких денег не тронуто |
| Порядок операций | «Сначала прочитается правая часть, потом запишется в ячейку» — правильный; дистрактор: «сначала запишется, потом посчитается» | Модель исполнения |
| Литеральная ошибка | `balance == 900` вместо `=` | «== — это вопрос, = — это действие» |
| Магия | «Python сам поймёт, что тут минус комиссия» | Бот делает ровно то, что написано, ни строкой больше |

### 4.4. Аттестация модуля `PY_TEST`

- Банк: 15 вопросов (по 1–2 на урок), формат `QUIZ_CASES`, порог **80%**, CBM-разметка, неограниченные попытки (порог мастерства 90+% — рекомендация ресёрча №2, п.11–13; для входного модуля допускаем 80%).
- Доступ к Фазе 1 формально открывается аттестацией этапа A2 (`gate: 'py_test'`), по аналогии с `gate: 'p0'`.
- Примеры вопросов (полный банк — в приложении к этому ТЗ при внедрении):
  1. «Бот выполнил: `price = 65000; price = price + 500`. Что лежит в ячейке `price`?» → 65500 (дистракторы: 65000 / «две ячейки» / ошибка).
  2. «Что выведет `print(f"Купил 0.1 BTC по {price}$")` при `price = 65500`?» → «Купил 0.1 BTC по 65500$» (дистрактор с фигурными скобками в выводе, дистрактор с пустым местом).
  3. «`closes = [65000, 65100, 64900]`. Чему равно `closes[0]`?» → 65000 (дистрактор 64900 «последняя», дистрактор 65100 «вторая», дистрактор «ошибка, нумерация с единицы»).
  4. «Почему бот делает ровно то, что написано в коде, а не то, что ты имел в виду?» — концептуальный вопрос-якорь.
  5. Кейс-вопрос Py-13: по массиву из 5 свечей и заданным MA предсказать сигнал (числа подобраны так, что быстрая MA пересекает медленную на 4-м баре).
- **Телеметрия:** фиксируются попытки, время, уверенность — данные пойдут в будущую Brier-калибровку (задел v14 CBM уже собирает сырые события).

### 4.5. Психологический слой проверки

- После провала квиза — никогда «Неверно!», всегда «Пока нет. Смотри: строка 2 читает старое значение ячейки…». Формулировка ошибки = микроконтент.
- Прогресс модуля виден как отдельная полоса «Python: 8 из 14» в трекере (существующая механика tiers).
- Стрик-механика модуля не агрессивна: стрик = «дни с кодом», перегорание исключено (рекомендация ресёрча №2, п.28–37 — используемое подмножество: XP за выполненные лабы, без лидербордов на старте).

---

## 5. Детальные спеки уроков

> Для каждого урока: хук, кейс, шаги, конфиг виджета, образец квиз-вопроса, термины в глоссарий. Комментарии в коде — обязательная часть контента. Числа — из сквозного примера (депозит 1000 $, BTC 65 000 $, комиссия 0.1%).

### Py-01. Что такое код и зачем он твоему боту

- **Хук (lead):** «Твой бот не спит, не боится и не жадничает. Но он делает ровно то, что ему написали. Научимся писать так, чтобы он понимал.»
- **Кейс:** «Флеш-краш 12 октября 2022: боты не "решили" — они исполнили написанные ранее правила за миллисекунды. Кто написал правила — тот и управлял.»
- **Шаги:** concept (программа = рецепт: «если цена упала ниже X — купить» — это уже код, только на русском) → analogy (рецепт блинов: пропустил шаг — блины не получились; бот читает рецепт буквально) → concept + `print` (первая команда: бот умеет говорить) → **code_lab (режим `run`)** → warn («бот не угадывает: написал print без скобок — он не станет откровеннее») → квиз → recap.
- **Код урока (1 строка):**
  ```python
  # сказать в консоль бота
  print("Привет, рынок! Мой депозит: 1000$")
  ```
- **Виджет `code_lab`:** режим `run`; в консоли появляется строка + курсор «Выполнили 1 команду за 0.001 сек». Эксперименты: кнопки-чипы «поменяй фразу» (3 варианта) — вывод меняется.
- **Образец квиза:** «Что сделает бот, получив рецепт print("Привет")?» → выведет «Привет» (дистракторы: «поздравит тебя голосом», «ничего, это комментарий», «купит BTC»).
- **Термины в глоссарий:** Код, Консоль бота (терминал).
- **КГ:** узел Py-01; ребро ← p0_l13 (вероятность уже пройдена — «считать умеешь, теперь учим бота»).

### Py-02. Переменные: ячейки твоего портфеля

- **Хук:** «Бот должен помнить твой баланс, цену и сколько ты купил. Где он это хранит?»
- **Кейс:** «Ошибка 2017 года одного обменника: программист перепутал ячейки — и клиенты увидели чужие балансы. Ячейка с неправильной этикеткой хуже пустой.»
- **Шаги:** concept (переменная = ячейка в таблице сделок: этикетка + содержимое) → concept + код → **code_lab (`edit-value`)** → warn (`=` это «положить», не «равно» — сравнение дальше) → квиз → recap.
- **Код урока:**
  ```python
  # положить в ячейку balance число 1000
  balance = 1000
  # ячейка с текстом — тикер в кавычках
  ticker = "BTCUSDT"
  # флаг: купил или нет
  bought = False
  print(balance, ticker, bought)
  ```
- **Виджет:** режим `edit-value`: инпуты для balance / ticker / bought (переключатель True/False); консоль показывает вывод. Число vs текст: попытка написать в balance слово «тысяча» → дружелюбное объяснение «ячейка с числом не понимает текст» (мягкое знакомство с типами).
- **Образец квиза:** «Что лежит в ячейке после `balance = 1000` и потом `balance = 990`?» → 990 (дистракторы: 1000, «две ячейки», ошибка).
- **Термины:** Переменная, Присваивание.
- **КГ:** ребро ← Py-01.

### Py-03. Цена и тикер: числа и строки

- **Хук:** «65000 и "65000" — для тебя похоже, для бота — разные вещи. Один торгует, второй ломает.»
- **Кейс:** «Скрипт 2021 года купил BTC на сумму, где запятая стоит не там: вместо 100 $ — 100000 $. Число и текст перепутаны — денег нет.»
- **Шаги:** concept (числа считаем, строки показываем) → concept + код (арифметика комиссии) → **code_lab (`edit-value`: калькулятор сделки)** → concept f-строка → **code_lab (второй запуск: собери фразу сделки)** → квиз → recap.
- **Код урока:**
  ```python
  price = 65000        # цена BTC
  amount = 0.1         # сколько BTC покупаем
  fee_rate = 0.001     # комиссия 0.1%
  fee = price * amount * fee_rate   # сколько отдаём бирже
  print("Комиссия:", fee)           # Комиссия: 6.5
  # f-строка: подставь значения в текст
  print(f"Купил {amount} BTC по {price}$")
  ```
- **Виджет:** калькулятор сделки: ползунки price (60 000–70 000), amount (0.01–0.5); код фиксирован, консоль пересчитывает комиссию и фразу. Ошибочный ввод (цена буквами) — дружелюбный подсказ в консоли.
- **Образец квиза:** «Что выведет `print(f"Цена {price}")` при price = 65000?» → «Цена 65000» (дистрактор: «Цена {price}» — забыл f; дистрактор: «Цена price»).
- **Термины:** Строка, f-строка, Комиссия (уже есть — связь).
- **КГ:** ребро ← Py-02; ребро → p0_l4 (спред и цена — «комиссия тоже цена сделки»).

### Py-04. Когда покупать? Сравнения и если-иначе

- **Хук:** «Весь твой будущий бот — это одна фраза: "ЕСЛИ цена упала — ТО купи, ИНАЧЕ жди". Сейчас соберёшь её сам.»
- **Кейс:** «Твой чек-лист из Фазы 0 (p0_l19) — это уже if/else, просто написанный словами: "ЕСЛИ команда отвечает на 5 вопросов — ТО проект, ИНАЧЕ шиткоин". Сейчас переведёшь такой чек-лист на язык бота.»
- **Шаги:** concept (вопросы с ответом да/нет: цена НИЖЕ 60000? → True/False) → concept + код → **code_lab (`fill-blank`: собери условие)** → **code_lab (`edit-value`: ползунок цены, бот решает)** → warn (`==` vs `=`) → квиз → recap.
- **Код урока:**
  ```python
  price = 59800
  # сравнение: это вопрос с ответом True/False
  if price < 60000:
      print("Покупаю!")     # отступ = «внутри если»
  else:
      print("Жду.")
  ```
- **Виджет №1 (fill-blank):** собрать `if price < 60000:` из чипов (`if`, `price`, `<`, `60000`); дистрактор-чип `=` — при выборе мягкое объяснение.
- **Виджет №2 (edit-value):** ползунок цены 59 000–61 000; консоль показывает решение бота и строку сравнения с результатом.
- **Образец квиза:** «price = 60100. Что сделает бот из урока?» → выведет «Жду.» (дистракторы: «Покупаю!», «сломается», «купит на 60000»).
- **Термины:** Условие (if/else), Логическое значение (True/False), Отступ.
- **КГ:** ребро ← Py-03; ребро → p0_l9 (типы заявок: «решение уже есть — осталось научиться отправлять»).

### Py-05. Лента свечей: списки

- **Хук:** «Цена — это одна ячейка. А история цен? Бот хранит её как ленту свечей.»
- **Кейс:** «Тренажёр чтения графика (p0_l6) показывал свечи по одной. В коде бот держит их все сразу — в списке.»
- **Шаги:** concept (список = лента закрытий, склеенных в ряд) → concept + код (индекс с нуля!) → **code_lab (`edit-value`: лента свечей, кнопки индексов)** → concept срез (последние 3 свечи) → квиз → recap.
- **Код урока:**
  ```python
  closes = [65100, 65050, 64980, 65020, 65120]  # 5 свечей
  print(closes[0])      # первая свеча! нумерация с нуля
  print(closes[-1])     # последняя свеча
  print(closes[-3:])    # последние 3 свечи
  ```
- **Виджет:** лента из 5 свечей (визуально, как p0_l7); под каждой — индекс 0…4 и -5…-1 (переключатель «счёт с начала / с конца»); клик по свече → в консоли `closes[i]` и число. Ползунок среза `[-N:]`.
- **Образец квиза:** «closes = [65000, 65100, 64900]. Чему равно closes[1]?» → 65100 (дистрактор 65000 «первая», дистрактор 64900 «последняя», дистрактор «ошибка»).
- **Термины:** Список, Индекс, Срез.
- **КГ:** ребро ← Py-04; ребро → p0_l7 (OHLC: «свеча, которую ты уже умеешь читать»).

### Py-06. Конвейер по истории: цикл for

- **Хук:** «500 свечей. Проверить каждую вручную? Бот сделает это за миллисекунды — цикл это и есть конвейер.»
- **Кейс:** «Скальперский бот 2024 года проверял 10 000 свечей каждое утро за 0.3 секунды. Цикл — единственное, что он делает по-настоящему.»
- **Шаги:** concept (конвейер: для каждой свечи — одно и то же действие) → concept + код → **code_lab (трассировка: пошаговый прогон цикла)** → warn (отступы = что внутри конвейера) → квиз → recap.
- **Код урока:**
  ```python
  closes = [65100, 65050, 64980, 65020, 65120]
  greens = 0
  # для каждой цены в ленте закрытий
  for price in closes:
      if price > 65000:   # сравниваем с уровнем
          greens = greens + 1
  print("Зелёных свечей:", greens)   # 3
  ```
- **Виджет (трассировка):** кнопка «Шаг →» прогоняет по одной свече: подсветка текущей свечи, текущей строки кода, ячейки greens (живой счётчик). Это ключевая тренировка «модели исполнения» — ученик видит, как бот читает строку 2 раза… 5 раз.
- **Образец квиза:** «Сколько раз выполнится строка print внутри `for price in closes` при 5 свечах?» → 5 (дистрактор 1, дистрактор 0, дистрактор «зависит от цен»).
- **Термины:** Цикл (for), Счётчик.
- **КГ:** ребро ← Py-05.

### Py-07. Свой первый станок: функции

- **Хук:** «Ты написал правило "зелёных свечей". А завтра нужно другое? Пакуй правило в станок — и включай когда хочешь.»
- **Кейс:** «Стратегия в Freqtrade (это твой будущий бот из Фазы 4) — это буквально функция: подаёшь данные свечей, получаешь сигнал buy/sell. Сейчас напишешь её мини-версию.»
- **Шаги:** concept (def = «собираем станок», return = «выдаёт на выходе») → concept + код → **code_lab (fill-blank: собери def)** → **code_lab (edit-value: вызов с разными параметрами)** → квиз → recap.
- **Код урока:**
  ```python
  # станок: на вход цены, на выход средняя
  def average(prices):
      return sum(prices) / len(prices)

  result = average([65000, 65100, 64900])
  print(result)   # 65000.0
  ```
- **Виджет №1:** собрать `return sum(prices) / len(prices)` из чипов. №2: вызвать average на 3-х наборах ползунком длины окна (2–5 свечей); консоль показывает вход → выход (станок визуально: бункер «вход», бункер «выход»).
- **Образец квиза:** «Что выведет `print(average([10, 20]))`?» → 15.0 (дистракторы: 30, 10, «ошибка»).
- **Термины:** Функция, Аргумент, Возврат (return).
- **КГ:** ребро ← Py-06; ребро → p4_l8 («готовые фреймворки: твои функции когда-нибудь переедут туда»).

**Конец блока А — чекпоинт.** Мини-аттестация блока А: 5 вопросов (по 1 из каждого урока Py-03…Py-07), порог 4/5. Открывает блок Б.

### Py-08. Паспорт свечи: словари

- **Хук:** «Одна свеча — это 5 чисел. Список их перепутает. Свече нужен паспорт.»
- **Кейс:** «Тренажёр "Прочитай свечу" (p0_l8) спрашивал: где High, где Open? Словарь — это и есть паспорт, где каждое число подписано.»
- **Шаги:** concept (словарь = паспорт: ключ-поле → значение) → concept + код → **code_lab (edit-value: разбор свечи по ключам)** → concept (список словарей = история, мостик к pandas) → квиз → recap.
- **Код урока:**
  ```python
  # паспорт одной свечи
  candle = {
      "open": 65000, "high": 65300,
      "low": 64800, "close": 65100,
      "volume": 120
  }
  print(candle["close"])          # 65100
  print(candle["high"] - candle["low"])  # размах: 500
  ```
- **Виджет:** свеча из p0_l7 справа, паспорт слева; клик по свечному элементу (тело/тень/объём) подсвечивает ключ словаря; ползунок «размах» считает high − low.
- **Образец квиза:** «Чему равно `candle["close"]` из паспорта урока?» → 65100 (дистракторы: 65300, «ошибка: close это текст», 64800).
- **Термины:** Словарь, Ключ.
- **КГ:** ребро ← Py-07; ребро → p0_l7.

### Py-09. Откуда бот берёт цены: API и JSON

- **Хук:** «Бот не смотрит на график. Он спрашивает биржу. Как именно — сейчас узнаешь.»
- **Кейс:** «Фишинг из p0_l20: мошенник просит "ключ биржи для диагностики бота". Ключ API — это не пароль, это права: торговать, выводить. Один ключ в чужих руках = пустой депозит.»
- **Шаги:** concept (API = официант: ты просишь меню — биржа приносит цены) → concept JSON (паспорт свечи из Py-08 — уже почти JSON!) → **code_lab (edit-value: имитатор запроса/ответа)** → warn + интерактив «что можно/нельзя» → квиз → recap.
- **Код урока:**
  ```python
  # бот просит у биржи цену (упрощённо)
  response = api.get_price("BTCUSDT")
  print(response["price"])   # 65000.0 — ответ пришёл как словарь!
  ```
- **Виджет №1:** имитатор: кнопка «Запросить цену» → «официант» идёт на биржу → приходит ответ-JSON (подсветка структуры: тот же словарь). Поле «тикер» редактируемое.
- **Виджет №2 (повтор паттерна `widget_p0_l2_share` из p0_l2):** карточки «можно сообщать / нельзя никогда» — ключ с правом вывода, seed-фраза, публичный ключ, тикер.
- **Образец квиза:** «Что НЕЛЬЗЯ никогда давать боту/агенту?» → ключ API с правом вывода средств (дистракторы: ключ только-чтение, публичный адрес, тикер BTCUSDT).
- **Термины:** API, JSON, API-ключ.
- **КГ:** ребро ← Py-08; ребро → p4_l3 (безопасность API — «подробности ждут в Фазе 4»); ребро → p0_l20.

### Py-10. Чужие готовые детали: библиотеки

- **Хук:** «Ты умеешь считать среднюю. А свечи скачать, графики нарисовать, тесты прогнать? Библиотеки — готовые детали от тысяч инженеров.»
- **Кейс:** «p4_l8: "Не пиши бота с нуля". Тот же принцип раньше: pandas — это двигатели, собранные другими. Твоя работа — руль, а не поршни.»
- **Шаги:** concept (import = «достань деталь со склада») → concept DataFrame (таблица = Excel с суперсилой) → **code_lab (edit-value: таблица DataFrame)** → warn (библиотека должна быть установлена; в песочнице всё уже есть) → квиз → recap.
- **Код урока:**
  ```python
  import pandas as pd   # достали со склада таблицы

  df = pd.DataFrame(candles)  # список паспортов → таблица
  print(df["close"].head())   # колонка close, первые 5 строк
  ```
- **Виджет:** таблица из 8 строк × 5 колонок (OHLCV); интерактив: клик по колонке → подсвечивается `df["close"]`; переключатель head()/tail(); ползунок df[«close»].rolling(3).mean() — подготовка Py-12.
- **Образец квиза:** «Что такое df["close"] в таблице свечей?» → колонка закрытий (дистракторы: одна свеча, файл, команда покупки).
- **Термины:** Библиотека, Импорт, DataFrame, pandas.
- **КГ:** ребро ← Py-09; ребро → p1_l1 (первый урок Фазы 1: «там уже настоящие pandas»).

### Py-11. Мостик: стакан → таблица → код

- **Хук:** «Всё, что ты видел глазами в Фазе 0, теперь выглядит как код. Проверим самих себя.»
- **Кейс:** (ссылка на кейс p1_l6 — векторный бэктест; здесь облегчённая версия для новичка)
- **Шаги:** **code_lab (мост-тренажёр, 3 уровня)** → квиз → recap. Урок-закрепление, нового синтаксиса НЕТ (правило §2.2.4).
- **Виджет (3 уровня моста):** 1) стакан/свечи глазами → выбрать совпадающую строку DataFrame; 2) строка DataFrame → выбрать её словарь; 3) мини-фрагмент pandas → выбрать что он сделает. Подсветка соответствий — как в `interactive_ob_bridge` p1_l6.
- **Образец квиза:** «Свеча 65100/65300/64800/65100 — какая строка в df?» → {open: 65100, high: 65300, low: 64800, close: 65100} (дистракторы с перепутанными ключами).
- **Термины:** нет новых.
- **КГ:** ребро ← Py-10; ребро → p1_l6 (двунаправленное: «в Фазе 1 увидишь этот мостик в бою»).

### Py-12. Первый индикатор: скользящая средняя

- **Хук:** «Цена дёргается. Твоя средняя — как рука с честным взглядом: сглаживает шум, показывает тренд.»
- **Кейс:** «Индикатором №1 в мире пользуются боты всех бирж. Теперь он твой: 2 строки.»
- **Шаги:** concept (средняя за N свечей = окно, ползущее по истории) → concept + код → **code_lab (edit-value: ползунок окна N поверх свечей)** → warn (у окна есть "хвост": первые N-1 свечей — пусто; это и есть look-ahead-родственник из p1_l7, простыми словами) → квиз → recap.
- **Код урока:**
  ```python
  def ma(closes, window):
      # средняя последних window свечей
      tail = closes[-window:]
      return sum(tail) / len(tail)

  print(ma([65100, 65050, 64980], 3))  # 65043.3
  ```
- **Виджет:** свечи + линия MA; ползунок window 2–8; счётчик «посчитано на свечах №X…№Y».
- **Образец квиза:** «ma([10, 20, 30], 2) = ?» → 25.0 (дистракторы: 20, 30, 60).
- **Термины:** Индикатор, Скользящая средняя (MA).
- **КГ:** ребро ← Py-11 (через Py-07), ребро → p0_l14 (среднее из Фазы 0).

### Py-13. Правило входа: пересечение MA

- **Хук:** «Две средние: быстрая — нервы, медленная — терпение. Быстрая пересекла медленную вверх — рынок проснулся.»
- **Кейс:** «Золотой крест 2023: новостные боты купили BTC в момент пересечения MA50/MA200. Ты соберёшь ту же логику в мини-масштабе.»
- **Шаги:** concept (сигнал = момент, когда fast пересекает slow вверх) → concept + код бэктеста → **code_lab (пошаговый бэктест: кнопка «Бар →»)** → квиз → recap.
- **Код урока (весь бэктест — 8 строк, на экране целиком):**
  ```python
  fast = [ma(closes, 2)] ...   # быстрая MA на каждом баре
  slow = [ma(closes, 5)] ...   # медленная MA
  # для каждого бара: если быстрая выше и вчера была ниже
  for i in range(1, len(closes)):
      if fast[i] > slow[i] and fast[i-1] <= slow[i-1]:
          print(f"Бар {i}: сигнал BUY")
  ```
  *(в уроке массивы fast/slow строятся циклом — показывается полностью; здесь сокращено для ТЗ)*
- **Виджет:** свечи + 2 линии MA; кнопка «Бар →» двигает вертикальный курсор: подсвечивает пересечение, в консоли печатается сигнал; счётчик сделок.
- **Образец квиза:** «fast выше slow, вчера было ниже. Сигнал?» → BUY (дистракторы: SELL, «ничего», «стоп-лосс»).
- **Термины:** Сигнал, Золотой крест (ссылка на глоссарий, если термин есть/добавить).
- **КГ:** ребро ← Py-12; ребро → p1_l6 (векторный бэктест — «взрослый брат твоего бэктеста»); ребро → p1_l7 (look-ahead bias).

### Py-14. Мини-проект: собери и проверь стратегию

- **Хук:** «Три недели назад ты не знал, что такое переменная. Сейчас — соберёшь стратегию и прогонишь её по истории. Сам.»
- **Кейс:** «Так начинается настоящая алго-работа: гипотеза → код → проверка на истории → только потом деньги (p2_l6, "Гипотеза → Данные → Модель").»
- **Шаги (квест):**
  1. **code_lab (edit-code):** полный код стратегии (15 строк с комментариями), 2 ползунка: окно fast (2–4), окно slow (5–10).
  2. Прогон по истории из 60 свечей: отчёт — PnL %, число сделок, макс. просадка, кривая капитала.
  3. **Задание с открытым ответом:** «Найди параметры с PnL > 0 и просадкой < 10%. Запиши, что нашёл» (личная заметка, сохраняется).
  4. **Открытие ловушки:** предзаготовленный сценарий показывает переобучение: «лучшие параметры прошлой недели сливают следующую» (мягкая подготовка к p1_l12 — робастность).
  5. Аттестация `PY_TEST` (15 вопросов, §4.4).
  6. Куда дальше: карточки-развилки — «Фаза 1: настоящая статистика», «FT-академия: бот без написания кода», «Вайбкодинг: агент пишет — ты ревьюишь».
- **Виджет:** лаборатория стратегий (объединяет ползунки Py-13 + отчёт): кривая капитала, таблица сделок, кнопки «Прогнать ещё раз»/«Сброс».
- **Критерий прохождения:** PY_TEST ≥ 80% + лаборатория запущена ≥ 3 раза с разными параметрами.
- **КГ:** ребро ← Py-13; рёбра → p1_l1 (Фаза 1), → ft02 (установка Freqtrade), → vc_l1 (вайбкодинг).

**Конец модуля.** Выдаётся бейдж «Первый алгоритм» (существующая система бейджей) + запись в дневнике прогресса.

---

## 6. Интеграция в код платформы (v14.1, чек-лист для агента)

### 6.1. Новый банк данных `PY_LESSONS`

Расположение — рядом с `PSY_LESSONS`/`MATH_LESSONS_SOURCE`. Схема записи — как у психологии:

```js
const PY_LESSONS = [
  {
    "id": "py_l1",
    "phase": 9,                    // следующая свободная после 8 (psy)
    "num": "Py1",
    "title": "Что такое код и зачем он твоему боту",
    "cognitiveLoad": "easy",
    "timeEst": "6 мин",
    "prerequisites": [],
    "case": { "title": "Реальный кейс: …", "text": "…" },
    "lead": "Твой бот не спит, не боится и не жадничает…",
    "termsCount": 2,
    "terms": [ { "ru": "Код", "en": "code", "desc": "…" } ],
    "blocks": [ /* §5: concept/analogy/warn/recap/interactive_code_lab/interactive_quiz_cbm */ ]
  },
  // … py_l2 … py_l14
];
```

Подключение в общий рантайм — там же, где матфакультатив и психология мержатся в `LESSONS`:

```js
LESSONS.push.apply(LESSONS, PY_LESSONS);
```

**Важно:** блок `interactive_code_lab` реализуется как новый рендер-тип в плеере (единый компонент на все 14 уроков, конфиг в теле блока: `mode: run|edit-value|fill-blank|edit-code`, `code`, `inputs[]`, `runtime: 'js-имитация'`, `hints[3]`, `solution`). Блок `interactive_quiz_cbm` — обёртка над существующей CBM-механикой v14 (LEVELS +1/0, +2/−2, +3/−6) с данными `{q, opts, a, explain, why_wrong[]}`.

### 6.2. Трек и последовательность: `CN_TRACKS`

В `CN_TRACKS.coreStages` вставить новый этап **между A и B** (не перенумеровывая существующие id):

```js
{ id: 'A2', title: 'Python с нуля для трейдера',
  phaseLabel: 'Py-01–Py-14',
  gate: 'py_test',
  lessons: [
    'py_l1', 'py_l2', 'py_l3', 'py_l4', 'py_l5', 'py_l6', 'py_l7',
    'py_l8', 'py_l9', 'py_l10', 'py_l11', 'py_l12', 'py_l13', 'py_l14',
  ] },
```

и добавить те же 14 id в `CN_TRACKS.coreSet` (чтобы бейдж был 🟢 core, а не 🟠 elective). Поднять `CN_TRACKS.version: 13 → 14` — существующая миграция профиля ученика подхватит новый этап без потери прогресса (проверить на тест-профиле: прогресс Фазы 0 сохраняется, этап A2 отображается «не начат»).

### 6.3. Smoke-тесты и инварианты

Обновить встроенную самодиагностику (`V10.smoke`) и внешние проверки (`tools/verify_app_browser.mjs`):

| Тест | Было | Станет |
|---|---|---|
| `lp3:program-213` | «213 уроков, каждый ровно в одной фазе» | `program-227`: 213 + 14 |
| Биективность треков | 79 core + 134 elective = 213 | **93 core** (79 + 14) + 134 elective = 227 |
| Проверка `phase` | 0…8 | 0…9 (9 = Py-трек, 14 уроков) |
| Новый тест `py-track` | — | py_l1…py_l14 существуют, `prerequisites` образуют ациклическую цепочку, у каждого ≥ 1 `interactive_code_lab` и ≥ 3 квиз-вопросов, `terms` непустые |
| `PHASE_TESTS` | 83 вопроса | без изменений (PY_TEST — отдельный банк) |

### 6.4. Глоссарий

В `CATS` добавить категорию (нужен свой цвет, зелёный свободен):

```js
code: ['Код и автоматизация', '#34d399']
```

В `TERMS_RAW` добавить ≥ 16 терминов с полями `{t, c:'code', d, a}` (d — определение по правилам §2.2, a — аналогия):

Переменная · Присваивание · Строка · f-строка · Условие (if/else) · Логическое значение · Отступ · Список · Индекс · Срез · Цикл (for) · Счётчик · Функция · Аргумент · Возврат (return) · Словарь · Ключ · API · JSON · API-ключ · Библиотека · Импорт · DataFrame · pandas · Индикатор · Скользящая средняя · Сигнал.

Hover-подсказки в уроках модуля заработают автоматически (существующий движок всплывающих подсказок читает `TERMS_RAW`).

### 6.5. Граф Знаний (154 узла → 169, 240 рёбер → 259+)

**Новый кластер:** `code` («Код и автоматизация», цвет #34d399) — 14 узлов Py-01…Py-14, узел-хаб — Py-14 (мини-проект).

**Рёбра (17):**

| Из | В | Семантика |
|---|---|---|
| p0_l13 | Py-01 | «считать умеешь — учим бота» |
| Py-01…Py-07 | следующий Py-0N+1 | цепочка обучения (6 рёбер) |
| Py-03 | p0_l4 | цена и спред |
| Py-04 | p0_l9 | заявки: решение → ордер |
| Py-05 | p0_l7 | OHLC |
| Py-07 | p4_l8 | функции → фреймворки |
| Py-08 | p0_l7 | паспорт свечи |
| Py-09 | p4_l3, p0_l20 | безопасность ключей (2 ребра) |
| Py-10 | p1_l1 | pandas в Фазе 1 |
| Py-11 ↔ p1_l6 | мостик стакан→код (2 ребра) |
| Py-12 | p0_l14 | среднее |
| Py-13 | p1_l6, p1_l7 | бэктест и look-ahead (2 ребра) |
| Py-14 | ft02, vc_l1 | куда дальше (2 ребра) |

Геометрия кластера — существующий физический движок графа (154 ноды/240 рёбер/6 кластеров уже без внешних библиотек); новый кластер привязать между «Фазой 0» и «Фазой 1».

### 6.6. Прогресс, ранги, локализация

- Прогресс этапа A2 пишется в существующие LS-ключи по id уроков — изменений не требует.
- Полоса «Python: N из 14» — новая секция в существующем tier-трекере.
- Бейдж «Первый алгоритм» — по образцу существующих бейджей.
- Все тексты — только ru (курс ru-first; en/pt/es — после валидации модуля, вне скоупа v14.1).

---

## 7. Роадмап Pyodide/WASM (этап 2, v15)

Виджет `interactive_code_lab` спроектирован так, что этап 2 не меняет контент — только движок:

1. **Загрузка:** Pyodide 0.28+ (CPython 3.12) по требованию при первом открытии лабы с `engine:'pyodide'`; lazy-load ~10 МБ с CDN, прогресс-бар, кэш Service Worker; фоллбек офлайн → остаётся JS-имитация этапа 1 (курс остаётся offline-first — требование ресёрча №3, п.34).
2. **Изоляция:** выполнение в Web Worker; сетевых запросов из песочницы нет; данные только seed (60 свечей из сквозного примера).
3. **Библиотеки предустановлены:** pandas/numpy микропакеты (~2 МБ доп.) — только для Py-10…Py-14.
4. **Правила показа:** время выполнения < 50 мс на бэктесте 60 свечей (норма ресёрча №3); таймаут 2 с с дружелюбным сообщением «Бот задумался дольше обычного — проверь цикл».
5. **Постепенность:** флаг `engine` переключается на урок/на курс, можно катить по одному уроку.

---

## 8. Метрики успеха (по канонам ресёрчей)

| Метрика | Цель | Как меряем |
|---|---|---|
| Доходимость модуля Py-01 → Py-14 | ≥ 48% (бенчмарк ресёрча №3) | телеметрия завершения шагов |
| Среднее время модуля | ≤ 2.5 ч суммарно | таймстампы |
| Точность квизов с 1-й попытки | 65–75% (ниже — курс слишком плотный, выше — слишком лёгкий) | CBM-события |
| Brier для PY_TEST | < 0.12 (цель ресёрча №3) | CBM-уверенность |
| Доля дошедших до Фазы 1 после Py-14 | ≥ 70% | телеметрия |
| Возвраты к лаборатории Py-14 | ≥ 3 прогонов/ученик | телеметрия |
| Жалобы «слишком сложно» (кнопка «Это непонятно» в лабах) | < 5% сессий | явная кнопка |

---

## 9. План внедрения (для агента, 4 PR)

1. **PR-1 (движок):** `interactive_code_lab` (4 режима + подсказки + телеметрия) и `interactive_quiz_cbm`; smoke-тесты новых блоков. Без контента.
2. **PR-2 (контент, блок А):** `PY_LESSONS` Py-01…Py-07 + глоссарий + CATS + PY_TEST вопросы 1–8.
3. **PR-3 (контент, блоки Б+В):** Py-08…Py-14 + PY_TEST 9–15 + CN_TRACKS (этап A2, coreSet, version bump) + smoke-обновления (227/93/фаза 9).
4. **PR-4 (Граф Знаний):** кластер code, 14 узлов, 17 рёбер, физика; регресс-скриншоты графа и трекера.

Порядок критичен: движок до контента (иначе 28 падающих виджетов), трек — последним PR-ом перед графом (иначе program-227 падает в промежуточных коммитах).

**Риски:**
- Прогресс существующих учеников — миграция CN_TRACKS.version (тест на 3 профилях: новый/полупройденный/213-завершённый).
- «Плоскость» квизов — обязательный ревью дистракторов по §4.3.
- Перегруз Py-06 (трассировка цикла) — первая точка отсева в аналогичных курсах; держать виджет пошаговым, никогда «нажал — выполнилось всё».

---

## 10. Что документ НЕ содержит (сознательно)

- Полные тексты всех 14 уроков (это следующий шаг — черновики уроков по данному ТЗ).
- Полный банк из 15 вопросов PY_TEST (шаблоны и 5 образцов — §4.4).
- Дизайн-макеты лаб (используется действующая дизайн-система плеера v14: токены, радиусы, анимации lpSlideIn).
- Локализацию en/pt/es.
