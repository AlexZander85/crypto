# ТЗ-2 для эксперта: перекодирование существующих интерактивов (взамен текущих реализаций)

> **Кому:** эксперт (автор fable.md–fable6.md) · **От:** сборка курса.
> **Что закрывает:** ~48 интерактивов, которые НЕ охвачены вашими спеками (ни «строить», ни «сохранять», ни «доработать») — при этом их текущий код визуально и функционально слабее уровня ваших спек части 3. Мы хотим: вы **сами кодируете** новые версии, мы вставляем их в приложение вместо текущих.
> **Чем это отличается от заказа №1 (спеки):** там вы писали ТЕКСТ-спеку, кодил агент. Здесь вы пишете ГОТОВЫЙ КОД по контракту §2. Спек-часть остаётся короткой (5 строк), основной объём — код.
> **Формат сдачи:** markdown-файл (как fable6.md), внутри — блоки ```js по одному виджету. Каждый блок самодостаточен и проверяется в браузере через «песочницу-харнесс» §2.4.
> **Основание:** ваша оценка качества (fable.md ч.4: «одна аналогия, объяснение после попытки, ага-момент») + наш аудит 256 виджетов: 164 не упомянуты ни в одной спеке, из них ~48 достаточно ценны, чтобы их перекодировать, а не чинить.

> **⚠️ Правка 1 (05.09.2026).** При составлении полной карты «урок → интерактивы» (см. `V15.1-LESSONS-INTERACTIVES-MAP.md`) найдены и исправлены ошибки Тира 1: (а) **номера М-уроков были сдвинуты** — сверяйте заново: W-02→М10, W-03→М6, W-04→М8, W-05→М29, W-06→М45, W-07→М31, W-08→М34, W-09→М37, W-10→М46, W-11→М47, W-12→М26, W-13→М41 (W-01 без изменений); виджет-id во всех карточках были и остаются верными — проверяйте привязку по id, а не по номеру; (б) W-06/W-10/W-11 — их текущие реализации вообще **не подключены к урокам** (мёртвый код: функции в `MATH_WIDGETS` есть, блоков в уроках М45/М46/М47 нет) — ваш код станет первым, который ученик увидит; (в) **W-47 снимается** — на `widget_p617_uma` есть ваша спека из fable5 (переименование в «Цена против опросов», перенос в Б10), дублировать не нужно; всего в ТЗ-2 остаются 47 позиций. Если вы уже начали код по старым номерам — ничего страшного: сдавайте по виджет-id, мы сами разложим по урокам.

---

## 0. Почему именно эти виджеты (методика отбора)

Полный аудит приложения: **256 виджетов** в 9 подсистемах (lesson-json, CN_W2, CN_MINI, CAPSTREAM, v4, MATH_WIDGETS, психология, бонусы, аркада). Из них:

- **~92 покрыты вашими спеками** (реестр интерактивов №1–131) — строится/перестраивается/доработается по ним, сюда НЕ входит;
- **6 CAPSTREAM-виджетов** (3D-стакан, анатомия свечи, ловушка спреда, разорение, карта ликвидаций, жизненный цикл сделки) — уже canvas-реализации приличного уровня, трогать не просим;
- **Python-песочница** (Pyodide+Worker) — зрелая, вне заказа;
- **строго отобраны 48 позиций** в 4 тирах ниже: (а) урок НЕ переписывается вами (текст стабилен, виджет не будет выброшен при сборке), (б) виджет несёт концептуальную нагрузку урока, (в) текущая реализация — plain-DOM (кнопки/текст/ползунки без графики и анимации), (г) тема вам близка (математика, психология, механика рынка).

**Что сознательно НЕ отдаём** (чтобы вы не потратили время): всё, что уже имеет вашу спеку; vc1–vc3 (заменятся спеками №111–113 при сборке ВК); p4_l1/p4_l3/g13 (закрыты симуляторами «Три ночи без тебя»/«Карта инфраструктуры»/«Утечка ключа» из fable5 — кстати, они не были внесены в реестр, мы внесли их отдельной правкой, №132–134); g18/g19/g23/g22 (спеки №125–127/№130); П16/П21/П27/П34/П41 и Д.1-список (спеки есть). Полный «резерв» — §7.

---

## 1. Константы канона (сверять каждое число в коде)

Те же, что в ТЗ-1 (`V15.1-EXPERT-TZ-QUESTION-BANKS.md` §0): счёт $1000 · комиссия 0,10%/0,02% · проскальзывание 2 б.п. · фандинг 0,03%/8 ч · риск на сделку 1% · винрейт 55% / R 1.3 · ED <10/10–25/>25 · бумага 8 недель и ≥50 сделок · плечо в год 1 запрещено · восстановление −33% → +50% · серия убытков ln(n)/−ln(q) · правило 72 · лимит 20% на площадку · 40/40/20 (обе версии). Если виджету нужны другие числа — брать из текста урока (указано в каждой карточке ниже), НЕ выдумывать новые.

---

## 2. Технический контракт кода (обязателен, проверяется автоматически)

### 2.1 Регистрация

Каждый виджет — одна самодостаточная функция, регистрируется в глобальной карте:

```js
window.EXPERT_WIDGETS = window.EXPERT_WIDGETS || {};
window.EXPERT_WIDGETS['widget_m_kelly_criterion'] = function(box){
  // box — <div> контейнер, уже существует в DOM
  // весь код, состояние и обработчики — внутри этой функции
};
```

- Имя ключа = существующий `id` виджета в приложении (указан в каждой карточке, менять нельзя — на него ссылаются уроки и статистика).
- Никаких других глобальных имён. Всё состояние — в замыкании функции.
- Обработчики событий — через `box.addEventListener` / делегирование, **не** инлайновые `onclick` с глобальными функциями (текущий код так делает — это и есть одна из причин замены).
- Идентификаторы DOM внутри виджета — с уникальным префиксом (`kw_…`), вложенные запросы — только `box.querySelector`/`box.getElementById`-эквивалент. **Запрещено** `document.getElementById` вне `box`.

### 2.2 Разрешено / запрещено

| Разрешено | Запрещено |
|---|---|
| vanilla JS (ES2017), Canvas 2D, inline SVG, CSS-переходы/анимации, `requestAnimationFrame`, `setTimeout/setInterval` | библиотеки, CDN, `fetch`, `import`, сборщики, внешние шрифты/картинки (кроме data-URI), `alert/confirm/prompt` |
| тёмная тема приложения через CSS-переменные | захардкоженные светлые темы, иные палитры |
| детерминированный ГПСЧ `mulberry32(seed)` (код в §2.3) | `Math.random()` для учебных сценариев, где важна повторяемость (для «шума рынка» — можно seed из кнопки «новый раунд») |
| адаптив от 360px, без горизонтального скролла | фиксированные ширины > 360px, таблицы шире контейнера |

**Дизайн-токены темы** (палитра приложения): фон карточки `#040714`-`#0d1022`, текст `var(--txt)` #eef1ff, вторичный `var(--mut)` #9aa3c7, линии `var(--line)`, акцент циан `var(--acc2)` #06b6d4, успех `var(--ok)` #22c55e, ошибка `var(--bad)`/`var(--err)` #ef4444, предупреждение `var(--warn)` #eab308, код/цифры `var(--mono)`. Скругления 8–12px, отступ 12–16px — как у текущих карточек, но лучше.

### 2.3 Таймеры и повторный рендер (чистота)

Виджет обязан переживать повторное открытие урока без дублей и утечек. Паттерн:

```js
window.EXPERT_WIDGETS['widget_m_kelly_criterion'] = function(box){
  // 0. чистим прошлый запуск
  if(box._expTimers){ box._expTimers.forEach(t=>clearInterval(t)); }
  if(box._expRaf){ cancelAnimationFrame(box._expRaf); }
  box._expTimers = []; box._expRaf = null;
  const later = (fn, ms, rep)=>{ const t = rep ? setInterval(fn, ms) : setTimeout(fn, ms); box._expTimers.push(t); return t; };
  const raf = fn => { const id = requestAnimationFrame(fn); box._expRaf = id; };

  // seeded RNG
  const mulberry32 = seed => () => {
    seed |= 0; seed = seed + 0x6D2B79F5 | 0;
    let t = Math.imul(seed ^ seed >>> 15, 1 | seed);
    t = t + Math.imul(t ^ t >>> 7, 61 | t) ^ t;
    return ((t ^ t >>> 14) >>> 0) / 4294967296;
  };
  const rnd = mulberry32(42); // фикс. seed сценария; кнопка «новый раунд» → mulberry32(Date.now())

  box.innerHTML = `…карточка…`;
  // …логика…
};
```

**Самопроверка перед сдачей (обязательна):** (1) открыть харнесс §2.4, отрендерить виджет; (2) нажать «Рендер повторно» — не должно быть дублей интерфейса и ускорения анимаций; (3) сузить окно до 360px; (4) проверить, что числа = канон.

### 2.4 Песочница-харнесс (для локальной проверки, приложите себе)

```html
<!DOCTYPE html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
<style>
  :root{--bg:#0d1022;--panel:#040714;--txt:#eef1ff;--mut:#9aa3c7;--line:rgba(255,255,255,.12);--acc2:#06b6d4;--ok:#22c55e;--err:#ef4444;--warn:#eab308;--mono:'Consolas','Menlo',monospace}
  body{background:var(--bg);color:var(--txt);font:14px/1.5 system-ui,sans-serif;padding:24px}
  .btn{background:var(--acc2);color:#040714;border:0;border-radius:8px;padding:8px 14px;font-weight:700;cursor:pointer}
  .btn.ghost{background:transparent;color:var(--txt);border:1px solid var(--line)}
  #wrap{max-width:720px;margin:0 auto}
</style></head><body><div id="wrap">
  <button class="btn ghost" onclick="rerun()">Рендер повторно (тест чистоты)</button>
  <button class="btn ghost" onclick="window.innerWidth<420&&alert('сузьте окно руками')">Тест ширины: сузьте окно до 360px</button>
  <div id="stage" style="margin-top:16px"></div>
</div>
<script>
/* ===== вставьте сюда свой блок ```js целиком ===== */
/* ===== конец блока ===== */
const ID = 'widget_m_kelly_criterion';           // ← поменять на id своего виджета
function rerun(){ EXPERT_WIDGETS[ID](document.getElementById('stage')); }
rerun();
</script></body></html>
```

### 2.5 Стандарт качества: что значит «лучше текущего»

1. **Живая картинка вместо текста.** Текущий уровень — кнопка пишет строку («Бросков: 40, орлов 62%»). Целевой — графическая сцена с мгновенной обратной связью: монеты сыплются, кривая частоты ползёт к 50%, линия урока «горизонт 50%» светится.
2. **«Ага»-момент как сцена.** Не подпись «такой-то эффект», а визуальное событие, которое ученик ПОСЛЕДНЯЯ секунда которого запоминает (в ЦПТ — гистограмма средних «схлопывается» в узкий колокол на глазах; в тильте — паутина стягивается на счёт с каждой «отыграться»).
3. **Действие → немедленная оценка.** Не «прочитай и нажми», а выбор с последствиями на сцене и разбором ПОСЛЕ попытки (ваш стандарт ч.4: дистракторы = реальные заблуждения, объяснение к каждому).
4. **Повторные попытки и режим «а что если».** Ползунки/кнопки меняют параметры на лету; кнопка «новый раунд» = новый seed; счёт попыток.
5. **Цифры канона видны.** Числа из §1/карточки — на сцене (подписи осей, значения), а не спрятаны в коде.
6. **Педагогическая развертка сохраняется:** цель → задание → ага → артефакт (см. §3 шапку к каждому виджету — 5 строк спеки ПЕРЕД кодом: «Цель/Задание/Ага/Дефолты/Артефакт»).

---

## 3. ТИР 1 — Матфак (13 виджетов): максимальный приоритет

> Это ваш профиль (математика) и самый слабой визуал в приложении: почти все 45 m-виджетов — кнопки и текстовые строки, ни одной живой картинки. Тексты М-уроков стабильны (ваш вывод: «матфак — редактура, не переписывание»), виджеты никто не тронет. Ниже — 13 концептуальных центров; остальные 32 — «резерв» §7 по вашему выбору.
> **Общие дефолты трека:** винрейт 55%, выплата 1:1 (R=1) и R 1.3 как эталон; счёт $1000; серия убытков ln(n)/−ln(q); все гистограммы/облака — SVG или canvas, оси подписаны по-русски.

| # | id (ключ регистрации) | Урок | Сейчас | Переписать: сцена и «ага» |
|---|---|---|---|---|
| W-01 | `widget_m_chto_voobsche_takoe_veroyatnost` | М1 «Что вообще такое вероятность?» | 3 кнопки ×1/×100/×10000, строка «Бросков: N · Орлов: X%» | Поле монет: падают решкой/орлом, стопка растёт; под ней — кривая частоты, ползущая к линии 50%. «Ага»: первые 10–20 бросков кривая скачет на ±20%, к 10 000 — липнет к 50%. |
| W-02 | `widget_m_zakon_bolshih_chisel` | М10 (ЗБЧ) | кнопка + текст | Одна и та же «честная» монета × 3 одновременных трека (n=10/100/1000): чем больше n, тем тише разброс. Пауза/шаг/скорость. |
| W-03 | `widget_m_matematicheskoe_ozhidanie` | М6 «Математическое ожидание» | ползунки, число EV | Два автомата (как в зале): крутишки, лампочки, итоговая копилка; ползунки p/win/loss; EV-шкала с зоной −/0/+; после 100 спинов — факт против EV. |
| W-04 | `widget_m_razbros_rezultatov` | М8 «Разброс результатов» | текст | Две стратегии, среднее одинаковое (+2%), разброс разный: две ленты траекторий за год на одном полотне; счёт «сколько дней в минусе» у каждой. |
| W-05 | `widget_m_asimmetriya_i_hvosty_zachem` | М29 «Асимметрия и хвосты» | почти пустой (1,3 КБ) | Два P&L-профиля: «продавец страховки» (часто +1%, редко −25%) против «покупателя хвоста»: две живые гистограммы, кнопка «прожить год» — по одной сделке в обе, хвост бьёт по счёту. |
| W-06 | `widget_m_cpt_centralnaya_predelnaya_teorema` | М45 ЦПТ | **не подключён**: реализация есть в `MATH_WIDGETS`, но блок в уроке отсутствует — ученик её не видит (мёртвый код; блок подключим на сборке, ваш вариант его заменит) | Сверху — любое уродливое исходное распределение (ползунок формы: равн./скошен./двугорбое); кнопка «брать выборку n=25» — сверху сыплются шарики, снизу гистограмма СРЕДНИХ растёт в колокол. «Ага»: исход — любой, средние — всегда колокол. |
| W-07 | `widget_m_doveritelnyy_interval_bez_strashnoy` | М31 ДИ | текст | 30 выборок подряд из одной генеральной (μ известна нам, не ученику): каждый эксперимент рисует свою полоску ДИ; зелёная — накрыла μ, красная — мимо. Счётчик «накрыли 28/30 ≈ 93%». Ползунок n и доверия. |
| W-08 | `widget_m_p_value_chto_eto` | М34 p-value | текст | «Фабрика сигналов»: 100 честных монет (нулевая гипотеза везде). Кнопка «прогнать всех». Порог α=0.05 подсвечивает ~5 «открытий». «Ага»: сигналы нашлись там, где их нет. |
| W-09 | `widget_m_korrelyaciya_dva_ryada_dvizhutsya` | М37 «Корреляция» | текст | Облако точек, ползунок r от −1 до 1; при |r|→1 точки стягиваются в линию; счётчик «доля дней, когда оба ряда в плюсе». Кнопка «выброс» — одна точка улетает, r прыгает: r ≠ устойчивость. |
| W-10 | `widget_m_regressiya_k_srednemu` | М46 «Регрессия к среднему» | **не подключён** (мёртвый код в `MATH_WIDGETS`, блока в уроке нет — см. W-06) | Два сезона «фонд А»: круг из 100 точек (год1 × год2), линия «идеальное продолжение» и эллипс сжатия к среднему. Наведи на точку — траектория. «Ага»: прошлогодний топ почти никогда не повторяет. |
| W-11 | `widget_m_kelly_criterion` | М47 Келли | **не подключён** (мёртвый код в `MATH_WIDGETS`, блока в уроке нет — см. W-06); в приложении есть ДРУГОЙ виджет Келли — `widget_g10_kellyfan` в аркаде, он сохраняется | Ползунок доли ставки 0→3×Келли; веер из 200 траекторий (seed!); при >Келли — часть траекторий ныряет в ноль навсегда (разорение — не «меньше прибыли», а «исчезновение»). Подпись канона: «70–90% исходов высоковероятны» — НЕ «гарантия». |
| W-12 | `widget_m_mediana_i_kvantili` | М26 «Медиана и квантили» | таблица чисел | Гистограмма 200 сделок с «мемом» внутри; ползунок тянет квантильную линию P25/P50/P75, подпись «N% сделок левее». Кнопка «добавить 1 выброс» — среднее прыгает, медиана стоит: разница видна. |
| W-13 | `widget_m_pereobuchenie_na_palcah` | М41 «Переобучение» | текст | Ползунок сложности модели 1–12: на левой панели модель подгоняется под train-точки (кривая извивается), правая — test-ошибка растёт. Две кривые ошибки: U-образная test против падающей train. |

**Приёмка Тира 1:** каждый виджет — карточка «Цель/Задание/Ага/Дефолты/Артефакт» (5 строк) + блок ```js; числа канона; тесты §2.3; гистограммы/облака подписаны по-русски.

---

## 4. ТИР 2 — Психология П1–П14, блок «Сеть и нервная система» (14 виджетов)

> Единственный большой блок психологии, чьи ТЕКСТЫ вы не переписывали (П15–П57 закрыты, П1–П14 — стабильные старые) — значит, виджеты гарантированно останутся в сборке. Текущий вид — текстовые сценарные тренажёры (выбор из 3–4 кнопок + строка разбора). Цель: сохранить сценарную драматургию, добавить сценическую подачу — интерфейс «телефона/чата/панели» вместо текста, немедленные последствия на счётчике, разбор после действия.
> **Общие правила трека:** каждое событие — карточка с контекстом (время, звук-строка, цифра); выбор из 3–4 вариантов; ПОСЛЕ выбора — «правда о событии» и последствие в двух метриках: «качество сна/спокойствие» и «предотвращённые потери/прибыль»; финал — сводка + артефакт (правило одним предложением). Протокол «мир или голова» (П1) — сквозной фильтр блока.

| # | id | Урок | Сцена и «ага» |
|---|---|---|---|
| W-14 | `widget_ps_l1_night_alerts` | П1 «Сломался или просто страшно?» | Ночной телефон: поток уведомлений 01:40–04:50 (бот молчит 15 мин; цена −1,8%; «срочная» новость; позиция в плюсе…). На каждое — «спать/записать утром/проверить/аварийная кнопка». «Ага»: только у 1 из 6 событий есть «факт мира», остальные — погода в голове; утром всё то же самое. |
| W-15 | `widget_ps_l2_trust_scale` | П2 «Доверяй машине, но стой рядом» | Качели: маятник «ручная опека ↔ слепое доверие» с двумя ползунками (частота проверок, порог вмешательства); 8 недель симуляции: слева — издержки вмешательств, справа — пропущенный сбой. Оптимум — узкая полоса посередине. |
| W-16 | `widget_ps_l3_green_streak` | П3 «Головокружение от успехов» | Серия зелёных дней растёт — рядом ползунок «размер ставки» ползёт сам (эго подталкивает); шкала «риск-аппетит» уходит в красный; в конце серии — обычный красный день × раздутый размер = удар, которого «не ждал». |
| W-17 | `widget_ps_l4_anchor_trap` | П4 «Ловушка „хоть бы вернуть своё“» | Позиция −33%: кнопки «закрыть / ждать / докупить»; лента «внутренний голос» (якорь на нуле); счётчики «время в минусе» и «дистанция до нуля» (−33% → нужно +50%, не +33% — канон!). |
| W-18 | `widget_ps_l5_babymonitor` | П5 «Ночная смена» | Радионяня: ползунок «сколько раз в ночь смотришь счёт» → графики: качество сна падает, а информационная ценность просмотров ≈ 0 (последние 10 взглядов не добавили ничего). |
| W-19 | `widget_ps_l6_dopamine_detector` | П6 «Качели как сахар» | Дофаминовый детектор: лента «событий» (зелёная свеча, чужой гол, движение цены); шкала «тяга к действию» растёт НЕ от пользы, а от движения; кнопка «ничего не делать» гасит шкалу медленно — «скука — часть профессии». |
| W-20 | `widget_ps_l7_future_letter` | П7 «Письмо из будущего» | Конструктор катастрофы ДО неё: 3 поля «что сломается через 8 недель» (выбор из карточек-признаков) → «отправить» → 8 недель перематываются → рядом с фактом. Счёт «угадано X из 3» + разбор, почему догадка ≠ предсказуемость (угаданное — признак, а не везение). |
| W-21 | `widget_ps_l8_match_judge` | П8 «Судьи считают технику» | Два «судьи в ложе» (оценка за гол vs за технику): 10 сделок сезона, за каждую — «толпа» шумит (результат), «судья» ставит за исполнение протокола. Итог сезона: корреляция результата и техники ≈ 0 на коротком отрезке. |
| W-22 | `widget_ps_l9_dopamine_trap` | П9 «Дофаминовый капкан» | Два экрана: «ожидание сделки» (прогресс-бар „до сигнала“ — шкала удовольствия высокая) и «после входа» (то же движение — скука). Пик кривой — ДО входа. Подпись: мозг платит за ожидание, не за результат. |
| W-23 | `widget_ps_l10_loss_pain` | П10 «Боль фиксации убытка» | «Пластырь»: позиция −1R горит красным; выбор «наклеить» (закрыть) оттягивается — чем дольше тянешь, тем больше «мысленная anesthesia» (шкала онемения), а убыток фактически не меняется — R уже зафиксирован стопом. |
| W-24 | `widget_ps_l11_hormone_storm` | П11 «Гормональный шторм» | Обвал на экране (свечи падают) + «кровь в голове»: шкалы кортизол/пульс; рядом — тест из 3 вопросов на логику (простые), таймер 10 с. «Ага»: на пике шкалы правильность ответов падает вдвое — «на обвале мы тупеем» измеримо. |
| W-25 | `widget_ps_l12_mental_battery` | П12 «Ментальный капитал» | Батарейка решений: день тикает (09:00→15:00→21:00), каждое решение (не только торговое) ест заряд; ползунок «во сколько трогаешь конфиг бота»: после 15:00 — батарея в красном, вероятность ошибки ×2 (визуально: дрожащая рука на ползунке). |
| W-26 | `widget_ps_l13_body_calm` | П13 «Физиология спокойствия» | Интерактив дыхания: круг «4-7-8» (вдох/задержка/выдох) с анимацией; рядом шкала «импульс вмешаться» — за 3 цикла падает; кнопка «сорваться сейчас» сравнивает выбор ДО/ПОСЛЕ дыхания на том же событии. |
| W-27 | `widget_ps_l14_tilt_web` | П14 «Паутина тильта» | Паутина: серия «отыграться» — каждая нить тянет счёт вниз, паутина сжимается; в центре — «просадка × размер» растёт нелинейно; кнопка «выйти из-за стола» разрывает нити; счёт «через сколько минут ты сдался» + классический финал удвоения. |

**Приёмка Тира 2:** тот же контракт §2 + сценарная драматургия (не менее 5 событий на тренажёр, правдоподобные тексты в стиле Тендлера/Дугласа из текущих уроков П1–П14), никакого «ты молодец!» за неверные действия — только факт и последствие.

---

## 5. ТИР 3 — Фаза 0, онбординг (13 основных + 5 дополнительных)

> Самый посещаемый блок курса: первое впечатление о продукте. Тексты уроков 0.1–0.14, 0.18–0.19 стабильны (вы их не переписывали), виджеты остаются в сборке. Текущий уровень — от «статичного SVG с кнопкой» до «строк в логе». Ниже — что должно остаться в памяти у новичка ПОСЛЕ вашей версии.
> **Общие дефолты трека:** счёт $1000, BTC ~$100 000, комиссия 0,10% тейкер / 0,02% мейкер, спред 50 б.п. в учебном стакане, риск 1%, плечо в год 1 запрещено.

| # | id | Урок · сейчас | Переписать: сцена и «ага» |
|---|---|---|---|
| W-28 | `widget_p0_l1` | 0.1 · текст-лог «[0.4с] транзакция передана нодам…» | Карта сети: 6 узлов-городов, транзакция бежит по линиям, подпись цифровой; майнер собирает блок, страница журнала блоков печатает хэш. «Ага»: перевод = объявление ВСЕМ, а не письмо Бобу. |
| W-29 | `widget_p0_l2` | 0.2 · похожий лог | Тот же движок (или общий с W-28): подпись приватным ключом показать как «замок→ключ», 2 подтверждения, комиссия майнеру. Вторая вкладка «что видно миру / что видишь только ты» (секретность адреса ≠ секретность баланса). |
| W-30 | `widget_p0_l4` | 0.4 · 3 цифры (Bid/Ask/спред) + 3 кнопки | Живой мини-стакан: лимитки на двух сторонах, кнопки «покупатель повысил / продавец снизил / сделка!» двигают строки; при совпадении — вспышка принта и спред схлопывается; кривая «последняя цена» рисуется. «Ага»: цена = последняя сделка двух терпеливых. |
| W-31 | `widget_p0_l5` | 0.5 · статичный SVG-полилиния | Поток сделок тиками (правая кромка), светится текущая цена; ученик сам жмёт «новая сделка» ×10 → они сшиваются в свечу; потом свеча «запечатывается» и начинается следующая. Связь с W-32 (0.8). |
| W-32 | `widget_p0_l8` | 0.8 · тренажёр OHLC | Крупная «живая» свеча: тики прилетают, тело/фитили растут на глазах; задания «что станет High, если придёт тик 101 200?» — выбор до тика, оценка после. |
| W-33 | `widget_p0_l10` | 0.10 · калькулятор проскальзывания | Стакан 5 уровней глубиной с объёмами; ползунок размера рыночного ордера: ордер «проедает» уровни, средняя цена исполнения ≠ лучшая; счёт «переплата от мгновения» = проскальзывание + комиссия 0,10%. |
| W-34 | `widget_p0_l11` | 0.11 · расчёт PnL | Позиция $1000, ползунки «цена входа/выхода, размер, комиссия»: мгновенный PnL с разложением на «движение − комиссии − проскальзывание»; кнопка «перевернуть сделку» показывает симметрию прибыли/убытка. |
| W-35 | `widget_p0_l13` | 0.13 · броски монетки | Тот же движок, что W-01/W-02 (матфак): монета трейдера — 55% против честной 50%; кривые частот обе; «ага»: маленькое преимущество видно только на дистанции. |
| W-36 | `widget_p0_l14` | 0.14 · калькулятор EV | Ползунки WR / средний плюс / средний минус → EV; шкала с зонами; «бесполезный grapes-винрейт»: при 99% WR и R=0.05 EV<0 — торговый автомат «выдаёт» серию и обвал. |
| W-37 | `widget_p0_l16` | 0.16 · ползунок плеча + число ликвидации | Визуальная позиция: столбец капитала, линия ликвидации; ползунок плеча 1–50×: линия подползает к цене; «прожить день» — случайное движение ±(1–5)%; при 25× обычный день убивает позицию. Канон: в год 1 плечо запрещено. |
| W-38 | `widget_p0_l18` | 0.18 · калькулятор разорения | 100 игроков в рулетку с разными ставками (1/2/5/10/25% банка): сетка траекторий; к финалу — сколько выжило при каждой ставке; кривая банкротства от размера ставки (не от винрейта!). |
| W-39 | `widget_p0_l19` + `widget_p0_l19_dd` + `widget_p0_l19_steps` | 0.19 · чек-листы скама | Единый детектив «Проект CoinX»: сайт/твит/ончейн-карточки, 5 признаков скама как улики; выбор «инвестировать / копать глубже / PASS»; этап «Etherscan» — настоящий интерфейс обозревателя (упрощённый): верификация контракта, держатели, возраст. |
| W-40 | `widget_p0_l20_phish` | 0.20 · 6 ситуаций фишинга | «Почтовый ящик»: письма/сайты/окна подписи; при наведении — лупа показывает URL (o → 0, ru → гu); ошибся — «кошелёк» худеет. 2 письма из 6 — настоящие (не все письма — скам). |

**Дополнительные (те же уроки, отдельные виджеты — по остаточному принципу, после основных 13):**

| # | id | Урок | Что улучшить |
|---|---|---|---|
| W-41 | `widget_g01_blockrace` | 0.1 · «Гонка блоков» (майнинг-мини-игра) | Сейчас — простая гонка; сделать честную экономику: скорость хэшей vs электричество, награда достаётся одному, остальным — счёт за энергию; подключить к правилу 72 награды/электричество. |
| W-42 | `widget_g02_mm_minute` | 0.4 · «Маркетмейкер на минуту» | Дать живой стакан с кнопками «выставить/подвинуть/снять» и кнопкой чужого агрессора; итоговая минута: спред-доход vs один проскок. |
| W-43 | `widget_v4_seed` | 0.2 · «Защити seed-фразу» | Карточная колода 12 слов: собери seed (drag/click), затем «перепиши на бумагу» в правильном порядке; шпион-сцена: кто видел твой экран. |
| W-44 | `widget_v4_regime` | 0.8 · «Угадай режим рынка» | Уже игра с раундами — добавить «почему»-разбор: подсветка фракталов (HH/HL, сжатие), счёт серии, нелинейное вознаграждение за серию (казино-эффект понимания). |
| W-45 | `widget_v4_riskmgr` | 0.12 · «Спаси депозит» | Превратить в полноценный симулятор правила: ползунки «риск на сделку» × «серия убытков» → выживаемость; запрет ≥2% подсвечен уставом курса. |

---

## 6. ТИР 4 — Трек «Рынки предсказаний», непокрытое (3 виджета)

> Трек в целом покрыт вашими текстами Б6/Б8/Б9…, но у этих трёх виджетов спек нет (проверено по реестру №107–131). CN_MINI-формат (кейсы+финал) в приложении уже неплох — но это текстовые карточки; нужна сцена.

| # | id | Урок | Переписать |
|---|---|---|---|
| W-46 | `widget_p610_clob` | 6.10 «CLOB: стакан YES/NO» | Живой двусторонний ордебук YES/NO: сумма asks<1 — арбитраж мигает, но комиссия+заморозка капитала съедают «бесплатные 3%» (кейсы CN_MINI сохранить как режим «разбор»). |
| W-47 | ~~`widget_p617_uma`~~ | ~~6.17 «UMA-резолюция»~~ | **СНЯТО (правка 1):** есть ваша спека fable5 — переименовать в «Цена против опросов» и перенести в Б10; при сборке сделаем по ней |
| W-48 | `widget_g17_asic` | 6.1 «ASIC под напряжением» (майнинг; текст Б1 вы ещё напишете — виджет отдаём как есть, код пригодится для Б1) | Экономика майнера: сложность, цена, электричество; кривая доходности ASIC за 24 мес; «твой ASIC приехал, когда халвинг» — кривая падает вдвое. |

---

## 7. Резерв (НЕ заказывать сейчас — судьба решится при сборке)

| Группа | Виджеты | Почему в резерве |
|---|---|---|
| Психология П43–П56 | ps_l43/44/46/47/49/53–56 (8 шт) | Тексты переписаны fable5, новых спек нет — сохранение старых виджетов решит сборщик E4/E5 |
| Модуль 6, частично | p6_l5_theta, p6_l11_steps, g21_polyarb | Смежные спеки: «Конструктор выплат» (Б5) и «Асфальтоукладчик» (Б11, вы ещё напишете) могут их поглотить |
| FT | ft14_backtest_reader | Перекрывается станцией SB-3 «Песочницы Freqtrade» |
| Вайбкодинг | vc1/vc2/vc3 | Заменяются вашими спеками №111–113 при сборке ВК |
| Фаза 1 | p1_l2_qq, p1_l6_bridge, p1_l9_anim и др. | Ф1 тексты переписаны вами — судьба виджетов решится при сборке |
| CAPSTREAM | c2–c7 (6 шт) | Уже canvas-уровень; апгрейд целесообразен ТОЛЬКО после Тиров 1–3, отдельным решением |
| Аркада-остатки | art_memecoin_3click | Тема близка Б10/Б11 — дождаться сборки бонусов |
| Матфак-остатки | 32 m-виджета из 45 | По вашему выбору после Тира 1 — список в реестре, раздел Ж |

---

## 8. Порядок сдачи и объёмы

| Пакет | Состав | Приблизительный объём кода | Что закрывает |
|---|---|---|---|
| П1 «Матфак» | W-01…W-13 (13 шт) | 6–10 КБ на виджет ≈ 100–130 КБ | Самый слабой визуал + ваш профиль |
| П2 «Психология» | W-14…W-27 (14 шт) | 5–8 КБ ≈ 80–110 КБ | Единственный непереписанный психо-блок |
| П3 «Онбординг Ф0» | W-28…W-40 (13 шт) | 6–12 КБ ≈ 90–150 КБ | Первое впечатление курса |
| П4 «Дополнительные+рынки» | W-41…W-48 (8 шт) | 40–70 КБ | По остаточному принципу |

- Пакеты независимы, сдавайте в любом порядке; приоритет P1 → P2 → P3 → P4 (если успеваете меньше — берите верхние строки каждого тира, они отсортированы по важности).
- Один файл на пакет: `expert-widgets-P1.md` и т.д. Внутри — карточка (5 строк спеки) + ```js-блок на виджет.
- Прежде чем писать пакет целиком: пришлите 1 (один) виджет из пакета как образец — утвердим стиль, потом дописывайте остальные. Это дешевле, чем переписывать пакет.

## 9. Что делаем мы (интеграция на нашей стороне — вам не нужно)

1. **Единый диспатчер-патч:** перед штатным рендером каждого виджета приложение будет проверять `window.EXPERT_WIDGETS[id]` — ваш код автоматически заменяет текущий (откат = удаление блока из файла, перезагрузка не нужна).
2. **Автоприёмка:** `node --check` на каждый блок; тест двойного рендера; ширина 360px; скан на `alert(`/`fetch`/`onclick=`; сверка чисел с каноном по словарю §1.
3. **Откат и A/B:** замена по одному виджету, при расхождении метрик завершения урока — возврат старого кода за 1 коммит.
4. Мы НЕ правим ваш код руками (кроме критических багов) — все правки возвращаем вам списком, чтобы стиль оставался единым.

