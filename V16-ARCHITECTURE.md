# V16 — Архитектура сборки новой версии «КриптоНавигатора»

> **Статус:** принято решение (обсуждение 2026-09-05/06): **ни «шампур» поверх v15.1, ни переписывание с нуля — ПЕРЕСБОРКА.**
> Репозиторий становится исходником, `index_vXX.html` — генерируемым артефактом сборки.
> Решение зафиксировано после оценки объёма предстоящей работы: ~133 спеки интерактивов ждут реализации,
> 48 виджетов ТЗ-2 ждут встройки, 603 позиции банков вопросов ждут интеграции, ~131 новый текст уроков ждёт сборки —
> и всё это на фоне монолита 8 МБ / 93 274 строки, где каждая правка = python-хирургия со скобочным балансом.
>
> **Связанные документы:** `V15.1-UPGRADE-PLAN.md` (карта изменений, задачи T-4.x) · `V15.1-CONTENT-PACK.md`
> (эталонный архив поставок эксперта) · `content/` (рабочая разбивка) · `V15.1-LESSONS-INTERACTIVES-MAP.md`
> (карта покрытия, статусы ✓/⟳/·/✍) · `V15.1-INTERACTIVES-REGISTRY.md` (реестр спек) ·
> `PROJECT_SAAS_CLOUDFLARE.md` + `saas/` (облачный таргет) · `content/02-interactives/tz4-live-market-audit.md`
> (аудит живого слоя — основа §12) · `content/04-landing/` (спека лендинга, T-4.12).

---

## 0. Диагноз: почему «шампур» дальше нельзя

1. **Инструментный порог пройден.** Grep по 8 МБ ненадёжен; файл не читается целиком; каждая правка —
   скрипты `html.find()` + скобочный баланс (`scripts/patch_*.py`, `apply_*.py`). Стоимость правки растёт с объёмом.
2. **Слои-заплатки.** `patchMathIntegration` поверх старого `mountMathWidget`; баг фильтра префикса `widget_m_`
   (18 виджетов **молча** не монтировались — нашлось только рантайм-тестом); исторический кейс 34 невидимых уроков
   Ф1–Ф5 (рендерер не умел старые типы блоков). Монолит прячет такие вещи.
3. **7 контейнеров уроков × 4 механизма привязки виджетов** (карта, `scripts/build_lesson_widget_map.py`):
   блоки `interactive_*` в JSON уроков · ATT-массивы (c2/c2b/v4) · CN_W2-attach с тернарниками ·
   рантайм-`attachMathWidgets` (const MAP, 22 урока). Каждый новый интерактив = решение «через какой из четырёх
   механизмов его втыкать».
4. **Параллельная работа невозможна.** Один файл = один поток; конфликты при пуше уже случались дважды.
5. **Дрейф артефактов.** `saas/`-паки извлекаются из v9 — 178 уроков против 225 в v15.1 (отставание 47);
   v5.1→v15.1 — ~45 МБ снапшотов без читаемых диффов.

## 1. Почему «с нуля» — тоже ловушка

1. **Движок battle-tested**: 225 уроков, 256 интерактивов, 77 типов блоков, живой рынок+новости+наставник,
   Python-песочница (Pyodide+Worker), аркада. Переписать = месяцы повторной QA без защитной сетки.
2. **Миграция на ~80% уже случилась** — см. §2: контент давно живёт вне монолита. «Собрать с нуля» опоздал:
   мы это уже делали по частям.
3. **Офлайн-однофайл — продуктовая фича** (desktop-билд `tools/build_desktop_exe.py` тоже на нём). При
   конвейере она сохраняется навсегда как один из таргетов сборки.
4. Большой взрыв = долгий запуск двух систем без защитной сетки. Вместо него — фазированная пересборка
   под golden-паритетом (§6).

---

## 2. Отправная точка: что уже готово (не «с нуля», а «достроить 20%»)

| Слой | Готовность | Где |
|---|---|---|
| Тексты уроков (маршрут+факультативы) | **100%** (~131 позиция) | `V15.1-CONTENT-PACK.md` (эталон, дословно) + `content/01-lesson-texts/` (14 файлов по модулям) |
| **Переписанные уроки матфака (ТЗ-7, fable8)** | **12/12** + 12 карточек виджетов (5 новых спек + 7 правок) | `content/01-lesson-texts/math-rewrite.md` + `content/02-interactives/math-rewrite-widget-cards.md` |
| Спеки интерактивов | **~133** (60 ✓ в телах уроков + 37 ✍ ТЗ-3-детализация + Асфальтоукладчик + 2 дозаполнения ждём) | `V15.1-INTERACTIVES-REGISTRY.md` (реестр), `content/02-interactives/ |
| Готовый код виджетов ТЗ-2 | **48/48** + 3 общих слоя | `content/02-interactives/widgets/` (node --check 51/51, smoke 16/16) |
| Спеки двигателей E1–E8 + языковой слой | **8/8 двигателей + Lexicon** | `content/02-interactives/tz3-specs/` (47 файлов) |
| Банки вопросов ТЗ-1 | **603 позиции** | `content/03-quizzes/` (12 файлов) |
| **Криптик: карточки улучшений (ТЗ-6, fable_kriptik)** | **29 KR** (P0-набор 13; «атом урока» = формат фазы 0) | `content/06-mentor/kryptik-kr-cards.md` |
| **Карты факультативов (ТЗ-8/9, этап 1)** | **2 трека: 23 урока РФ (Р/О/В) + 14 уроков Ж — концепты, тексты заказаны (ТЗ-10/11)** | `content/05-electives/` |
| Контракт виджета | `window.EXPERT_WIDGETS[id] = function(box)` | `content/02-interactives/expert-rebuild-contract.md` + ТЗ-2 |
| Харнесс приёмки | двойной рендер, 360px, скан контракта, счётчик таймеров | `tools/expert-widget-harness.html` |
| Линтеры | квизы (0 BLOCK по 10 банкам), числовые вопросы (fable7) | `tools/quiz_lint.py`, спека в `content/03-quizzes/numeric-variants-f0-f5.md` |
| Облачный вертикальный срез | auth magic-link, синк прогресса 8/8, платежи, паки в R2, телеметрия | `saas/` (Cloudflare Workers) |
| Генераторы карты покрытия | 7 контейнеров + 4 механизма привязки | `scripts/build_lesson_widget_map.py` + `gen_map_doc.py` |
| Движок v15.1 | рендер, навигация, прогресс, геймификация, живой слой, песочницы | `index_v15.1.html` → переедет в `engine/` как есть |

---

## 3. Целевая структура репозитория

```
repo/
├── engine/                      # ЯДРО ПРИЛОЖЕНИЯ (извлекается из v15.1 БЕЗ изменения логики, фаза 1)
│   ├── core/                    #   рендер уроков, блоки (77 типов), навигация, прогресс, поиск
│   ├── widgets-legacy/          #   MATH_WIDGETS + CN_W2 + ATT — текущие виджеты как есть
│   ├── live/                    #   живой рынок + новости + наставник (детекторы, гейтинг)
│   ├── sandbox/                 #   Python-песочница (Pyodide-менеджер) + Песочница Freqtrade (SB-1..5)
│   └── shell.html               #   шаблон-оболочка (HTML+CSS+точки входа)
│
├── content/                     # ИСТОЧНИК ИСТИНЫ КОНТЕНТА (уже существует, расширяем)
│   ├── 01-lesson-texts/         #   тексты уроков по модулям (готово)
│   ├── 02-interactives/
│   │   ├── tz3-specs/           #   22 спеки ТЗ-3 (готово) — ТЗ для реализаторов
│   │   ├── widgets/             #   48 готовых виджетов ТЗ-2 (готово) + новые реализации
│   │   └── engines/             #   NEW: реализации E1..E8 + языковой слой (по спекам tz3-specs)
│   ├── 03-quizzes/              #   банки вопросов (готово)
│   ├── data/                    #   NEW: JSON-инстансы двигателей (scenario.json E1, profile.json E2,
│   │   │                        #        кейсы E6, фикстуры FT-15/19(2)/20, glossary.json, report_metrics)
│   └── lessons.json/            #   NEW: канонические JSON уроков (по одному на урок) — фаза 0/1
│
├── manifest/                    # МАНИФЕСТЫ СБОРКИ
│   ├── app.manifest.json        #   что входит в офлайн-билд (уроки/виджеты/двигатели/банки/данные)
│   ├── engines.manifest.json    #   порядок инициализации двигателей (зависимости из Приложения А)
│   └── saas.manifest.json       #   нарезка контент-паков для облачного таргета (§8)
│
├── build/                       # СБОРКА (не коммитим артефакты, коммитим скрипты)
│   ├── build.mjs               #   главный конвейер: manifest → inject → dist/index_v16.html
│   ├── build-app.mjs            #   (наследует saas/tools/build-app.mjs — добавляет integration.js)
│   ├── lint-content.mjs         #   линтер контента двигателей (Приложение А, п.5: пустые gutter,
│   │                            #   термины без ru_card, метрики без formula_ru/zones → падает в CI)
│   └── golden/                  #   паритетные DOM-снапшоты v15.1 (эталон для сравнения)
│
├── dist/                        # АРТЕФАКТЫ (git-ignored кроме релизных тегов)
│   └── index_v16.html           #   тот же офлайн-продукт, но генерируемый
│
├── saas/                        # ОБЛАЧНЫЙ ТАРГЕТ (существует; мигрирует на общий источник — §8)
├── tools/ scripts/              # существующая инструментальная обвязка (карта, линтеры, харнесс)
└── V16-ARCHITECTURE.md          # этот файл
```

**Правило:** в `engine/` при переносе логику НЕ меняем (фаза 1 — чистый переезд). Правки — только
фаза 2, под паритетными тестами, по одному пункту за коммит.

---

## 4. Канон форматов (фаза 0 — формализовать де-факто существующее)

### 4.1. Урок (lessons.json/<id>.json)

Единый конструктор, покрывающий все 7 текущих контейнеров (LESSONS, MATH_LESSONS_SOURCE,
PY_LESSONS_BLOCK_A/BC, PSY_LESSONS+2, VIBECODING_LESSONS, FT):

```json
{
  "id": "ft04", "num": "FT-04", "module": "ft",
  "title": "…", "minutes": 12,
  "terms": ["stake_amount", "dry_run"],
  "blocks": [
    {"type": "concept", "text": "…"},
    {"type": "interactive_engine", "engine": "e2", "instance": "ft_config_basic", "title": "…"},
    {"type": "interactive_widget", "id": "widget_m_kelly_criterion", "title": "…"},
    {"type": "quiz", "questions": [ … ] }
  ],
  "quiz_normalization": "in-place"   // исторический баг: квиз в blocks vs lesson.quiz — нормализуется сборкой
}
```

- Типы блоков: существующие 77 + 2 новых: `interactive_widget` (прямой вызов `EXPERT_WIDGETS[id]`)
  и `interactive_engine` (двигатель + инстанс-JSON). **Это и есть механическое решение проблемы
  4 механизмов привязки** — все старые механизмы конвертируются сборщиком в эти два типа.
- Конвертация текстов из `content/01-lesson-texts/` → JSON: по карте §6.3 плана (правила переноса
  заголовков/таблиц/врезок в блоки). Один прогон скрипта, результат — ревью дифом.

### 4.2. Виджет (уже канонизирован ТЗ-2)

`window.EXPERT_WIDGETS[id] = function(box)` · vanilla JS · seeded RNG (mulberry32) ·
`box._expTimers` (чистота повторного рендера) · адаптив 360px · без fetch/alert. Приёмка —
`tools/expert-widget-harness.html`. Реализации из `content/02-interactives/widgets/` (48) — эталон стиля.

### 4.3. Двигатель (новое, из спек ТЗ-3)

```
content/02-interactives/engines/e1-terminal/
  engine.js            # ядро: window.ENGINES.e1 = { mount(box, instance, ctx) }
  content contract     # инстансы — ТОЛЬКО JSON в content/data/e1/*.json (Приложение А спеки)
```

- Контекст `ctx` (общий для всех двигателей): `glossary` (Lexicon), `journal` (E5 API),
  `telemetry.onEvent`, `rng(seed)`, `lang` (LangSwitch), `save/load state`.
- Инстанс-JSON — точно по моделям данных спек: `scenario.json` (E1 §5.1), `profile.json` (E2 §3.1),
  кейс E6 (§5), фикстуры FT-15 (§2.6), FT-19(2) (§1.11), FT-20 (§2.12), пресеты E7 §10 / E8 §11.
- **Линтер контента в CI (обязателен, Приложение А п.5):** пустые `ru_gutter` без `skip_gutter`,
  термины без `ru_card`, метрики без `formula_ru`/`zones`, валидаторы без `why_card`, сценарии без
  `misconception`/`takeaway` → контент с ошибками не деплоится.

### 4.4. Банки вопросов

Формат уже канонизирован ТЗ-1 + `quiz_lint.py` (0 BLOCK — достижимо, доказано батчем 3).
Новые банки из `content/03-quizzes/` подключаются в `manifest/app.manifest.json`.

### 4.5. Числа и канон курса

Константы курса (55% / R 1.3 / риск 1% / серия 7–8 / $1000 / ED <10/10–25/>25 / 8 недель И ≥50 сделок /
8–10 месяцев / 40/40/20-развод) — в `manifest/course-canon.json`; линтер контента сверяет числа
двигателей с каноном (спеки это прямо требуют: «числа берутся из текста урока»).

---

## 5. Конвейер сборки

```
build.mjs
  ├─ 1. Читает manifest/app.manifest.json (списки уроков/виджетов/двигателей/данных/банков)
  ├─ 2. Собирает LESSONS_* из content/lessons.json/ (конвертация канона → рантайм-массивы)
  ├─ 3. Конкатенирует engine/core → engine/widgets-legacy → engines (E1..E8, порядок из
  │     engines.manifest.json: Glossary/TouchToKnow/LangSwitch/Journal → E1 → E2 → мост → E3..E8)
  ├─ 4. Инлайнит content/data/*.json + widgets/*.js (EXPERT_WIDGETS) + банки
  ├─ 5. Вставляет всё в engine/shell.html в точки-маркеры
  ├─ 6. Прогоняет lint-content.mjs + quiz_lint.py (0 BLOCK) → иначе сборка падает
  └─ 7. Пишет dist/index_v16.html + dist/build.json (хэши, версии, счётчики)
```

- **Никаких бандлеров-трансформаций на старте** (esbuild опционально позже): чистая конкатенация
  сохраняет офлайн-однофайловость и нулевой порог отладки. Маркеры-точки вставки — те же `<script>`
  блоки №1..№40+, что в v15.1 (адресация по маркерам, не по номерам строк).
- **Один источник — два таргета** (см. §8): офлайн-файл и SaaS-паки собираются из одного
  `content/` + `manifest/`, «перенос в платный SaaS» становится опцией сборки.

### 5.1. Диспетчер — единая точка рендера интерактивов

Заменяет 6 параллельных точек (auto-render forEach в renderLessonDetail, ATT-массивы, CN_W2-attach,
attachMathWidgets const MAP, patchMathIntegration-обёртку, window.mountMathWidget):

```js
// engine/core/dispatch.js — ЕДИНСТВЕННЫЙ механизм
function renderInteractive(box, block, lessonId) {
  if (block.type === 'interactive_widget') {
    const w = window.EXPERT_WIDGETS[block.id] || window.MATH_WIDGETS[block.id];
    if (w) return w(box);
  }
  if (block.type === 'interactive_engine') {
    const e = window.ENGINES[block.engine];
    const instance = CONTENT_DATA[block.engine][block.instance];
    if (e && instance) return e.mount(box, instance, engineCtx(lessonId));
  }
  // fallback на legacy-рендер по типу блока (постепенно пустеет)
  return legacyRender(box, block);
}
```

T-4.7 (диспатчер для 48 виджетов ТЗ-2) и T-4.11 (двигатели) реализуются ЭТИМ механизмом —
в v16 не нужен отдельный патч приложения, диспатчер встроен в ядро.

---

## 6. Golden-паритет с v15.1 (защитная сетка пересборки)

**Принцип: «переезд» и «улучшение» — никогда в одном шаге. Паритет рендера — доказательство
безопасности фазы 1.**

1. **Эталоны:** agent-browser (headless) прогоняет v15.1: все 225 уроков открываются по id →
   DOM-снапшот каждого `lesson-detail` (нормализованный: без timestamp/анимаций) → `build/golden/v15.1/*.json`.
2. **Проверка:** тот же прогон на `dist/index_v16.html` → диф по каждому уроку.
   - Фаза 1 (чистый переезд): цель — **100% паритет** (допустимые отличия — только whitespace).
   - Фаза 2 (замена кода ТЗ-2 на виджеты эксперта): диф ОЖИДАЕМ в 48 виджет-блоках — паритет
     проверяется по остальным ~208 урокам + smoke нового виджета в харнессе.
   - Фаза 3 (двигатели в пустых уроках FT/FAI): диф ожидаем в 26 уроках (было «— ПУСТО»).
3. **Регресс-ликст** (уже существует в плане §8.2): вкладки, поиск, прогресс, помодоро, RAG-ментор,
   Граф знаний — smoke-скрипт по списку экранов.
4. **Карта покрытия перегенерируется** после каждой фазы (`scripts/build_lesson_widget_map.py`):
   статусы ✓/⟳/·/✍ мигрируют в «собрано»; карта остаётся источником истины покрытия.

Зерно инфраструктуры уже есть: `tools/verify_app_browser.mjs`, `tools/verify_quests_browser.mjs`,
smoke-харнесс виджетов, `agent-browser` CLI.

---

## 7. Интеграционные потоки (как вливать поставки эксперта дальше)

| Поставка | Куда ложится | Что делает агент |
|---|---|---|
| Тексты уроков (fable8+…) | `content/01-lesson-texts/<module>.md` + контент-пак (раздел N, дословно) | конвертация в `lessons.json/<id>.json` по §6.3 плана, ревью дифом |
| Спеки интерактивов (tz3-формат) | `content/02-interactives/tz3-specs/` или per-lesson файл | оценка → создание задачи реализации → двигатели/виджеты |
| Код виджетов (ТЗ-2-формат) | `content/02-interactives/widgets/W-XX__*.js` | node --check + харнесс + строка в manifest → сборка |
| Банки вопросов | `content/03-quizzes/*.md` | quiz_lint (0 BLOCK) → manifest |
| Инстансы-JSON двигателей | `content/data/<engine>/*.json` | lint-content.mjs → manifest |
| **Карта трека (этап 1: fable_STOCK-MARKET-RU / fable_LIVING-OFF-CAPITAL)** | `content/05-electives/<track>-track.md` + пак | решение открытых вопросов → ТЗ этапа 2 → ожидание текстов/спек → сборка трека (T-4.15/T-4.16) |
| **Аудит подсистемы (fable_kriptik-формат)** | `content/06-mentor/` + пак | разнести карточки на «движок» (реализация) и «контент» (заказ эксперту); атом урока → схема фазы 0 |

**С этого момента ни одна поставка не требует хирургии на 8 МБ** — всё ложится файлами в `content/`
и подхватывается manifest'ом. Дуэль «git push» решается merge'ом файлов, а не 8-мегабайтного blob'а.

> **Волна 9 (2026-09-09) прошла по этим потокам целиком:** fable8 → 01-lesson-texts + карточки в 02-interactives
> (пак — раздел 12); fable_kriptik → 06-mentor (раздел 13); карты треков → 05-electives (разделы 14–15).
> Заказа этапа 2 (`V15.1-EXPERT-TZ-ETAP2-ELECTIVES.md`) не ждёт ни одной сборочной задачи — конвейер
> продолжает строиться по дорожной карте §9.

---

## 8. Сходимость с SaaS (`saas/`, `PROJECT_SAAS_CLOUDFLARE.md`)

1. `saas/tools/extract-content.mjs` переключается с `../index.html` (v9!) на `content/lessons.json/`
   + `manifest/saas.manifest.json` — **отставание паков на 47 уроков исчезает как класс**.
2. Нарезка паков: `core_demo` (Ф0+психология — бесплатно) / `core_p0…p8` (платно по фазам) / `terms` /
   `tests` / `earning` — сохраняется, но теперь сигнализируется манифестом, а не regex-вырезанием
   из HTML. Brotli-бюджет ≤150KB/пак (§21.1) проверяется сборкой.
3. `content/data/` (инстансы двигателей) + `content/02-interactives/engines/` → отдельные паки
   `engines` и `data` (догрузка по требованию, гейтинг по тарифу).
4. `public/integration.js` (auth/синк) не меняется — аддитивный слой, продукт-файл не трогаем,
   т.к. dist собирается тем же конвейером.

---

## 9. Дорожная карта

### Фаза 0 — Канон (2–3 дня)
- [ ] Зафиксировать схемы §4 (lesson JSON, engine contract, инстансы, manifest) в `manifest/schemas/`
- [ ] `course-canon.json` (константы курса) — извлечь из плана §7.2
- [ ] Решить открытые вопросы спек: языковой слой §11 (4 шт.), FT-16 §5 (4 шт.), FT-01 §10, FT-02 §11
- [x] ~~Заказать эксперту~~ **ЭКСПЕРТ РАБОТУ ЗАКОНЧИЛ (волна 10, 2026-09-09: ТЗ-10/ТЗ-11 закрыты — все тексты и все спеки обоих треков)**; R8/R9, транш 2 W-E (~21 урок), дозаполнения и ответы на ~69 открытых вопросов волны 10 — больше не заказываются: **авторская доработка наша, финальный список — `V16-FINAL-CONTENT-GAPS.md`, задачи T-4.17/T-4.18**
- [ ] **KR-04: включить «атом урока» в канон схемы §4** (theses/analogy/trap/numbers/terms/tasks/socratic/normalize/difficulty — из Приложения B fable_kriptik.md) — один источник для наставника и урока

### Фаза 1 — Конвейер + паритет (1–2 недели, нулевое изменение логики)
- [ ] `engine/` — перенос скриптов из v15.1 как есть; `shell.html` с маркерами вставки
- [ ] `build.mjs` → первый `dist/index_v16.html`
- [ ] Конвертация `content/01-lesson-texts/` → `content/lessons.json/` (225 + 17 ждущих сборки)
- [ ] Golden-эталоны v15.1 (§6) → **паритет 225/225**
- [ ] Диспатчер §5.1 в ядре; legacy-виджеты и attachMathWidgets читаются через него
- [ ] `saas/`-миграция на общий источник (§8) — опционально сразу или фазой 1.5

### Фаза 2 — Долговая чистка под паритетом (постепенно, по одному пункту за коммит)
- [ ] Снос заплаточных слоёв (patchMathIntegration → диспатчер; дубль mountMathWidget)
- [ ] Встройка 48 виджетов ТЗ-2 (T-4.7): manifest-строки + харнесс-приёмка; старый код — в git-истории
- [ ] Сборка 131 текста (T-4.5/4.6/4.9) — фазированно по волнам плана §7.1
- [ ] Интеграция 603 позиций банков (E1-линтер, 0 BLOCK — уже доказано достижимо)
- [ ] Унификация нормализации квизов (исторический баг «квиз в blocks») — в конвертере, один раз

### Фаза 3 — Двигатели и новые интерактивы (волны W-A…W-D из ТЗ-3)
- [ ] **W-A (скелет):** Glossary + TouchToKnow + LangSwitch + Journal hook → E1 (контент `ft02_install`
      + «Диагност») → E2 + мост → линтер контента в CI. E5-ядро («Три строки» + гейт двух вопросов).
- [ ] **W-B (уроки с ценой ошибки):** FT-04/05/06/11/13/16/17/19/20 — инстансы из спек
- [ ] **W-C:** каталог E4 получен (файл 23: R1–R7/R10/R12 + 7 новых как JSON-конфиги) — реализовать ядро E4 (Guard API, Clock, ворота) и подключить шаблоны; R8/R9 — после дозаказа
- [ ] **W-D:** остальные FT/FAI + E6 (банк 34 кейса) + E7/E8/E3 во всех инстансах + языковой слой L-0…L-5
- [ ] **W-E:** точечное усиление «одиночных» уроков — транш 1 ПОЛУЧЕН (2026-09-07, файлы 24–40: 34 спеки, 38 уроков) + **продолжение ПОЛУЧЕНО 2026-09-08 (файлы 41–47: 17 спек — все 14 М-уроков волны, П20/П22/П23/П26/П30/П48–П49, ВК3/ВК4; 61 из 94 закрыты)**; транш 2 (~33 урока: М1/М3/М5–М15/М17–М22/М26/М29/М31/М33–М37/М41/М43–М48, П17/П18/П25/П28/П32/П33/П36/П38/П43/П46/П52) — ждать/дозаказать
- [ ] После каждой волны: перегенерация карты (✍ → «собрано»), golden-диф только в ожидаемых уроках

### Фаза 3.5 — Факультативы нового поколения (ЭТАП 2 ПОЛУЧЕН ПОЛНОСТЬЮ 2026-09-09 — источники готовы)
- [ ] **Трек «Фондовый рынок РФ» (T-4.15):** тексты всех 23 уроков — `content/05-electives/smru-lessons.md`; спеки всех 23 интерактивов — `content/02-interactives/electives-specs/` файлы 01–11; КП-1/КП-2/КП-3/КП-ОВ — банки частично в спеках, полные — наша доработка (T-4.17); входной тест — T-4.17; гейтинг после 5.7/FT-20; ветка О — «второй год». 6 РФ-ритуалов — конфиги E4 (концепты в уроках, конфиги наши); глоссарий — в общий Lexicon
- [ ] **Трек «Контур жизни» (T-4.16):** тексты 14 уроков + бонусы Ж10+/Ж7+ — `content/05-electives/loc-lessons.md`; спеки ЖИ-1…ЖИ-22 + Ж7+ — electives-specs файлы 12–22; ЧП1–3 интегрированы в спеки; «Семейный устав оператора» — движок 5.6 (эталон заполнения — наша доработка, T-4.17); общий движок «пошаговая симуляция с событиями и двумя шкалами» (ЖИ-4/6/15)
- [ ] **Код всех 49+ интерактивов треков — наша реализация** (эксперт закончил; спеки с моделями расчёта и золотыми числами достаточны для передачи агенту-реализатору по контракту ТЗ-2)
- [ ] **Криптик P0 (T-4.13):** шлюз качества, бан-лист, лестница реакций, экраны KR-19/20/21, два регистра, ночной режим — поверх атомов фазы 0; контент-часть (реплики/строки/лестницы) — от эксперта
- [ ] Оба трека — после микро-лайва 5.1 (Ж — гейтинг; РФ — после 5.7/FT-20) — не раньше сборки маршрута; в manifest как отдельные паки `elective_ru` / `elective_life` (догрузка по тарифу)

### Definition of Done v16
- [ ] `dist/index_v16.html` собирается одной командой `node build/build.mjs`; размер ≤ v15.1 + 10%
- [ ] Golden-паритет: 225/225 уроков; регресс-лист вкладок — 0 ошибок
- [ ] Карта покрытия: 0 «— ПУСТО» уроков; FT/FAI 26/26 со встроенными интерактивами
- [ ] quiz_lint 0 BLOCK по всем банкам (старым + 603 новым); lint-content 0 ошибок
- [ ] saas-паки собраны из общего источника (не из HTML), дифф-версионирование работает
- [ ] Ни одной правки `engine/` без строки в worklog/коммите «фаза 2, пункт N» (правило одной правки)

---

## 10. Риски и ограничения

| Риск | Митигирование |
|---|---|
| Конкатенация сломает порядок инициализации | engines.manifest.json с зависимостями из Приложения А спеки E1/E2; smoke инициализации в golden-прогоне |
| Конвертация текстов в JSON внесёт тихие искажения | тексты берутся ДОСЛОВНО из контент-пака (дословность проверена контрольными фразами); диф конвертации ревьюится по-модульно |
| Golden-снапшоты хрупки (анимации/таймеры) | нормализация снапшотов (вырезаем volatile-атрибуты); сравнение структурное, не побайтовое |
| Объём работы по 88 интерактивам + 8 двигателей | двигатели покрывают ~70% механики; инстансы — чистый JSON (методологи правят без разработчика); приоритизация волн W-A→W-E; 8/8 двигателей и 88/88 спек уже в репо (tz3-specs/, файлы 01–47) |
| Двойная работа engine/widgets-legacy и EXPERT_WIDGETS | фаза 2 постепенно переносит 48 замен ТЗ-2 + 40 legacy в `content/02-interactives/widgets/`; widgets-legacy пустеет |
| ~~Эксперт поставляет E4/R3–R12/W-E позже~~ | **поставлено (2026-09-06/07/08):** E4 + 9/12 ритуалов + W-E транш 1 и его продолжение (М/П/ВК) — волны W-A…W-E стартуют без ожидания; R8/R9 и транш 2 W-E (~33) — опциональные дополнения |

---

## 11. Почему это дешевле и быстрее обоих отвергнутых вариантов

- **Против «шампура»:** каждая из ~235 предстоящих единиц работы (131 текст + 48 виджетов + 71
  интерактив + 603 позиции + 8 двигателей) ложится файлом в `content/` и строкой в manifest —
  вместо операции на 8 МБ. Очередь T-4.7/T-4.9/T-4.11 превращается из «хирургии» в конвейер.
- **Против «с нуля»:** движок переносится как есть с паритет-доказательством (225/225); риски
  фазы 1 ≈ 0 (логика не меняется), фазы 2 — ниже любой текущей ручной правки монолита, потому что
  впервые под автотестами.
- **Бонус:** параллельная работа (агенты/эксперт/мы) без конфликтов; SaaS-миграция из «проекта»
  становится опцией сборки; исторические файлы v5.1…v15.1 остаются эталонами golden-тестов.

---

## 12. Живой слой: дуальные режимы «Наблюдение / Обучение» — одни данные, две роли

> **Решение владельца (2026-09-08):** эксперт предложил превратить вкладку «Живой рынок» в обучающий инструмент
> (упражнения, ритуал визита, журнал — аудита ТЗ-4 + Г.1–Г.10). Принято, но с одной поправкой: **приложением
> пользуются и ПОСЛЕ прохождения курса — по подписке, для наблюдения за рынком с наставником.** Поэтому слой
> получает **два режима (две субвкладки) на одних и тех же данных**, а не одну «обученную» вкладку.
>
> **Дополнение 2026-09-09 (fable_kriptik, ТЗ-6):** дуальность режимов распространяется и на наставника —
> карточки KR-27 «два регистра» (Инструктор/Дежурный) и KR-21 «ночной режим» работают в обеих субвкладках:
> в «Наблюдении» Криптик всегда в регистре «Дежурный» (без иронии, только протоколы), в «Обучении» —
> полноценный Инструктор с лестницей реакций KR-12. Реализация — T-4.13.

### 12.1. Принцип: один слой данных — две проекции

```
                    ┌─────────────────────────────────────────┐
                    │  DATA LAYER (общий, без дублирования)    │
                    │  6 источников · кэш снимков · детекторы  │
                    │  (12+18) · классификатор новостей 7 типов│
                    │  паспорт рынка · сценарные дни (сим)     │
                    └───────────────┬─────────────────────────┘
                                    │ единый поток наблюдений
                    ┌───────────────┴───────────────┐
                    ▼                               ▼
        ┌───────────────────────┐       ┌───────────────────────────┐
        │  РЕЖИМ «НАБЛЮДЕНИЕ»   │       │  РЕЖИМ «ОБУЧЕНИЕ»          │
        │  (как сейчас)         │       │  (видение эксперта,        │
        │  информирующая роль   │       │  обучающая роль)           │
        └───────────────────────┘       └───────────────────────────┘
```

- **Data layer один**: источники, снимки, детекторы, классификатор новостей, паспорт рынка (Г.1–Г.2) —
  общий модуль `engine/live/`. Ни один запрос к сети и ни один детектор не дублируются между режимами.
- Режимы отличаются только **поведенческим слоем**: что показывается, что спрашивается, что пишется в профиль.

### 12.2. Режим «Наблюдение» (информирующая роль — текущее поведение)

Для выпускников курса и подписчиков «после обучения»:

- Панели источников + поток карточек детекторов + лента новостей — как в v15.1.
- Карточка А→Б→В («где смотреть → что это значит → чего это НЕ значит») — остаётся: это язык наставника.
- **Без** упражнений, гейтов, FSRS, ритуала визита, таймера внимания и журнала обязательных записей —
  всё это учебные механики. Тихое обновление (дельта-триггер Г.1 + «с прошлого визита изменилось: 2 из 6») — да:
  он снимает «игровой автомат» и нужен обоим режимам.
- Дельта-триггер и паспорт рынка — общие с «Обучением» (это свойства data layer).
- Настройка «интервал опроса» (для тарифов) и экспорт карточек CSV.

### 12.3. Режим «Обучение» (обучающая роль — видение эксперта из ТЗ-4)

Для проходящих курс (вход по умолчанию, пока курс не завершён):

- **Ритуал визита** Г.9: 5-шаговый чек-лист (паспорт → карточка → упражнение из очереди → строка в журнал → выход),
  «визит засчитан», недельная карточка Г.10.
- **Упражнения** Б.1–Б.8 на живых данных («12 BTC прямо сейчас», «Завтрашний индекс» — честная калибровка
  факт-прогнозов Б.7/Г.5, «найди в ленте» В.4, генеративный «Анти-FUD»).
- **FSRS-ротация** Г.4 упражнений и «семейств ошибок»; **матрица слепых зон** Г.3 словами («ты путаешь Р3 с Р2 —
  7 из 12»); **сценарные дни** Г.8 в учебном генераторе (7 подписанных сценариев — редкие детекторы
  гарантированно проживаются).
- **Журнал живого рынка** Г.10 + экспорт CSV (мост к Py-05/Py-08) + вклад в Process Score (П33) и
  слабые зоны Криптика (ТЗ-6).
- Гейты контента (лента после 2.1/2.2, «Анти-FUD» после 0.18) — только здесь.
- Таймер внимания (П5/П6, 15 мин → карточка 🧘) — общий на обе субвкладки, но «мягкий» в «Наблюдении».

### 12.4. Что делится между режимами (и почему это дёшево)

| Слой | Разделение |
|---|---|
| Источники, кэш, детекторы, классификатор | **общие** (один код, одни данные) |
| Паспорт рынка, дельта-триггер, тихое обновление | **общие** (Г.1–Г.2 — анти-дофаминовые базовые) |
| Карточка А→Б→В | **общая** (язык наставника в обоих режимах) |
| Упражнения, FSRS, ритуал, журнал, гейты, Brier | только «Обучение» |
| Телеметрия | раздельные префиксы событий `live.obs.*` / `live.edu.*` — чтобы метрики режимов не смешивались |
| Прогресс/профиль | «Наблюдение» пишет только пассивное (визиты); «Обучение» — активное (упражнения, Brier, слепые зоны) |

### 12.5. Правила реализации

1. **Дефолт режима по стадии ученика:** идёт курс → открывается «Обучение»; курс завершён (сертификат) →
   дефолт «Наблюдение», переключение вручную запоминается. Никаких платных стен между режимами —
   оба входят в тарифы, где есть живой слой (Про/Макс), различие только в поведении.
2. **Один и тот же детектор → разное оформление:** в «Наблюдении» карточка с полным А→Б→В сразу;
   в «Обучении» — карточка может стать вопросом упражнения («что это говорит о цене?» — Б.2) и попасть в FSRS.
3. **Сценарные дни (сим-генератор)** — атрибут data layer: «Наблюдение» может их включить вручную
   («учебные данные»), но по умолчанию живой поток.
4. **Кодовая структура v16:** `engine/live/sources.js` + `engine/live/detectors.js` + `engine/live/news.js`
   (data layer) · `engine/live/modes/observe.js` + `engine/live/modes/learn.js` (поведенческие слои) ·
   инстансы упражнений — `content/data/live/*.json`. Режимы — чистые подписчики на общий поток наблюдений
   (pub/sub), никакой логики «если режим, то другой детектор».
5. **SaaS-сходимость:** оба режима едут в паке `live` (§8 п.3) без изменения тарифной сетки; режим —
   поле профиля, синхронизируется между устройствами.

### 12.6. Что это НЕ

- Не «две вкладки с разными данными» — данных один набор, включая учебные снимки.
- Не платное разделение: «Обучение» не тариф, а стадия использования.
- Не замена текущей вкладки: текущее поведение v15.1 сохраняется как режим «Наблюдение»
  (наследование без потерь, как с уроками в golden-паритете).

*Архитектура v16 · составлено 2026-09-06, актуализовано 2026-09-08 (порции 2–3 спек ТЗ-3 + спека лендинга + режимы «Наблюдение/Обучение») · базируется
на решении владельца (обсуждение «шампур или с нуля»), инвентаризации контента на 2026-09-08 (225
уроков / 256 интерактивов / 48 виджетов ТЗ-2 / 88 спек ТЗ-3 в трёх порциях / 8 двигателей / 603 позиции
банков) и спеках `content/02-interactives/tz3-specs/` (47 файлов) + `content/04-landing/` (спека лендинга, T-4.12).
Первая контрольная точка: конец фазы 1 — `dist/index_v16.html` с паритетом 225/225.*
Выданы эксперту 2026-09-08: ТЗ-6 (AI-наставник Криптик), ТЗ-7 (переписывание сложнейших математических уроков), ТЗ-8 (фондовый рынок РФ), ТЗ-9 (жизнь с капитала).*

---

## 13. ADR-001: отказ от «всё в одном файле» — двухфазный план (конвейер → Vite SPA)

> **Статус: ПРИНЯТО (2026-09-09, решение владельца после закрытия контент-цикла).**
> Контекст: эксперт работу закончил — контентная база полна (225 уроков основного маршрута + 39 уроков
> факультативов + 603 позиции банков + ~157 спек интерактивов + 48 готовых виджетов + лендинг).
> Впереди — многолетняя жизнь продукта: сборка, доработка, SaaS-деплой. Вопрос: продолжать ли
> настаивать на single-file артефакте. **Вывод: нет.** Single-file был страховкой эпохи «монолит =
> единственный носитель», теперь он — тормоз сборки, деплоя и качества. Ниже — анализ вариантов,
> решение и план миграции.

### 13.1. Что изменилось (почему вопрос возник именно сейчас)

1. **Контент-цикл закрыт.** Все будущие правки — наши и агентов; им нужен нормальный DX
   (правка одного файла урока → сборка → диф), а не хирургия в 7.5 МБ монолите.
2. **SaaS — основной таргет.** Пейволл, тарифы, dogрузка паков, обновления без релизов
   «весь файл целиком», CDN-кэширование по чанкам — монолит обнуляет каждое из этих преимуществ.
3. **«Красота как в компьютерной игре»** (запрос владельца): 60 FPS анимации, переходы
   экранов, физика графиков — требуют маленького main-bundle и предзагрузки только нужного
   экрана; в монолите старт парсит 8 МБ JS до первого кадра.
4. **Инструментальная база готова.** 47 файлов спек + lessons.json-канон (§4.1) + манифесты
   (§3) — структура «источник → сборка» уже описана этим документом; остаётся выбрать рантайм-таргет.

### 13.2. Варианты (анализ)

| # | Вариант | Плюсы | Минусы | Вердикт |
|---|---|---|---|---|
| A | **Монолит навсегда** (v15.1 как вечный формат) | офлайн-копия одним файлом; нулевая инфраструктура; «отправил файл = отправил курс» | деплой = перезаливка 8+ МБ; кэш браузера бесполезен; правка урока = регенерация всего; CI-диф нечитаем; HTML/JS парсинг блокирует первый рендер; SaaS-функции прикрутить можно, но криво; конфликты пуша (дважды случились) | ✗ отвергнуто |
| B | **Конвейер + монолит-артефакт** (Фаза 1 = terminal: build.mjs → dist/index_v16.html) | сохраняет офлайн-свойство A; конвейер уже спроектирован (§3–§5); golden-паритет прост (тот же рантайм) | не решает ни один пункт 13.1 №2–3; сборка-и-деплой всё ещё целиком; со временем всё равно упрёмся | **как ЭТАП, не как терминал** |
| C | **B → затем Vite SPA** (двухфазный; предложено владельцем) | 100% golden-паритет на переходе (Фаза 1 = страховочная сетка); Vite-слой наращивается поверх ТОЙ ЖЕ структуры content/engine — без второй пересборки; TypeScript и чанки приходят по готовности; откат на Фазу 1 возможен в любой момент | два таргета сборки какое-то время поддерживать; соблазн «улучшить при переезде» (запрещено правилом §13.4) | **✓ ПРИНЯТО** |
| D | **Сразу Vite SPA** (перепрыгнуть Фазу 1) | один таргет с первого дня | теряем golden-паритет как сетку: конвертация 225 уроков + 77 блоков + 256 интерактивов в новый рантайм одним махом = недетектируемые регресы; риск повторить «с нуля» из §1 | ✗ отвергнуто |
| E | **SSR-фреймворк (Next/Nuxt)** | SEO из коробки | курс — приложение, не сайт; SEO нужен только лендингу (он уже отдельная спека, T-4.12); SSR-оверхед на 225 интерактивных экранов не окупается; hydrate-бандл съедает выгу | ✗ отвергнуто (SSR только лендингу, опционально SSG) |

### 13.3. Решение

**Фаза 1 (B) — «разрезать и склеить», с golden-паритетом 225/225 как выходным критерием.**
Монолит разбирается в `engine/ + content/ + manifest/`; `build/build.mjs` склеивает
`dist/index_v16.html`. Ноль изменения логики. **`dist/index_v16.html` остаётся в строю
как (а) эталон golden-прогонов, (б) офлайн-артефакт для дистрибуции одним файлом**
(это свойство дёшево — побочный продукт конвейера, а не цель).

**Фаза 2 (C) — Vite как второй таргет ТОГО ЖЕ источника, затем основной деплой-таргет SaaS.**
`vite.config.ts` читает те же `content/lessons.json/`, `engine/` (обёртки-модули), манифесты.
Продукт получает: code-splitting по урокам/фазам (первый экран < 300 КБ gzip против ~8 МБ),
ленивые чанки паков по тарифу, TS-проверку контрактов спек (§4 канон = TS-типы), HMR для DX,
PWA-офлайн вместо «одного файла», CDN-кэш, preview-деплои на каждый PR.
**Не «поверх», а «рядом»: build.mjs не удаляется** — оффлайн-артефакт и golden-прогоны
продолжают собираться им; Vite-билд и build.mjs-билд сходятся к одному source of truth.

### 13.4. Железные правила миграции

1. **Переезд и улучшение — никогда одновременно.** Фаза 1 не меняет ни одну строку логики;
   любое улучшение (TS, рефактор виджета, анимации) — отдельным коммитом фазы 2+.
2. **Один source of truth.** Запрещены «vite-копии» файлов: только алиасы/обёртки к `content/`
   и `engine/`. Появился дубль — это дефект сборки, ловится диф-прогоном.
3. **Golden-паритет живёт дольше Фазы 1.** Прогон v15.1 ↔ v16 (build.mjs) ↔ v16 (vite)
   сравнивает все 225+39 уроков; паритет vite-таргета с build.mjs-таргетом — выходной
   критерий Фазы 2 (допускаются только документированные диффы: чанковая подгрузка).
4. **TS постепенно, слоями:** (1) `manifest/schemas/*.ts` — типы канона §4 (манифесты/уроки/инстансы
   двигателей) с `ajv`-валидацией в CI; (2) JSDoc `@ts-check` в engine/core; (3) полный TS только для
   нового кода (двигатели E1–E8, виджеты треков). Легаси-код НЕ переписываем на TS ради TS.
5. **60 FPS — бюджетом, не пожеланием.** Каждый новый интерактив треков/двигателей:
   анимации только `transform/opacity` (+ `will-change` точечно), canvas/SVG для плотных
   графиков, `requestAnimationFrame`-циклы с одним лупом на экран, `prefers-reduced-motion`
   уважаем. CI-проверка бюджета бандла на урок (лимит КБ) с первой волны W-A.

### 13.5. План Фазы 2 (после закрытия Фазы 1; оценка 1.5–2.5 недели)

- [ ] **P2.1.** Каркас `vite/` (или `vite.config.ts` в корне): алиасы `@engine`, `@content`,
      плагин `content-loader` (manifest → import-map уроков), роутер экранов — 2 экрана.
- [ ] **P2.2.** Таргет сборки тех же 225 уроков; code-splitting: `core` (движок+диспатчер),
      `p0…p8`, `math`, `psy`, `ft`, `fai`, `elective_ru`, `elective_life`, `engines`, `data`.
      Ленивая догрузка пак-чанков по гейтингу (тариф/уровень допуска T-9.1).
- [ ] **P2.3.** `manifest/schemas/*.ts` + ajv-валидация в CI (типы канона §4 — урок, виджет,
      двигатель, инстанс, банк вопросов, ритуал E4, атом KR-04).
- [ ] **P2.4.** Диспатчер §5.1 и `EXPERT_WIDGETS` — реальные ES-модули (вместо глобального
      объекта — реестр с `import()` по требованию); 48+49 виджетов — динамические чанки.
- [ ] **P2.5.** PWA: service worker (stale-while-revalidate для чанков, network-first для
      auth/синка), manifest, установка на телефон — офлайн-режим без single-file.
- [ ] **P2.6.** Деплой: Cloudflare Pages (или аналог) — preview на PR, прод-окружение,
      `saas/`-интеграция (auth/синк — существующий `integration.js`).
- [ ] **P2.7.** Golden-прогон третьим участником: v15.1 ↔ build.mjs ↔ vite (трижды-паритет);
      выходим в прод только при диффах из белого списка.
- [ ] **P2.8.** Лендинг (T-4.12) — отдельный вход/бандл по спеке `content/04-landing/`
      (SSG-прешейн, SEO/JSON-LD из спеки); в SPA-роутер не смешиваем.

### 13.6. Что получает владелец (заметки для продукта)

- **Скорость первых секунд:** первый экран курса грузится из ~200–300 КБ gzip, а не 8 МБ;
  остальные фазы — по мере прогресса ученика. Это и есть «ощущение игры»: экран открывается мгновенно.
- **Деплой без страха:** PR → preview-URL → golden-прогон → прод; откат = редеплой прошлого коммита.
- **Доработка контента = правка одного JSON-урока**, а не 8 МБ файла; агенты работают по файлам
  спек без конфликтов (боли «двух push-конфликтов» больше нет).
- **SaaS-механика дёшево:** паки по тарифам, эксперименты A/B на отдельных чанках, кэш CDN.
- **Офлайн не теряем:** PWA + (по желанию) артефакт `dist/index_v16.html` для «флешки».

### 13.7. Риски фазы 2 и митигации

| Риск | Митигация |
|---|---|
| Расползание «vite-версии» от build.mjs-версии | правило 13.4-2 (один источник) + трижды-паритетный прогон в CI |
| Service worker кэширует устаревшие чанки | версия в именах чанков + SW-update flow («доступна новая версия») |
| Слишком ранний TS-большевизм | правило 13.4-4: слои, легаси не трогаем |
| Анимации съедают батарею | `prefers-reduced-motion`, пауза лупа при `visibilitychange` |
| Vite-зависимость (среда сборки) | build.mjs остаётся автономным (node без npm-инсталла) для офлайн-артефакта |

---

*ADR-001 фиксирует решение 2026-09-09. Фаза 1 стартует немедленно (§9), Фаза 2 — по закрытию
паритета 225/225. Изменение решения — только новым ADR с ретро-анализом.*
