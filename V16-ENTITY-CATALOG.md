# V16 — Каталог переиспользуемых сущностей (T-4.19, задача C промта 05)

> **Назначение:** анти-дот к «изолированному коду» агентов. Всё, что УЖЕ существует и что
> переиспользовать проще, чем создать заново. **Правило дубля (00-CONTEXT §5.9):** прежде чем
> создать файл/сущность/движок/компонент — сверься с этим каталогом; дублирование = дефект
> работы (RETURN на ревью).
>
> **Синхронизация:** при каждом обновлении читаю `content/02-interactives/widgets/_registry.json`
> (агент 16; сейчас файл ещё не создан — заведён при старте Т-4.11) — новые ОБЩИЕ компоненты
> попадают в оба списка (§6 этого каталога + _registry.json). Обновление каталога — версионируется.
>
> **Формат строки:** `id` · где живёт · контракт использования (API/формат) · кто переиспользует ·
> как расширять (не форкая) · анти-пример (что НЕ надо создавать рядом).

---

## 1. Двигатели E1–E8 (спеки готовы; реализация — агент 16, контракт ENGINES)

| id | Где живёт (спека) | Контракт использования | Кто | Как расширять | Анти-пример |
|---|---|---|---|---|---|
| E1 «Терминал-переводчик» | tz3-specs/01 | `window.ENGINES.e1.mount(box, instance, ctx)`; сценарии — ТОЛЬКО JSON в `content/data/e1/*.json`; ctx: glossary/journal/telemetry/rng/lang/save-load | 16 (код), 04 (сборка), 15 (сценарии) | новые сценарии = новые JSON-инстансы, движок не трогаем | свой «терминал» в виджете FT-02 (уже снят: FT-02 выставляет требования к E1) |
| E2 «Живой конфиг» | tz3-specs/01 | профили `content/data/e2/*.json`; bridge E2↔E1; charter.yaml ↔ E4 (правила устава) | 16, 15 (профили), 01 (charter устава Ж) | профили и charter-правила данными; код конвейера один | парсить конфиг регулярками в каждом виджете |
| E3 «Плёнка бэктеста» | tz3-specs/13 | общий компонент; компонентная модель (допустима любая реактивная либа, контракт данных сохранить) | 16; потребители FT-05(2)/FT-06 | шаги плёнки — параметрами инстанса | каждый FT-урок рисует свою ленту сделок |
| E4 «Ритуал» | tz3-specs/23 | `RitualTemplate/Instance/Run/Step/Gate/Streak/Window/Guard/Clock`; каталог `rituals/catalog/*.json` (схема при сборке); RealClock/SimClock; правило полуночи; Guard API | 16 (движок), 01 (6 РФ-конфигов + R8/R9), 03 (ритуалы W-E) | новые ритуалы = новые шаблоны JSON (см. §2.1) | «свой мини-ритуал» с таймерами в коде виджета |
| E5 «Журнал» | tz3-specs/02 | `journal.*` API; IndexedDB; экспорт JSON/MD; гейт двух вопросов; holdout-счётчик | 16; все движки пишут события сюда | новые типы записей — через схему, не в обход | localStorage-«дневнички» в виджетах (были в v15.1 — дубли) |
| E6 «Охотник за утечкой» | tz3-specs/02 (банк 34 кейса, §6 спеки) | таксономия L/N-типов; форматы кейсов `code`/`tab`/`text`; режимы «Вердикт→Где→Тип→Почини→Цена»; пополнение: трек ≥3 кейса и ≥1 чистый, соотношение ≥3:1 | 16 (движок+банк), 17 (кейсы ВК3/М), 01 (кейсы Р) | банк `content/data/e6/*.json`; движок не трогаем | отдельные «найди утечку»-игры (ВК2 расширение идёт ЧЕРЕЗ E6 — уже так в спеке 39) |
| E7 «Линейка времени» | tz3-specs/03 | drag-границы Train/Valid/Test; счётчик открытий final test | 16; М42/FAI-03b идут на нём (спеки 44/06) | новые датасеты границ — JSON | свой слайдер периодов в М42 |
| E8 «Двуязычный отчёт» | tz3-specs/03 | словарь метрик; зоны (норма/серая/стоп); режимы guided/exits/compare/theory | 16; FT-09 — ядро report_metrics | новые метрики = записи словаря, зоны — из канона ED | форматировать метрики строками в виджете |

**Порядок сборки двигателей (Приложение А спеки 01):** Glossary/TouchToKnow/LangSwitch/
Journal hook → E1 → E2 → мост E2↔E1 → контент. Контент (сценарии/профили/кейсы) — в JSON
файлов контента, НЕ в коде компонентов; линтер контента в CI.

## 2. Общие механики и компоненты

### 2.1. «Ситуация-выбор» (решение №64 реестра)

- **Где:** концепт — `V16-DECISIONS-REGISTRY.md` §5 (№64), модель — сценарии smru/loc
  («**Ситуация-выбор.** …Что делает оператор?»), диспетчер — V16-ARCHITECTURE §5.1.
- **Контракт:** единый переиспользуемый компонент диалоговых сцен: вводная (1 абзац) →
  2–4 исхода → разбор каждого («почему кажется» + последствия) → артефакт в журнал.
  Рендер — ТОЛЬКО через диспатчер T-4.7 (это и есть причина, почему он собирается ПЕРВЫМ).
- **Кто:** 16 (компонент), 15/01 (сценарии КП и чекпоинтов), все треки.
- **Расширять:** новые сценарии = данные; механику не форкать.
- **Анти-пример:** в ЖИ-4/6/15 «свой» движок диалогов — спеки прямо требуют общий
  «пошаговая симуляция с событиями и двумя шкалами» (см. 2.2).

### 2.2. Общий движок «пошаговая симуляция с событиями и двумя шкалами»

- **Где:** electives-specs/README + GAPS Б.5: ЖИ-4, ЖИ-6, ЖИ-15 — один движок, написать
  ОДИН раз (события по шагам, две шкалы состояния, пороги).
- **Контракт:** шаги-события JSON; две шкалы (например, капитал/доверие семьи); пороги
  из golden-чисел спеки. **Кто:** 16. **Расширять:** ЖИ-5 и другие симуляции года —
  параметрами. **Анти-пример:** три разные реализации для ЖИ-4/6/15.

### 2.3. Конструктор 5.6 (ЖИ-17 «Семейный устав»)

- **Где:** спека ЖИ-17 (файл 20 electives-specs) + урок 5.6 (charter.yaml, E2).
- **Контракт:** поля устава = правила charter.yaml (единая модель с E2!); ЖФ-эталон —
  заполненный образец (GAPS Б.4). **Кто:** 16 (UI), 01 (эталон заполнения), 15 (поля КП).
- **Анти-пример:** второй формат «семейного устава» в PDF/тексте, рассинхронный с charter.

### 2.4. Паттерны ТЗ-2 (код виджетов: 48 файлов — эталон стиля кода)

- **Где:** `content/02-interactives/widgets/` + `expert-rebuild-contract.md` + README §Контракт.
- **Контракт (обязателен каждому новому виджету W-49+):**
  `window.EXPERT_WIDGETS[id] = function(box){…}`; vanilla JS ES2017; Canvas/SVG; CSS-переменные
  с фолбэками (`--txt,--mut,--acc2,--ok,--bad,--warn,--line,--mono`); детерминизм
  `mulberry32(seed)`, «новый раунд» — `Date.now()`; cleanup в первых строках
  (`box._expTimers/_expRaf/_expCleanup/_expRO`); адаптив ≥360px; без внешних зависимостей и
  `alert/confirm/prompt`; артефакт `box.dataset.artifact` + событие `expert:artifact`
  (bubbles; слушать все три имени: `expert:artifact`/`exp:artifact`/`expert-artifact`);
  60 FPS: анимации только transform/opacity, один rAF-луп, `prefers-reduced-motion`.
- **Кто:** 16 (весь новый код), 03 (виджеты В.2), 04 (диспетчер).
- **Расширять:** копия каркаса из ближайшего по типу W-файла; регистрация в _registry.json.
- **Анти-пример:** jQuery-подход, свои id-события артефактов, requestAnimationFrame-лупы
  без снятия, Math.random() вместо сид.

### 2.5. Общие слои виджетов (грузить ДО виджетов)

- `_shared-00-exp-lib.js` = `window.__EXP_LIB__` (mulberry32, gauss, цвета, setupBox,
  quantile — для W-11/W-12; guard-обёрнут).
- `_shared-01-expert-lib.js`, `_shared-02-exp-common.js` — см. README widgets §«Общие слои»
  (порядок загрузки важен).
- **Кто:** 16/04. **Анти-пример:** копировать `mulberry32` в каждый новый файл —
  импортировать из слоя; свою гауссиану — тоже (уже есть в __EXP_LIB__).

### 2.6. E4-шаблоны ритуалов (15 в каталоге, см. реестр §9)

`rituals/catalog/*.json` — расширение БЕЗ форка движка; новые шаблоны валидируются схемой;
тексты — `i18n/ru/rituals.json`, EN-токены `{{term:forceexit}}`. Анти-пример: хардкод
ритуала в JS-виджете.

## 3. Данные и датасеты

| id | Где живёт | Контракт | Кто | Анти-пример |
|---|---|---|---|---|
| glossary.ft.json | спека «Песочница Freqtrade» (freqtrade-sandbox-spec.md, пак 1.10) | глоссарий FT-терминов песочницы | 16 | второй FT-глоссарий рядом с Lexicon — маппить в него |
| scenario.json | там же | сценарий песочницы (детерминированный) | 16 | свои сценарии кодом |
| linter.cases.json | там же | кейсы конфиг-линтера | 16 | дубли в тестах движка |
| банк E6 (34 кейса) | tz3-specs/02 §6 | формат кейса из спеки; пополнение ≥3+1чистый/трек, ≥3:1 | 16/17/01/15 | «приватный» набор утечек в ВК3 — идти в общий банк |
| фикстуры FT-15/FT-19(2)/FT-20 | tz3-specs/20/21/22 | числовые эталоны приёмки (сделка, сверка dry-run, чек-лист 20/20) | 16/04 | пересчитывать «похожие» числа — сверять с фикстурой |
| банк «В» (20 задач) | content/03-quizzes/bank-v-write-line-python.md | формат задач тренажёра «Напиши строку сам» + контракт раннера (py-write-line-trainer-spec.md) | 16/17 | свой формат задач рядом |
| банки ТЗ-1 (603 позиции) | content/03-quizzes/ (8 файлов) | формат «часть 4»: стем/4 варианта/пояснения/ключ; `correct_option_id`, не индекс | 01/04 | перегенерация вопросов «по мотивам» без сверки с банком |
| реестр чисел лендинга | landing-spec.md §0 [FIX] | ТОЛЬКО эти числа на странице; любое новое — согласование | 03/04 | вписать «красивую цифру» мимо реестра |
| params_moex.json | создаёт 04 по решениям §7 реестра | юр-чувствительные числа РФ-трека «проверить на дату» ⚑ | 04/01 | числа ставок/тарифов в текстах уроков или коде |
| params_life.json | там же | то же для Ж-трека (страны, пороги, сроки) | 04/01 | |
| Lexicon seed 245+ | tz3-specs/04 (схема §1.2) | токены: `ru`, `ru_simple` (core ~120), EN-дубль, пример из урока; переопределение по уроку/блоку | 16 (ядро), 01/17 (токены треков) | второй словарь «для электчивов» — только записи в Lexicon |
| канон чисел курса | manifest/course-canon.json (создаст 04) | 55% / R 1.3 / риск 1% / серия 7–8 / $1000 / ED 10–25 / 8 недель ≥50 / 8–10 мес / 40/40/20 — единый источник | ВСЕ | число в тексте ≠ канону без пометки «учебный пример» |
| семейства персонажей | см. контракт стиля §5 | Алексей (сквозной), Марина/Соня/Тимур/Игорь (Ж), Криптик (2 регистра) | 15/16/17 | новые имена/биографии |
| «Дневник Алексея» | фазы 2–5 (в v15.1) + smru (33 вставки) | жанр: 3–6 строк, первое лицо, без цифр доходности; НЕ в Ж-треке | 15/04 | «дневник Марины» |
| «Ткни в непонятное» (≈186 определений) | electives-specs + smru + loc | термины → SRS-карточки (лицо RU / оборот RU+EN+пример+ссылка) и → Lexicon | 01/17 | дубли определений между SRS и Lexicon |

## 4. Слои и подсистемы

| id | Где живёт | Контракт | Кто | Анти-пример |
|---|---|---|---|---|
| Языковой слой | tz3-specs/04 | Lexicon + компоненты A–E (A «Ткни», B переключатель, C карточки ошибок, D озвучка формул, E паутина); волны L-0…L-5; покрытие ≥95% Py/FT/FAI/ВК, ≥85% Ф1–Ф5 | 16/17 | аннотатор, режущий HTML-подсветку (правило спеки: после подсветки, только текстовые узлы) |
| SRS-слой (T-9.3) | создают 01/17 по плану T-9.3 | Лейтнер 1/4/12/30; на инфраструктуре спейсинга ошибок v15.1 (cn_quiz_mistakes, LESSON_TOPIC), НЕ с нуля | 01/17 | новая БД карточек рядом со спейсингом |
| RAG-база наставника | rag/knowledge_base_psy.json (+ vectorize_records_psy.ndjson, rag_mentor_engine.js) | 302 атома / 15 книг; BM25; идёт из v15.1 паритетом | 04 (перенос), 17 (пополнение только через формат атома) | пятый формат атома; смешение с KR-04 |
| Детекторы живого слоя (12+18) | tz4-live-market-audit.md | карточки в поток / строки паспорта (§3.1 аудита); 8 упражнений Б.1–8; Г.1–Г.10 | 16/04 | «свой» детектор спреда — уже есть в ТЗ-4 |
| Диспетчер T-4.7 | engine/core/dispatch.js (создаст 04, V16-ARCH §5.1) | ЕДИНСТВЕННАЯ точка рендера интерактивов; проверка `window.EXPERT_WIDGETS[id]` перед штатным рендером; слушает 3 имени артефактов | 04; все агенты через него | прямой вызов EXPERT_WIDGETS из урока в обход диспатчера |
| golden-эталон 225 | build/golden/ (создаст 04) | DOM-снапшоты уроков v15.1; критерий Фазы 1 | 04 | «улучшить» DOM при переносе (ADR-001 §13.4-1) |
| _registry.json виджетов | content/02-interactives/widgets/_registry.json (заведёт 16) | машинный индекс кода/двигателей/инстансов; точка возобновления агента 16; читают 02/03/04/05 | 16 ведёт; 05 синхронизирует с этим каталогом | коммит кода виджета без строки в _registry.json |

## 5. Готовый код ТЗ-2 (48 виджетов) — реестр для переиспользования

Полный список со статусами — `V16-CONTENT-USAGE-REGISTRY.md` §5 (id W-01…W-48 + 3 слоя);
инженерные детали — widgets/README («Спец-факты»: двойные поставки W-03/W-13, фикс W-35,
W-39 три id, W-16 досылка, W-47 судьба Б10). **Переиспользование:** новый виджет,
дублирующий механику существующего (стакан/монетка/веер средних/Келли…) = RETURN.
Правки W-05/06/07/08/10/11/12 — из карточек ТЗ-7 (math-rewrite-widget-cards.md), применяются
К коду ТЗ-2, не создают копии.

## 6. Порядок слияния нового общего кода (для агента 16)

1. Проверка: каталог (этот файл) + `widgets/README.md` + `_registry.json` — механика уже есть?
2. Если да — переиспользовать/расширить данными; если нет — создать с контрактом §2.4/§2.5.
3. Новая ОБЩАЯ сущность → запись в `_registry.json` + уведомление агента 05 (worklog) →
   агент 05 добавляет её в этот каталог (следующий апдейт) — двухсторонняя синхронизация.
4. Изменение КОНТРАКТА существующей сущности — только через агента 05 (это разрыв
   потребителей) и версию каталога.

---
## 7. Изменения каталога

- v1.0 (2026-09-10, агент 05): начальный состав — двигатели E1–E8, механики (ситуация-выбор,
  ЖИ-движок, конструктор 5.6, паттерны ТЗ-2, слои), данные/датасеты, слои, код ТЗ-2.
  Ожидает синхронизации: `_registry.json` (ещё не создан агентом 16).
