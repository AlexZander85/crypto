# Worklog — КриптоНавигатор, режим «Обучение»

---
Task ID: stage2
Agent: Super Z (main)
Task: Этап 2 — тесты, экзамены и карточки в плеере «🎓 Обучение» (по ТЗ v2 §11 и патч-плану Этапа 2, P0–P10)

Work Log:
- Прочитаны ТЗ v2, Отчёт Этапа 1, Патч-план Этапа 2; верифицированы все якоря в коде v12.3 (renderPhaseTestView 18155, calcPhaseTestResult 18306 с alert 18366/18371, finishMathTest 27724 с alert 27725/27731, shuffledOptions 36316, обёртка checkPhaseTestAnswer 36446, плеер Этапа 1 в блоках learn_player_css/js).
- P0: счётчики банков подтверждены дампом (25/12/10/12/12/12/202/38/10/10/10/25/21); dump_before.json снят.
- Написан модуль Этапа 2: stage2_css.css (64 строки) + stage2_js_part1..4.js (1520 строк) — тестовый режим TS, порции, черновик A17, staging-DOM, resume, разбор, пересдача, диагностики A16, пикер 🏁, карточки FC, навигация фаз, обёртки каркаса Этапа 1 (goNext/goPrev/buildRoot/closePlayer/openPlayer/finishHtml/injectHomeBanner/updateChrome/toggleHotkeys), API openTest/onTestResult/openFlashcards/selfTest, smoke lp2:*.
- inject_stage2.py: идемпотентная сборка index_v12.4.html (2 вставки в блоки плеера + 4 перехвата alert дословным переносом аргумента).
- Найдены и исправлены баги в ходе приёмки: (1) smoke-проба cn_learn_test затирала реальный черновик при каждой загрузке; (2) falsy-баг TS.ph = r.ph || null ломал p0 (ptest_null_0); (3) renderTestStep перезаписывал удалённый после сдачи черновик обратно в LS; (4) делегат фазовых ответов проверял !b3.disabled — кнопка уже дисейблилась inline-обработчиком, ответ не попадал в черновик; (5) resume не предзаполнял TS.visited — порции до pos были некликабельны в карте; (6) у мат-тестов не появлялся resume-диалог (touched не учитывался).
- Приёмка (Playwright/Chromium, headless): quick_check1 (каркас P1), check_entries (P6: 8 слотов, 3 мат-кнопки, пикер 13, баннер главной), acceptance_a (P3 семантика, P4 resume p8 через реальную перезагрузку страницы, §7.2), acceptance_b (P5 граничные кейсы + §7.3 паритет: p1 83/83, 75→83 max, math_stats JSON-идентичен, capstone 87/87; 0 диалогов в плеере vs 4 в старом UI), acceptance_c (P6.3/P7/P8: CTA, карточки 6 шт p0_l1, навигация фаз, 1..9, разделитель ×1, 42 шага p8), acceptance_d (автопрогон 13 банков: resume→сдача→результат→разбор, 0 ошибок консоли; 360px).
- Дампы «после»: все 11 контентных секций IDENTICAL байт-в-байт; check_diff.py подтвердил дифф-контракт (2 вставки + 4 alert-строки).
- Отчёт сохранён: download/Отчёт_Этап2_тесты_и_экзамены_в_плеере.md; артефакт: download/index_v12.4.html (+ скриншоты).

Stage Summary:
- Этап 2 выполнен полностью (P0–P10), все 7 приёмочных критериев ТЗ §11.6 + A16/A18 зелёные.
- Ключевые решения: staging-DOM перед calcPhaseTestResult; соль попытки в черновике (A17); диагностики без записи стейта (A16); math-пересдача без сброса mathTestState; консолидация поднята CSS-фиксом 1001500 (A20).
- Для Этапа 3: bankMeta() уже формирует состояния 13 банков для блока «Тесты и экзамены» в Learn Home; пикер 🏁 можно перенести в хаб.

---
Task ID: stage3
Agent: Super Z (main)
Task: Этап 3 — платформенный слой: Learn Home, программа курса, якоря, поиск, конспект + рекомендация адаптивной тренировки (по ТЗ v2 §12 и патч-плану Этапа 3, P0–P9)

Work Log:
- Прочитаны ТЗ v2, отчёты Этапов 1–2, Патч-план Этапа 3; верифицированы якоря в v12.4: формула «Курс %» (renderProgressStats 22117–22136), coreLessonList/coreLessonsDoneCount (13957/13960, core=60 уроков), PHASES+сплайс фазы 8 (42760), TERMS (4422), ABBREVIATION_GLOSSARY (25136), themeToggleBtn (852), LS-словарь (13849: ключ cn_phase_tests), final startAdaptiveWorkout (31581) + обёртки applyMistakeBias/startSessionWithPrecheck (36517/36743), prepost-модалка z 120000 (36658), классы квиза correct/wrong.
- P0: dump3_before.json снят (2 764 230 байт, стабилен); calib_pct.js — реплика формулы vs DOM «Прогресс» на 6 профилях (0/1/6/19/40/2%) — число-в-число.
- Написан модуль Этапа 3: stage3_css.css (205 строк) + stage3_js_part1..3.js (1281 строка) — хаб Home (8 секций: продолжить/закладки/недавние/программа/прогресс/тесты/карточки/конспект + поиск), learnCoursePct (дословная реплика), lp3PhaseNums/Title/Counts, панельные вкладки Шаги|Программа|Конспект (cn_learn_syllabus), якорь «Фаза N: X/Y» + финал (X%, CTA «в курсе», «К тестам»), поиск (ленивый индекс, Ctrl+K, blockIndex→stepIdx), конспект (FAB, панель, inline-комментарий, undo, markdown), недавние LRU ≤5, smoke lp3:* + selfTest3, API openHome/closeHome/search/openNote/exportNotesMarkdown/_goAdaptive/_learnCoursePct.
- inject_stage3.py: идемпотентная сборка index_v12.5.html — ровно 2 вставки (check_diff3.py: PASS, правок существующих функций 0).
- Найдены и исправлены баги в ходе приёмки: (1) smoke3() вызывался до расширения API → lp3:home-api ложный FAIL — перенос после назначения API; (2) поиск: hay для уроков конкатенировал num+title+text, из-за чего pos указывал мимо text и фрагменты нарезались без совпадения — hay=it.text (каждая запись индекса — отдельное поле); (3) коллизия классов с глобальными стилями приложения: .big (-webkit-text-fill-color:transparent — невидимый текст CTA) → lp3-big; .sub (глобальный margin 24px) → lp3-sub; (4) фокус после Esc хаба терялся (entryEl=body/устаревший элемент) — исключён body из захвата + фолбэк на кнопку «🎓» шапки; (5) тестовые артефакты: innerText поддаётся text-transform (регэкспы переведены в lowercase), offsetParent=null у position:fixed FAB, уровень 4 concept скрыт за табом, «Пропустить» prepost-опроса отменяет запуск сессии (существующая семантика — сценарий проходит опрос), консолидация памяти многошаговая.
- Приёмка (Playwright/Chromium, headless): acceptance_3a (22 OK: хаб, секции, CTA точно на шаг без диалога, возврат в хаб, фокус, программа 10×Σ213, галочки=lessonsDone, «Курс %»=DOM, недавние LRU+reload, закладки, 13 банков, черновик попытки в CTA, все кнопки <button>, 360px); acceptance_3b (21 OK: вкладки, дерево, ≤2 кликов до FT-01, ветка в LS, подсветка, якорь=бейджу, якорь→программа+drawer, Ctrl+K, подсветка mark, переход blockIndex, пустой результат, OOS/Келли, финал 20%=4/20, CTA p0_l5); acceptance_3c (14 OK: выделение→FAB→заметка, переход к шагу, комментарий, удаление+undo, markdown, pre/code исключён, 3 неверных→«Попытка 3»→финал с рекомендацией, клик→quiz.isAdaptive=true через существующий контур, Tab-ловушка, §7.2 только cn_learn_*+штатные); regression3.js (ридер, старый тест, Этап 2, 6 скриншотов); quick3.js (0 page/console ошибок).
- Дампы «после»: 11 секций IDENTICAL байт-в-байт (dump3_after.json).
- Отчёт сохранён: download/Отчёт_Этап3_платформенный_слой.md; артефакт: download/index_v12.5.html (+6 скриншотов в download/скриншоты_этап3/, README обновлён).

Stage Summary:
- Этап 3 выполнен полностью (P0–P9), все 7 критериев ТЗ §12.10 + рекомендация заказчика зелёные; дифф-контракт и дампы PASS.
- Ключевые решения: хаб — оверлей плеерного стека (не вкладка, go() не тронут); «Курс %» — дословная реплика с подписями, различающими метрики; return-home через собственный флаг _lp3ReturnHome в цепочке обёрток closePlayer; рекомендация адаптива — только CTA на финале (≥3 неверных, сессионный счётчик Этапа 1), ADAPTIVE_QUESTION_BANK не тронут (§11.9).
- Для будущих этапов: счётчик попыток легко персистировать новым ключом cn_learn_*; наставник остаётся кнопкой «💬» (встройка в поток — отдельное решение продукта); экспорт конспекта в файл — кандидат в бэклог.

---
Task ID: stage4
Agent: Super Z (main)
Task: Этап 4 — персистентный счётчик ошибок, наставник в потоке шагов (§5.5), триаж находок Этапов 1–3

Work Log:
- Прочитаны ТЗ v2 (§5.5/§6/§11.9/§12.6), отчёты Этапов 1–3; верифицированы якоря в v12.5: S.quizAttempts (59383/60117/60610), quizPassed (59749), attachStepDelegates (60057), renderBottom (60182), lp3CourseFinaleHtml (стадия-3), finaleExtrasHtml-цепочка (62535/63689), mentorOpenPanel (47183) + v10-обёртка (52138), MENTOR.ask/buildLessonText (47038), mentorTier/upsell (47014/47153, id #mentor_upsell_modal, z 100000), TS.active (61068), рендер шага (59954, rAF без guard на 60010).
- Написан модуль Этапа 4: stage4_css.css (18 строк: z-фикс апселла 1000500 + мобайл-футер) + stage4_js_part1..3.js (361 строка): LS cn_learn_mist={lid:{n,ts}}, хук инкремента обёрткой attachStepDelegates (слушатель ПОСЛЕ родного → паритет 1:1 по построению), обёртка lp3CourseFinaleHtml с синхронным глушением сессионного счётчика (без строковой хирургии, без дублей), карточка финала с честной пометкой «(всего, с учётом прошлых сессий)», сброс по CTA (_goAdaptive) и «Не напоминать» (_mistReset), обёртка mentorOpenPanel (авто-контекст шага: idx/total/title/kind + фрагмент ≤420 симв. из DOM), карточка шага в панели + «Подсказка по этому шагу» через существующий MENTOR.ask('socratic', lid, {step, stepTitle, stepText}), футер-кнопка «💬 Наставник» (renderBottom), selfTest.lp4 + smoke4 (5 проверок lp4:*).
- inject_stage4.py: идемпотентная сборка index_v12.6.html (2 вставки: CSS перед </style>, JS перед маркером конца Этапа 3); check_diff4.py: PASS (+379 строк, правок 0).
- Найдено и исправлено в ходе приёмки: (1) опечатка склонения «попыткки» → повёрнута морфология (неверн[ая/ые/ых] попытк[а/и/ок]); (2) тест-сценарий накопления между сессиями перестроен: после сдачи квиз блокируется навсегда (cn_lesson_checks.quizDone) — накопление идёт ДО прохождения (2+2 между сессиями → total=4, метка «Попытка 2» сессионная); (3) регэксп §7.2 ослаблен до cn_* (cn_learn_pos/recent/syllabus — штатные ключи плеера), mentor_cache_* признан существующим v10-кэшем наставника.
- Спорадическая pageerror «scrollHeight» расследована: строка 60010 (v12.5:59992) — rAF-колбэк renderStep Этапа 1 без guard if(!contentEl) (в копии Этапа 2 guard есть); воспроизводится тем же жёстким автопрогоном на v12.5 (3/3) — унаследованная гонка rAF/close, в приёмке допущена по образцу ERR_FILE_NOT_FOUND.
- Приёмка (Playwright/Chromium, headless): acceptance_4a (11 OK: паритет sess/total, LS-формат, персистентность через reload, метка «Попытка N» сессионная, одна карточка на финале без дублей Э3, «Не напоминать», CTA → существующий адаптивный контур + сброс, §7.2, selfTest, консоль); acceptance_4b (11 OK: футер-кнопка, панель с шаговым контекстом z 1000002, подсказка через MENTOR.ask + лимит-счётчик, шапка-«💬» авто-контекст, v10-мост жив, demo-тире апселл НАД плеером, вызов приложения без шага, тестовый режим без карточки, 360px, §7.2, консоль); regression4 (8 OK ×3: ридер, Э1, Э2, Э3-хаб, финал с карточкой, V10.smoke 54/54, автопрогон 20 уроков вкл. ps_*/ft*).
- Дампы: dump4_after.json (2 764 230 байт) vs dump3_before.json — 14 контентных секций IDENTICAL; lsAll различается только device-id/таймстампом смоук-пробы.
- Отчёт сохранён: download/Отчёт_Этап4_персистентность_наставник_триаж.md; артефакт: download/index_v12.6.html (+5 скриншотов в download/скриншоты_этап4/, README обновлён).

Stage Summary:
- Этап 4 выполнен: все 3 пункта заказчика закрыты, дифф-контракт и дампы PASS, приёмка 30/30 + регрессия ×3 зелёная.
- Ключевые решения: два счётчика (сессионный «Попытка N» §12.6 сохранён, накопитель cn_learn_mist только для рекомендации); сброс по CTA = намерение зафиксировано; контекст наставника через payload существующего MENTOR.ask (код наставника/лимиты/фильтры не тронуты); апселл поднят CSS-фиксом 1000500 (снята оговорка §6 — необходимое условие встройки наставника); тестовый режим без шагового контекста (порции — вопросы).
- Триаж: 19 дедуплицированных позиций → 1 чинено (апселл z), 1 закрыто ранее (коллизия классов), 16 «не трогать» с обоснованием; в бэклог адресованы №1,2,6,7,13,14 (+рефакторинг №12), продуктовый вопрос №16 (prepost «Пропустить» отменяет адаптивную сессию) вынесен заказчику.
- Для будущих этапов: расширение накопителя на тестовые банки — тем же паттерном; точка серверной логики промпта шага — payload.source='learn_player_step'.

---
Task ID: stage5
Agent: Super Z (main)
Task: Этап 5 — бэклог: ридер-фиксы (№1, 2, 6, 7), персистентность _ptStart (№13), накопитель на тестовые банки, «недавние» для тестов, экспорт конспекта в файл

Work Log:
- Прочитаны отчёт Э4 (§7.1/§7.4 бэклог) и модули Этапов 2–4; пробы Playwright восстановили полную картину находок: №1 — dash рендерился веткой 14551 И общей веткой startsWith('interactive') 14776 (второй контейнер пустой); №7 — 6 draw-функций падают на осиротевших таймерах (updateP0Candle innerText @24057, g05Draw @33699, g12Draw @33882, g19Draw @34117, g20Draw — статически) + М46: undefined проходит filter(v=>v!==null) → все cy/y1/y2 NaN (в т.ч. на m_cpt ошибки «переезжали» через navLesson — сам m_cpt чист) и «Сброс» → null.reduce; автопрогон 426 открытий @35мс на v12.6 — 11 ошибок.
- Написан модуль Этапа 5: stage5_js_part1..3.js (396 строк): cn_learn_recent_tests (LRU ≤5, обе точки входа openTest — внутренняя связка и window-цепочка Э3, без дублей), секция «🕘 Недавние тесты» в хабе (bankMeta, вставка по детерминированному якорю «Тесты и экзамены»), экспорт конспекта (кнопка data-lp3-notes-dl рядом с «Скопировать», Blob+a.download, имя с датой, BOM, API exportNotesFile), накопитель на тестовые банки (submitTest: wrongs=total−correct по факту сдачи в cn_learn_mist[testId]; диагностики исключены; перерисовка результата после инкремента — карточка сразу; карточка «Тест дался непрочно» по накопленному ≥3 с CTA testId и «Не напоминать» в тестовом режиме), cn_pt_start (ридер E11/E12 + обёртки newAttempt/startTestSession — resume продолжает таймер), smoke5 (6 проверок lp5:*) + selfTest.lp5.
- inject_stage5.py: идемпотентная сборка index_v12.7.html — 1 вставка JS + 12 хирургических правок ридера (E1..E12), каждая с единственным вхождением якоря и меткой-комментарием №; check_diff5.py: строгий контракт через обратную реконструкцию v12.7 → v12.6 байт-в-байт — PASS.
- Найдено и исправлено в ходе приёмки: (1) гонка «результат рендерится до инкремента накопителя» — добавлена перерисовка экрана результата в обёртке submitTest; (2) смоук lp5:notes-dl-btn проверял кнопку на пустом конспекте — проба засеивает заметку с восстановлением LS; (3) приёмка №6 читала cn_fund_aum из LS до первой записи — переведена на ин-мемори fundAum (было 18000 «двойного» начисления — артефакт измерения); (4) helpers приёмки пересоздаются после reload.
- Приёмка (Playwright/Chromium, headless): acceptance_5a (17 OK: пустое состояние, LRU-порядок + reload, попытки p1 2→6→3 с карточкой/«Не напоминать»/CTA→quiz.isAdaptive+сброс, math_core карточка, экспорт .md с содержимым, _ptStart: фиксация/resume после reload/обнуление после сдачи, §7.2, selfTest, консоль); acceptance_5b (11 OK: 4 урока dash=1 без пустого дубля, вкладки уровней переживают перерисовку и изолированы по урокам, numericQuiz +8000 один раз/после reload 0 с пометкой, М46 точки/линии числовые + «Сброс» без падения, g05, автопрогон 426@35мс — 0 ошибок (v12.6: 11), smoke 60/60); regression5 (9 OK ×1: старый ридер, Э1, Э2, Э3-хаб+секция Э5, Э4-финал, Э5-карточка результата, V10.smoke 60/60, автопрогон 20 уроков, консоль).
- Дампы: dump5_after.json vs dump3_before.json — 14 контентных секций IDENTICAL байт-в-байт.
- Отчёт сохранён: download/Отчёт_Этап5_бэклог_ридер_и_плеер.md; артефакт: download/index_v12.7.html (+6 скриншотов в download/скриншоты_этап5/, README обновлён до этапов 1–5).

Stage Summary:
- Этап 5 выполнен полностью: все 5 пунктов бэклога Э4 §7.4/§7.1 закрыты; находки №1, 2, 6, 7, 13 переведены в ЗАКРЫТО; №8/№14/№12-рефакторинг и продуктовый вопрос №16 остаются у владельца.
- Ключевые решения: 12 правок ридера — минимальные, с метками № и постатейной таблицей отката (§3 отчёта); накопитель тестов считает неверные ответы сданной попытки (единица следует за механикой черновика), диагностики не считаются; cn_pt_start — общий ключ ридера и плеера (0 = попытка завершена); «недавние тесты» — история открытий с bankMeta-состояниями.
- Красные линии: контент 14/14 IDENTICAL, ADAPTIVE_QUESTION_BANK не тронут; плеер — только обёртки; правки ридера — по прямому решению владельца продукта с полным протоколом.

---
Task ID: stage6
Agent: Super Z (main)
Task: Этап 6 — встроенные практикумы (принцип + реализация), accessibility фокуса плеера, ИИ-верификация метода Фейнмана

Work Log:
- Разведка пробами: (1) LearnPlayer._goSim (Э1) закрывал плеер и уходил на вкладку «Тренажёров» — 42 сим-ссылки фаз 0–5, у ВСЕХ есть #sim_* боксы (встраивание возможно); фазовые 7–9 (мат/психология/Freqtrade ft01–ft12) сим-ссылок не имеют, ft-лабы внутренние (ai_agent_task/копирование, 0 внешних URL); (2) a11y-эмпирика v12.7: initial focus (кнопка «✕») и return focus (S.entryEl) работают, aria-modal отсутствует; (3) контур наставника v10 УЖЕ содержит экшен MENTOR.ask('feynman') (вердикт understood/partial/missed + advice + gaps, мок честно помечен, ДЕФ-22) и рендер window.mentorF1 (лимиты/фильтр/cn_feynman_ai) — на шаге Фейнмана не был размещён; renderFeynmanBox переопределён B3-версией (общий для ридера и плеера feynmanHtml Э1).
- Модуль Этапа 6: stage6_css.css (19 строк) + stage6_js_part1..3.js (290 строк): P1 — оверлей практикума learn-overlay[data-ov="lp6sim"] с DOM-переносом узла #sim_<id> (без клонов/дублей id), showSim-инициализаторы до переноса, восстановление узла при ЛЮБОМ закрытии (обёртки closeOverlays + closePlayer + startTestSession + собственный Esc-обработчик), фолбэк на прежнее поведение при отсутствии бокса; P2 — aria-modal="true" на buildRoot (урок+тест) и Home (хаб), фокус в модал ридера при readerWasOpen (точка входа могла быть валидной, но семантически устаревшей — кнопка «🎓» под модалом), фокус-менеджмент оверлея (role=dialog, фокус на «✕», возврат в шаг); P3 — кнопка «🤖 Спросить Наставника: понятно ли я объяснил?» в renderFeynmanBox (ридер+плеер) → LearnPlayer._feynmanAsk → СУЩЕСТВУЮЩИЙ window.mentorF1 (код наставника не тронут, §5.5/§11.9), guard короткого ввода (<10 симв. — подсказка без расхода лимита), контейнер вердикта aria-live.
- inject_stage6.py: идемпотентная сборка index_v12.8.html — ровно 2 вставки (CSS 19 строк + JS 290 строк), 0 правок; check_diff6.py: обратная реконструкция v12.8 → v12.7 байт-в-байт — PASS.
- Найдено и исправлено в ходе приёмки: (1) закрытие ПЛЕЕРА при открытом практикуме теряло перенесённый бокс (closePlayer не вызывает closeOverlays) — restore добавлен в обёртку closePlayer; (2) возврат фокуса при входе из ридера шёл на валидную, но семантически устаревшую точку входа (кнопка «🎓» под модалом) — при readerWasOpen фокус всегда в модал ридера; (3) §7.2-проверка приёмки не учитывала задокументированный в Э4 кэш mentor_cache_* — добавлен в допуск.
- Приёмка (Playwright/Chromium headless, ?mockai=1): acceptance_6 (12 OK: оверлей внутри .learn-root с единственным узлом и aria-modal; возврат узла по «✕»/Esc/закрытию плеера; candle-конструктор в оверлее; фолбэк; a11y урока/теста/хаба; фокус в ридере; кнопка Фейнмана в плеере и ридере; вердикт «ПОНЯЛ» с честной пометкой ДЕМО через mentorF1; §7.2; smoke 64/64; selfTest lp3–lp6; консоль чистая); regression6.js (9 OK: старый ридер, Э1–Э5, smoke 64, автопрогон 20 уроков, консоль); дамп dump6_after.json vs эталон Э3 — 14/14 IDENTICAL.
- Отчёт сохранён: download/Отчёт_Этап6_практикумы_a11y_фейнман.md; артефакт: download/index_v12.8.html (+6 скриншотов в download/скриншоты_этап6/, README обновлён до этапов 1–6).

Stage Summary:
- Этап 6 выполнен: все 3 пункта заказчика закрыты; дифф-контракт «2 вставки, 0 правок» восстановлен после отступления Э5 (ридер-правки были санкционированы отдельно).
- Ключевые решения: DOM-перенос узлов вместо клонирования; единые точки восстановления узла (closeOverlays/closePlayer) против «полузакрытых состояний»; принцип встроенных практикумов зафиксирован документально с оговоркой Freqtrade; ИИ-Фейнман — проводка существующего экшена feynman/mentorF1, а не новый контур; фокус — дополнения (aria-modal + краевой случай ридера) к работавшим механизмам Э1/Э3.
- Для будущих этапов: новые практикумы Академии проектировать инлайн-виджетами/оверлеями по образцу Э6; §5.5-контекст наставника можно расширять payload-ом без правок наставника.
